---
title: "Native OS surfaces"
description: "Keep Mobile agent runs visible with notifications, Live Activities, haptics, and quick actions."
---

Ryu keeps useful agent state visible when the app is not in the foreground:

- On iOS, an enabled Live Activity shows a running agent on the lock screen and Dynamic Island. On
  Android, the equivalent is a low-priority ongoing agent notification.
- When a background run finishes, Ryu can post a separate finished alert. Push registration is
  scoped to the active node and only starts after the operating system permission is granted.
- Quick actions expose common entry points such as New Chat and Tasks from the app icon. Haptics
  cover sends, quick actions, and run outcomes, and respect the Haptics setting in the app.

The native capability tool reports which of these surfaces are available in the current build and
whether notifications are currently permitted. Expo Go and the browser intentionally report fewer
native capabilities than a development client or store build.

## Connection-aware native surfaces

Mobile listens to the operating system's network state, not only the result of an app request. When
Wi-Fi or cellular connectivity is unavailable, the app keeps the current route mounted and shows a
small safe-area-aware **Offline mode** banner while it waits. If the device is connected but the
selected remote node cannot be reached, the banner changes to **Node offline**, retries automatically,
and offers a manual retry. **Connection restored** confirms recovery without replacing the current
screen. The same provider sits above native Gateway, Spaces, database, multiplayer, and plugin
routes; on-device mode does not report the built-in local fallback as a remote outage.

## Companion access

Sandboxed app companions use the same read-only `host.capabilities` descriptor and, when their
approved grants allow it, the narrow native bridge methods `native.haptics`,
`native.notifications.create`, and `native.liveActivities.update`. The bridge bounds text, keeps
the node credential on the trusted host, and returns an explicit unavailable or permission failure
when the OS surface is missing.

## Tool-call explanations

The existing `@ryu/action-summary` plugin adds a first-person question plus a short out-of-band
explanation for thinking blocks and tool calls in remote Core conversations; the Mobile chat renders
that intent above the summary as a separate callout. Core's remote approval request carries the same
question and summary bridge.

On-device models receive the same one-sentence instruction before native tool use; if a model emits
a bare function call, the native loop supplies a short descriptor-based line instead. Sensitive
actions still pause on a confirmation card. The explanation is not a substitute for the permission
or confirmation boundary.

## Related pages

<Cards>
  <DocCard href="/docs/mobile/tools-and-privacy" title="Tools & privacy" description="Read the permission, approval, and offline rules." />
  <DocCard href="/docs/extend/develop/extensions/native-bridges" title="Native bridges" description="Use the cross-host capability contract for extensions." />
</Cards>

Continue with [Mobile](/docs/mobile), [Surfaces](/docs/surfaces), [native bridges](/docs/extend/develop/extensions/native-bridges), [Browser extension](/docs/browser-extension), and [Mobile tools and privacy](/docs/mobile/tools-and-privacy).
