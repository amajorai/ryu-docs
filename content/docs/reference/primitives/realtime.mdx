---
title: "Realtime"
description: "Room-keyed realtime fan-out primitive: a transport-agnostic RoomRegistry mapping room id to a per-room actor with isolated ephemeral state."
tags: ["primitives", "realtime", "websocket", "presence", "fan-out"]
---

## What it is

The room-keyed realtime fan-out primitive — how live state (presence, events, collaborative
document sync) reaches every connected surface without each consumer building its own
transport.

- **`RoomRegistry`** — maps `room_id` to a per-room tokio actor that owns isolated
  ephemeral state (presence map + idle clock) behind a command channel.
- **Bounded broadcast fan-out** per room.
- **Hibernation** — rooms hibernate when idle and rehydrate on the next join.
- **RAII membership** — presence is reaped automatically when a member's guard drops.
- **Typed event contract** — the `Frame` enum over `Events` / `Presence` / `DocSync`
  channels; subscribing returns a droppable receiver that is itself the unsubscribe handle.

## Swap seam

Consumed as a non-optional in-process path dependency — the chat fan-out path drives it
directly, never over IPC. Zero dependency on `apps/core`. Transport-agnostic by design: the
same registry backs WebSocket and any future realtime transport.

## Where it lives

`crates/core/realtime`

## API surface

- `/api/realtime/ws` (see the
  [Realtime API reference](/docs/develop/api-reference/core/core/realtime_ws)).

## Related

<Cards>
  <DocCard href="/docs/primitives/collab" />
  <DocCard href="/docs/primitives/vad" />
  <DocCard href="/docs/develop/api-reference/core/core/realtime_ws" />
</Cards>
