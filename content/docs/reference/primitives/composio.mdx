---
title: "Composio"
description: "Composio integration orchestration: the user's account seam — toolkit/action/trigger catalog, connections, and triggered execution."
tags: ["primitives", "composio", "integrations", "tools", "webhooks"]
---

## What it is

The Composio integration seam — the connection between Ryu and the user's Composio account.
It owns:

- the **preferences-first API-key resolver**,
- the **toolkit / action / trigger catalog client**,
- **connection initiate/status**,
- the **persisted trigger-subscription store** (poll + fail-closed HMAC-SHA256 webhook
  verification), and
- the **MCP execute-action HTTP path** with connection-required elicitation detection.

This is how "works with any integration" stops being a promise: Composio's thousands of
app actions surface as ordinary tools, with the same governance as everything else.

## Swap seam

In-process default (no IPC on any hot path). The only workflow/agent-engine couplings —
run a workflow or an agent when a trigger fires — invert through the `ComposioHost` trait,
so the crate has zero dependency on `apps/core`. The gateway's governed-egress Composio
execute is a separate package.

## Where it lives

`crates/core/composio`

## API surface

- `/api/composio/*` — connections, toolkits, actions, and triggers (see the
  [Composio API reference](/docs/develop/api-reference/core/composio)).

## Related

<Cards>
  <DocCard href="/docs/primitives/tool-registry" />
  <DocCard href="/docs/primitives/webhook-ingress" />
  <DocCard href="/docs/develop/api-reference/core/composio" />
</Cards>
