---
title: "Official Apps & System Apps"
description: "Every app Ryu ships in the store, whether it is enabled on a fresh install, its default sidecar port, and its default settings."
tags: ["Defaults", "Apps"]
---

Ryu ships an official app catalog. On a fresh install the picture is simple: the
**pre-installed** apps below are the only ones with a record, and each of those is seeded **enabled**.
Every other official app has **no record at all** - the Store lists it as available, and Install →
Enable from the Store creates it. See [App lifecycle](/docs/core/app-manifest-lifecycle) for the
mechanism.

System apps are Core-owned and ship with the runtime. Other apps in these tables are official
marketplace packages: they are verified by Ryu, but installed, updated, and removable as packages.
The tables below list the per-app defaults that matter.

## Enabled on first run

These are seeded **enabled** on a fresh install. They are the batteries.

### Apps

| App | Sidecar port | Default model | Notes |
|---|---|---|---|
| Calendar | - (in-crate) | - | Companion page was always-on before extraction |
| Dictation | - (Core Voice Recognition) | whisper `ggml-base.en.bin` | Previously hardcoded into Island; the plugin is now the single switch |
| Learning | - (in-crate) | - | Owns the `learning.skills-enabled` / `learning.enabled` consent switches |
| MarkItDown | 8094 | - | The default `document.parse` provider |
| Voice | - (in-crate) | kokoro Audio / whisper Voice Recognition | W0 data-path shell |
| Webhooks | - (in-crate) | - | Companion page was always-on before extraction |
| WhatsApp | - (Gateway channels) | - | Native workspace entry for Personal and Business (Cloud API) channel setup |

### Core subsystems (fixtures)

These ship as manifests so Core can describe its own subsystems to the plugin lifecycle. They are
all pre-installed; several are mandatory or load-bearing:

| App | Status | Notes |
|---|---|---|
| Engines | **Load-bearing** + pre-installed | Local llama.cpp chat (`:8080`) + embeddings (`:8081`); the model `gemma-4-E2B-it-Q4_K_M` |
| Durable | **Load-bearing** + pre-installed | In-process durable workflow engine |
| Agents | **Load-bearing** + pre-installed | Agent catalog the composer reads |
| Spaces | **Mandatory** + pre-installed | Document/workspace root every retrieval path resolves through |
| RAG | **Mandatory** + pre-installed | Retrieval; requires Engines |
| Layers | **Mandatory** + pre-installed | Toolkit defaults (search limit, crawl depth, crawl page limit) |
| Skills | **Mandatory** + pre-installed | Skill injector |
| Media | **Mandatory** + pre-installed | Image/render path |
| Hardware | **Mandatory** + pre-installed | Device probing the engine picker reads |
| Output Styles | Pre-installed | The composer's nine built-in styles |

## Available in the Store (install-on-demand)

Every other official app ships **without a lifecycle record** - a fresh machine does not list it as
installed, and uninstalling one stays uninstalled. Most are opt-in for a deliberate reason (a key,
a binary that is not on a normal install, or a capability you should switch on). Install them from
the Store; the install path attaches any compiled-in companion UI bundle.

### Space-document boards

| App | Port | Notes |
|---|---|---|
| Whiteboard | - | Owns Space docs via `spaces:docs`; Excalidraw + Mermaid |
| Canvas | - | Space docs + media generation nodes |

### Sidecar apps

| App | Port | Default model | Notes |
|---|---|---|---|
| Research | 7995 | - | MCP tools; Python autoresearch on `:8087` |
| Dashboards | 7997 | - | Started eagerly; `DEFAULT_REFRESH_RATE` 300 s |
| Teams | 8002 | - | Coordination defaults to `broadcast` |
| Clips | 7992 | - | Requires Shadow |
| Recipes | 7999 | - | Requires Ghost |
| Social (Outpost) | 8005 | - | Publishes under your accounts - deliberate opt-in; poll 30 s, claim lease 300 s |
| Subtitles | 8013 | `gemma-4-E2B-it-Q4_K_M` | Translation model; Voice Recognition whisper `:8090`; target language `en` |
| RLM (Deep Read) | 8014 | - | Reads files in your home dir - deliberate opt-in; chunks 6 000 chars |
| CRM (Harbor) | 8009 | - | Page size 50, currency USD, max rating 5 |
| Mission Control | 8010 | - | Side model via `hook:side-model` |
| Drafts | 8012 | - | Armed-send outbox |
| Reasoning | 8006 | - | Inert until a policy is written |
| Blueprint | 8011 | - | Inert until an agent publishes a plan |
| Tuition | 8007 | - | Daily minutes 20, ready threshold 0.80, target mastery 0.90 |
| News (Wire) | 8008 | - | Brief at 07:30, item cap 25, dedupe `standard` |
| UGC | 8004 | - | Metric refresh every 6 h |
| Desktop (Virtual Desktop) | 8015 | - | Xvfb + VNC toolchain; lazy, idle-stops |
| Simulator | 7994 | - | iOS `simctl` / Android `adb` |
| Mail (Agent Inboxes) | 7996 | - | Max body 26 MiB |
| Finetune | 7990 (+8086) | - | Rust proxy + Unsloth Python trainer |
| Meetings | 7998 | `gemma-4-E2B-it-Q4_K_M` (notes) | whisper `:8090`, diarize `:8087` |
| Monitors | 8003 | - | Interval `5m`; transition-gated alerts |
| Quests | 7991 | `gemma-4-E2B-it-Q4_K_M` (judge) | Detection cadence `2m`; modes off/manual/assist/auto |
| Workflows | - (in-crate) | - | Petgraph DAG; permission levels view → run → edit → delete |
| Activity | - (in-crate) | - | Read-only unified feed |
| Approvals | - (in-crate) | - | Approval inbox |
| Timeline | - (Shadow-side) | - | Shadow device-local, `:3030` |
| Skill Editor | - (in-crate) | - | SKILL.md authoring with version history |
| Warmup | - (in-crate) | - | Default ping times 05:00 / 10:00 / 15:00 / 20:00 on each agent's cheapest model |

### Document parsers (opt-in weight)

The four `document.parse` providers. Only MarkItDown (above) is pre-installed; these three are
deliberately heavy (pip installs, Torch stacks, model downloads):

| App | Port | Notes |
|---|---|---|
| Docling | 8095 | IBM Docling; ~1 GB+; models cached in `~/.ryu/models/hf` |
| Mineru | 8096 | 16-32 GB RAM advised; GPU |
| Unstructured | 8093 | 1-2 GB pip install + native helpers (poppler/tesseract/libreoffice/pandoc); ~60 formats |

### Policies and system apps

| App | Port | Notes |
|---|---|---|
| Browser | 7993 | Real-Chromium sidecar; not pre-installed because no publishable asset exists yet - the sandboxed iframe tab is the fallback |
| Predict (Autocomplete) | - (Core `/api/predict/*`) | Model `gpt-4o-mini`; sends text from arbitrary apps to a model - install-on-demand by design |
| Firewall | - (policy) | Core-tier policy; opt-in |
| Routing | - (policy) | Core-tier smart-routing policy; opt-in |
| Sandbox | - (policy) | Core-tier sandbox policy; opt-in |
| Memory | - (in-crate) | Explicit Memory surface waits for opt-in; auto-recall is kernel and stays on |
| Pxpipe | 47821 | Needs Node on PATH and a hand-configured provider |
| Scrapling | - (MCP) | Needs `pip install "scrapling[ai]"` |
| Headroom | 8787 | Cost-reduction proxy; opt-in |
| Island | - (Electron, desktop-owned) | No release auto-installs the Electron bundle |
| Safe Actions | - (Core-owned verifier and store) | Typed plan policies, reviews, certificates, and receipts; install the app to manage the surface |

## Everything else

Apps in the Store catalog that are not first-party official packages (third-party, Community-tier) are covered by the
[Apps](/docs/apps) section and the [Marketplace](/docs/surfaces/desktop/user-guide/marketplace). The tables
above are the official catalog and Core system surfaces; only the system subset ships in the Core binary.

## See also

<Cards>
  <DocCard href="/docs/reference/defaults/plugins" />
  <DocCard href="/docs/reference/defaults/ports" />
  <DocCard href="/docs/core/app-manifest-lifecycle" />
  <DocCard href="/docs/surfaces/desktop/user-guide/store" />
</Cards>
