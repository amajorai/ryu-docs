---
title: "Projects"
description: "Keep the work moving"
tags: ["Productivity"]
catalog: {"kind":"app","id":"@ryu/projects","category":"Productivity","version":"0.1.0","official":true,"builtIn":true,"system":false,"preInstalled":false,"stability":"experimental","hidden":false,"surfaces":{"gateway":"none","core":"full","desktop":"full","island":"limited","mobile":"limited","extension":"none","web":"full","cli":"list"}}
---

This page is part of the [Apps catalog](/docs/apps), [app manifest lifecycle](/docs/core/app-manifest-lifecycle), [Plugins vs Apps](/docs/extend/develop/extensions/plugins-vs-apps), [Marketplace](/docs/extend/develop/extensions/marketplace), [Gateway governance](/docs/gateway/governance) documentation path. In the same category, compare [Outreach](/docs/apps/outreach), [People](/docs/apps/people), [Quests](/docs/apps/quests), [Recursive Language Model](/docs/apps/rlm).

## Persistence semantics

Project edits are shown optimistically while they are saved. Live edits use the
node's server response as the final state; a rejected update restores the prior
value and shows an error. A task is reported as added only after its update is
persisted. Hostless preview edits use browser-local demo persistence.

## What it exposes

### Tools

### `projects.run` - Run Projects coordinator

Dispatch one request to a durable Projects coordinator.

Backend: HTTP · Method: POST

| Parameter | Type | Required | Description |
|---|---|---|---|
| `projectId` | string | yes |  |
| `prompt` | string | yes |  |

### Companion surfaces

- **Projects** (html)

## Permissions

**Grants:** `app:http`, `tool:http-egress:127.0.0.1`, `chat.sendFollowUp`, `shell:integrate`, `spaces:docs`, `storage:kv`, `ui:toast`

| Level | Description | Implies |
|---|---|---|
| **Can view** | See projects, owners, due dates, and task progress. |  |
| **Can edit** | Create projects, add tasks, update delivery status, dispatch coordinator work, and manage subscriptions. | `projects.view` |

## UI it contributes

### Apps shelf

When enabled, this app appears as one icon tile in the desktop sidebar's Apps shelf. Use sidebar sections for the app's record lists; feature navigation stays inside the Companion.

## Engine requirement

Requires Ryu >=0.1.0.

## Sidecar

Runs an out-of-process sidecar: `projects`.
