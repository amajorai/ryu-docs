---
title: "Canvas"
description: "Wire up media and agent nodes on one board"
tags: ["Creative"]
---

## What it does

A node board for generative media: wire up image, video, chat, Audio, Voice Recognition, upload, and note nodes, then run the graph through Ryu's media and agent bridges. Each board persists as a Space document, so it stays editable, shareable, and searchable.

## Surfaces

| Surface | Support |
|---|---|
| gateway | none |
| core | none |
| desktop | full |
| island | limited |
| mobile | limited |
| extension | none |
| web | full |
| cli | list |

## What it exposes

### Companion surfaces

- **Canvas** (html)

## Permissions

**Grants:** `spaces:docs`, `core:list_agents`, `media:generate`, `media:transcribe`, `hook:run-agent`, `hook:side-model`

| Level | Description | Implies |
|---|---|---|
| **Can view** | Open canvases and read their contents. |  |
| **Can edit** | Create and change canvases, including the documents they store in Spaces. | `canvas.view` |
| **Can generate media** | Produce images and transcriptions inside a canvas, which spends model credits on every generation. | `canvas.edit` |

## UI it contributes

### Sidebar sections

- **Canvas**

## Dependencies

Requires these apps:
- `@ryu/spaces`

Needs these grants: `spaces:docs`

## Engine requirement

Requires Ryu >=0.1.0.
