---
title: "Recipes"
description: "Record a UI sequence once, replay it forever"
tags: ["Automation"]
---

## What it does

Desktop-automation recipes: record → save → replay UI action sequences. Backed by Ghost's RecipeStore, so it declares Ghost as a dependency and the graph refuses to disable Ghost out from under it.

## Surfaces

| Surface | Support |
|---|---|
| gateway | none |
| core | full |
| desktop | full |
| island | full |
| mobile | full |
| extension | full |
| web | full |
| cli | full |

## Permissions

**Grants:** `ghost:record`

| Level | Description | Implies |
|---|---|---|
| **Can view** | Browse saved desktop-automation recipes and the steps each one replays. |  |
| **Can record** | Record and save new recipes, which captures every UI action performed while recording. | `recipes.view` |
| **Can replay** | Replay a saved recipe, which drives this machine's real mouse and keyboard against whatever is on screen. | `recipes.view` |

## UI it contributes

### Hook events

- **Desktop recording finished** - Fires when a recording session is stopped (POST /api/recipes/record/stop) and the captured action sequence has been turned into an editable recipe draft. The draft is NOT persisted anywhere - `save` is a separate, later call - so it travels in the payload: a consumer that wants to refine or install it must take it from here. Fires once per session; a session that is never stopped never fires. If the draft would push the payload past Core's event-size cap, it is omitted and `draft_omitted: true` is set - the event still fires, so a subscriber learns the recording ended and can tell the draft was shed rather than empty.
- **Recipe replay succeeded** - Fires when a recipe finished replaying against the native desktop without erroring. Carries ghost's structured run result (per-step success/timing), which is the only place that detail exists - nothing persists a run record. Fires once per replay, so a recipe run on a schedule fires on every run.
- **Recipe replay failed** - Fires when a replay could not complete: ghost is unreachable, the recipe is missing, or a step's target could not be found on screen. `error` is the same message the caller received. A replay raises either this or `replay.ended`, never both.

## Dependencies

Requires these apps:
- `ghost` (min `0.1.14`)

## Engine requirement

Requires Ryu >=0.1.0.

## Sidecar

Runs an out-of-process sidecar: `ryu-recipes`.
