---
title: "Clips"
description: "Find the moments worth sharing"
tags: ["Media & Voice"]
---

## What it does

A local-first video repurposing workspace for importing or capturing recordings, finding transcript-led moments, reviewing short-form formats, editing vertical previews, and preparing exports. The out-of-process ryu-clips sidecar owns the Core→Shadow capture and ingest surface.

This page is part of the [Apps catalog](/docs/apps), [app manifest lifecycle](/docs/core/app-manifest-lifecycle), [Plugins vs Apps](/docs/extend/develop/extensions/plugins-vs-apps), [Marketplace](/docs/extend/develop/extensions/marketplace), [Gateway governance](/docs/gateway/governance) documentation path. In the same category, compare [Dictation](/docs/apps/dictation), [Subtitles](/docs/apps/subtitles).

## Surfaces

| Surface | Support |
|---|---|
| gateway | none |
| core | full |
| desktop | full |
| island | full |
| mobile | full |
| extension | full |
| web | full |
| cli | full |

## What it exposes

### Companion surfaces

- **Clips** (html)

## Permissions

**Grants:** `app:http`

| Level | Description | Implies |
|---|---|---|
| **Can view** | Browse and play back recorded clips, including whatever was on screen when they were captured. |  |
| **Can capture** | Record new clips and ingest external ones, which starts screen capture on this machine. | `clips.view` |

## UI it contributes

### Apps shelf

When enabled, this app appears as one icon tile in the desktop sidebar's Apps shelf. Use the app's sidebar sections for record lists; feature navigation stays inside the Companion.

### chat widget templates

- **Clips widget** - Clips chat widget metadata is coming soon; no render tool is available yet.

### Hook events

- **Clip recording started** - Fires when POST /api/clips/start has actually begun a capture on Shadow and a clip id exists. A start Shadow refused, or one that never reached it (the fail-soft `&#123; available: false &#125;` body), does not fire. The clip has no video and no duration yet - those arrive with `clip.ready`; this is the moment to react to recording having begun (announce it, arm a timer, dim a surface).
- **Clip ready** - Fires once a finished clip bundle exists - from BOTH paths that produce one: stopping a recording (POST /api/clips/&#123;id&#125;/stop) and completing an ingest of a watched URL or a local video file (POST /api/clips/ingest). `source` is how a consumer tells them apart; the bundle itself does not record which. Fires once per clip, alongside (not after) the auto-file into the Clips Space, so it still fires if that filing fails. `framesEndpoint` is the Core-served path, already rewritten off Shadow's internal one, so a consumer can fetch frames from it directly; the mp4 is at /api/clips/&#123;id&#125;/file.

## Dependencies

Requires these apps:
- `shadow` (min `0.1.14`)

## Engine requirement

Requires Ryu >=0.1.0.

## Sidecar

Runs an out-of-process sidecar: `ryu-clips`.
