---
title: "Warmup"
description: "Open your usage windows before you sit down"
tags: ["Automation"]
---

## What it does

Starts your subscription agents' rolling usage windows on your own schedule: a one-word ping to each subscription agent, on its cheapest model, at the times of day you choose (05:00 / 10:00 / 15:00 / 20:00 by default) - so a window is already open when you sit down to work.

This page is part of the [Apps catalog](/docs/apps), [app manifest lifecycle](/docs/core/app-manifest-lifecycle), [Plugins vs Apps](/docs/extend/develop/extensions/plugins-vs-apps), [Marketplace](/docs/extend/develop/extensions/marketplace), [Gateway governance](/docs/gateway/governance) documentation path. In the same category, compare [Autocomplete](/docs/apps/predict), [Recipes](/docs/apps/recipes), [Webhooks](/docs/apps/webhooks), [Workflows](/docs/apps/workflows).

## Surfaces

| Surface | Support |
|---|---|
| gateway | none |
| core | full |
| desktop | full |
| island | limited |
| mobile | limited |
| extension | none |
| web | full |
| cli | list |

## What it exposes

### Companion surfaces

- **Warmup** (html)

## Permissions

**Grants:** `warmup:crud`, `core:list_agents`

| Level | Description | Implies |
|---|---|---|
| **Can view** | See which subscription agents are warmed, on what schedule, and when each was last pinged. |  |
| **Can ping now** | Trigger a warmup ping immediately, which opens a usage window on that agent's subscription. | `warmup.view` |
| **Can configure** | Change which agents are warmed and at what times, which decides how much subscription quota is spent unattended. | `warmup.run` |

## UI it contributes

### Apps shelf

When enabled, this app appears as one icon tile in the desktop sidebar's Apps shelf. Use sidebar sections for the app's record lists; feature navigation stays inside the Companion.

### chat widget templates

- **Warmup widget** - Warmup chat widget metadata is coming soon; no render tool is available yet.

## Engine requirement

Requires Ryu >=0.1.0.
