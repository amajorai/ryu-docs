---
title: "Warmup"
description: "Open your usage windows before you sit down"
tags: ["Automation"]
---

## What it does

Starts your subscription agents' rolling usage windows on your own schedule: a one-word ping to each subscription agent, on its cheapest model, at the times of day you choose (05:00 / 10:00 / 15:00 / 20:00 by default) - so a window is already open when you sit down to work.

## Surfaces

| Surface | Support |
|---|---|
| gateway | none |
| core | full |
| desktop | full |
| island | limited |
| mobile | limited |
| extension | none |
| web | full |
| cli | list |

## What it exposes

### Companion surfaces

- **Warmup** (html)

## Permissions

**Grants:** `warmup:crud`

| Level | Description | Implies |
|---|---|---|
| **Can view** | See which subscription agents are warmed, on what schedule, and when each was last pinged. |  |
| **Can ping now** | Trigger a warmup ping immediately, which opens a usage window on that agent's subscription. | `warmup.view` |
| **Can configure** | Change which agents are warmed and at what times, which decides how much subscription quota is spent unattended. | `warmup.run` |

## Engine requirement

Requires Ryu >=0.1.0.
