---
title: "Webhooks"
description: "See every inbound webhook this node answers"
tags: ["Automation"]
---

## What it does

Inbound webhook endpoint registry: resolved public URLs, signing-secret configuration, last-delivery times, and the ingress backend status.

This page is part of the [Apps catalog](/docs/apps), [app manifest lifecycle](/docs/core/app-manifest-lifecycle), [Plugins vs Apps](/docs/extend/develop/extensions/plugins-vs-apps), [Marketplace](/docs/extend/develop/extensions/marketplace), [Gateway governance](/docs/gateway/governance) documentation path. In the same category, compare [Recipes](/docs/apps/recipes), [Warmup](/docs/apps/warmup), [Workflows](/docs/apps/workflows).

## Surfaces

| Surface | Support |
|---|---|
| gateway | none |
| core | full |
| desktop | full |
| island | limited |
| mobile | limited |
| extension | none |
| web | full |
| cli | list |

## What it exposes

### Companion surfaces

- **Webhooks** (html)

## Permissions

**Grants:** `webhooks:crud`, `ui:toast`

| Level | Description | Implies |
|---|---|---|
| **Can view** | See registered inbound endpoints, their public URLs, signing-secret status, and when each last received a delivery. |  |
| **Can manage** | Configure and rotate inbound signing secrets, which decides which outside systems may trigger work here. | `webhooks.view` |

## UI it contributes

### Apps shelf

When enabled, this app appears as one icon tile in the desktop sidebar's Apps shelf. Use sidebar sections for the app's record lists; feature navigation stays inside the Companion.

### chat widget templates

- **Webhooks widget** - Webhooks chat widget metadata is coming soon; no render tool is available yet.

## Engine requirement

Requires Ryu >=0.1.0.
