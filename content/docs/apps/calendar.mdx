---
title: "Calendar"
description: "Every scheduled run on one calendar"
tags: ["Productivity"]
---

## What it does

The scheduled-runs calendar: every agent and workflow scheduled job projected onto Month, Week, Day, and Agenda views - past runs from history, upcoming runs from cron/interval - with a New automation dialog that schedules an agent.

The **Agenda** list opens with a status-page overview for the selected 24-hour day: one global strip
for all scheduled jobs, followed by one strip per job. The strips run from midnight to midnight:
green marks completed runs, red marks failures, and amber marks projected scheduled runs. The
chronological run rows remain below the overview, and dense schedules are represented as one labeled
aggregate segment when individual firings would make the list unreadable.

## Surfaces

| Surface | Support |
|---|---|
| gateway | none |
| core | none |
| desktop | full |
| island | limited |
| mobile | limited |
| extension | none |
| web | full |
| cli | list |

## What it exposes

### Companion surfaces

- **Calendar** (html)

## Permissions

**Grants:** `calendar:crud`, `core:list_agents`

| Level | Description | Implies |
|---|---|---|
| **Can view** | See past and upcoming scheduled runs for every agent and workflow on this node. |  |
| **Can schedule** | Create, reschedule, and remove automations, which decides what runs unattended on this node and when. | `calendar.view` |

## UI it contributes

### chat widget templates

- **Calendar widget** - Calendar chat widget metadata is coming soon; no render tool is available yet.

## Engine requirement

Requires Ryu >=0.1.0.
