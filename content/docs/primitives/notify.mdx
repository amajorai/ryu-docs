---
title: "Notify"
description: "Shared notification-delivery wire types and send primitives: swappable channel targets — webhook, Telegram, Expo push, BYO SMTP."
tags: ["primitives", "notifications", "notify", "webhook", "telegram", "push"]
---

## What it is

The shared notification-delivery wire types + send primitives — the swappable **channel
targets** (webhook / Telegram / Expo push / BYO SMTP email recipient) and the dep-light HTTP
send functions that back monitor alerts, policy alerts, workflow channel-send, and
user-scoped notifications.

The kernel notification *store* and fan-out orchestration stay Core-side
(`apps/core/src/notify`); this crate holds only what Core and the out-of-process monitors
engine **both** need, so they can share one delivery vocabulary without linking each other.

## Swap seam

Zero dependency on `apps/core`. In-process by default.

## Where it lives

`crates/core/notify`

## Related

<Cards>
  <DocCard href="/docs/primitives/activity" />
  <DocCard href="/docs/primitives/email-send" />
  <DocCard href="/docs/develop/api-reference/core/notifications" />
</Cards>
