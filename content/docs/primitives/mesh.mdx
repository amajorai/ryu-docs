---
title: "Mesh"
description: "Mesh read/shape primitive: the optional Tailscale/Headscale plane — status shaping, fail-closed peer auth, and Funnel helpers."
tags: ["primitives", "mesh", "tailscale", "headscale", "networking"]
---

## What it is

The mesh read/shape primitive (#478): the read side of the optional **Tailscale / Headscale**
plane that links multiple Ryu nodes. It owns:

- `RYU_MESH_ENABLED` gating.
- **`GET /api/mesh/status`** shaping from `tailscale status --json`.
- The **fail-closed shared-mesh-token bearer resolution** for `GET /api/mesh/peers` — the
  node-admittance security model this crate anchors (`enforce_remote_auth` stays in Core and
  consults `is_insecure_auth_token_placeholder` here).
- The **Funnel helpers** for public webhook ingress.

## Swap seam

In-process by default, consumed as a non-optional path dependency (the fail-closed startup
gate reads `is_enabled()` / `is_insecure_auth_token_placeholder` unconditionally). The one
kernel coupling — the `tailscale` / `tailscaled` process shell-outs, the "what runs" half of
the mesh — inverts through the narrow `MeshHost` trait.

## Where it lives

`crates/core/mesh`

## API surface

- `/api/mesh/status`, `/api/mesh/peers`, and `/api/mesh/config` (see the
  [Nodes API reference](/docs/develop/api-reference/core/nodes)).

## Related

<Cards>
  <DocCard href="/docs/primitives/webhook-ingress" />
  <DocCard href="/docs/gateway/managed-on-your-own-node" />
  <DocCard href="/docs/develop/api-reference/core/nodes" />
</Cards>
