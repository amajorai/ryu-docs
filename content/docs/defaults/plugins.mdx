---
title: "Built-in Plugins & Capabilities"
description: "Every plugin Ryu ships with, the default provider of each capability, which plugins are default-on, and each plugin's default settings."
tags: ["Defaults", "Plugins"]
---

Ryu ships plugins the same way it ships apps: compiled-in manifests with a tier, a default-on flag,
and a not-pre-installed flag. Plugins are the extension surface - tools, turn hooks, MCP servers,
Pi extensions, skills - while apps are self-contained products with a sidecar. Both share the same
lifecycle machinery; see [App lifecycle](/docs/core/app-manifest-lifecycle).

This page covers the plugin-side defaults: which capability each plugin provides by default, which
plugins are default-on, and the per-plugin settings that have defaults.

## Default capability providers

A capability (like `web.search` or `document.parse`) can have several providers. When more than one
is enabled, the read model needs a default - and for several capabilities, one provider is the
**only** one enabled on a fresh install, so it *is* the default by construction:

| Capability | Default provider | Swappable for | Notes |
|---|---|---|---|
| `web.search` | **Exa** | Tavily, Brave, Serper, Parallel, Firecrawl | Exa needs no key (public MCP endpoint fallback), so it is the one search provider safe to ship on |
| `web.extract` | **Spider** | Scrapling, Firecrawl, Tavily, Serper, Parallel | Spider is a default-on `command` tool |
| `web.crawl` | **Spider** | Spider Cloud, Firecrawl | Crawl depth default 1, limit default 10 |
| `computer.control` | **Ghost** | Bytebot (remote) | Ghost targets the local machine; Bytebot a remote desktop |
| `browser.control` | **Agent Browser** | Browser app | Agent Browser is selectable (not the default flag); the Browser app ships off |
| `document.parse` | **MarkItDown** | Docling, Mineru, Unstructured | MarkItDown is the only one cheap enough to seed default-on |
| `memory` | **Built-in store** | Mem0, Honcho | Built-in stays default; the others augment |
| `rag` | **In-process** (engines embed sidecar) | - | Mandatory |
| `engines` | **llama.cpp** (`:8080`/`:8081`) | vLLM, SGLang, MLX, OMLX, Ollama | Load-bearing |

## Default-on plugins

Like apps, most plugins ship **without a record** - they are in the catalog and one Install → Enable
away. Only a subset is seeded **enabled** on a fresh install:

### Tool & capability plugins (default-on)

| Plugin | What it adds | Notes |
|---|---|---|
| `@ryu/ghost` | `computer.control` tools | Default provider; MCP stdio |
| `@ryu/shadow` | `memory__context`, timeline tools | Local sidecar `:3030` |
| `@ryu/spider` | `web.extract` / `web.crawl` tools | Default provider |
| `@ryu/agentbrowser` | Web browsing tools | npx MCP server |
| `@ryu/exa` | `web.search` tools | Default provider, no key needed |
| `@ryu/docs` | Docs search MCP (`docs.ryuhq.com/mcp`) | Remote, fail-open, no install |

### Turn-hook plugins (default-on)

These were formerly hardcoded chat features; they ship as real, swappable plugins. Each one uses a
cheap `match` pre-gate so an idle hook never costs a sandbox spawn:

| Plugin | What it does | Defaults |
|---|---|---|
| `@ryu/goal` | Persistent goals - a one-line transcript judge | Judge = side model; max 25 turns |
| `@ryu/proof` | Proof-of-work: spawns an independent verifier sub-agent | Verifier = sub-agent (`code_read` preset); max 12 rounds |
| `@ryu/receipts` | Visual sibling of proof - captures a screenshot/recording as evidence | Evidence `any`; max 8 rounds |
| `@ryu/double-check` | Reviews answers you flag | Gated by flag/toggle; side model |
| `@ryu/chat-title` | Progressive conversation titles | Renames on first turn, then every 5 turns |

### Pi extension plugins (default-on)

These add tools to the managed Pi agent (Pi reads its extensions at process start, so toggling one
takes effect in a new chat):

| Plugin | What it adds |
|---|---|
| `@ryu/pi-shell` | Background bash (`bash_background` / `bash_output` / `bash_kill`) |
| `@ryu/pi-subagent` | Sub-agent (`Task`) tool |
| `@ryu/pi-monitor` | `monitor` tool (a port of Claude Code's Monitor) |

### Other default-on plugins

| Plugin | What it does |
|---|---|
| `@ryu/auto-expand` | Pre-turn prompt improver (composer toggle + `/expand`) |
| `@ryu/output-styles` | The composer's nine built-in styles (ELI5, I have ADHD, Explanatory, Learning, Proactive, Plain text, No AI slop, Bro, Gen Z) - a picker that must be reachable |

## Available in the Store (not pre-installed)

These ship **without a record** - a fresh machine does not list them as installed, and the Store
shows them as available. Install → Enable from the Store. Each is opt-in for a stated reason (a
per-turn model call, a binary, or a capability you should switch on):

| Plugin | What it does | Defaults | Why it ships off |
|---|---|---|---|
| `@ryu/recap` | Summarizes the turn that just finished (`/recap`) | Auto, min chars 1200, detail `standard` | A side-model call per long turn |
| `@ryu/no-ai-slop` | Runs the no-ai-slop editing skill on finished answers | Passes 1 (0-12), mode `revise` | A sub-agent per reviewed answer |
| `@ryu/no-more-mistakes` | Turns a correction into a durable rule in a Space | Capture/brief on; Space `Mistakes`; max 12 rules | A sandbox spawn per user turn |
| `@ryu/agent-comms` | Agent-to-agent mailbox (`agents__directory/send/ask/thread`) | - | Delivery hook cannot be match-gated |
| `@ryu/advisor` | `/advisor` - a stronger model reviews your work | Side model; `fail_open: true` | A side-model call per turn |
| `@ryu/mem0` | Second `memory` provider | Search limit 10, min relevance 0.1 | Needs an API key |
| `@ryu/honcho` | Memory provider (`memory__context`) | `reasoning_level: minimal`, messages 10 | Needs an API key |
| `@ryu/bytebot` | `computer.control` provider for a **remote** desktop | `http://127.0.0.1:9990/computer-use` | A capability that drives another machine |
| `@ryu/security-guidance` | `/security-review` on your code | Side model | A side-model call per turn |
| `@ryu/pxpipe` | Loopback token-saving proxy | `http://127.0.0.1:47821` | Needs Node + a hand-configured provider |
| `@ryu/headroom` | Compression (60-95% token reduction) | `:8787`, `timeout_ms` 4000, `min_messages` 4 | Needs a configured transform |
| `@ryu/rtk` | Local `rtk` CLI wrapper | Mode `wrap`, timeout 120 s | Needs the `rtk` CLI |

The full list of search/parse/security/LSP plugins and their exact settings lives on each plugin's
own page under [Plugins](/docs/plugins) - the tables above are the defaults that matter on a fresh
install.

## Mandatory and load-bearing

Two protection tiers sit on top of default-on (see the [App lifecycle](/docs/core/app-manifest-lifecycle)
page for the exact mechanics):

| Tier | Members | Behaviour |
|---|---|---|
| **Mandatory** | `@ryu/spaces`, `@ryu/rag`, `@ryu/layers`, `@ryu/skills`, `@ryu/media`, `@ryu/hardware` | Never disableable, never uninstallable, no `force` override |
| **Load-bearing** | `@ryu/engines`, `@ryu/durable`, `@ryu/agents` | Disable refused without an explicit `force = true` |

Mandatory is a strict subset of default-on, so a fresh install always has them.

## Reference-only plugins

A few plugins in `plugins-store/` are **not registered** in `BUILTIN_MANIFESTS` at all - they exist
for a third party to fork as reference implementations and never ship in the catalog:
`tool-firewall` (blocking destructive tool calls), `hook-observers` (one observer hook per event),
and `toolsmith-example` (the worked `inline_deno` tool example). `sample` / `sample-widget` are the
reference plugin / inline-chat-widget templates.

## See also

<Cards>
  <DocCard href="/docs/defaults/apps" />
  <DocCard href="/docs/defaults/models" />
  <DocCard href="/docs/plugins" />
  <DocCard href="/docs/core/unified-tool-catalog" />
</Cards>
