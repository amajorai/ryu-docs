---
title: "Git"
description: "Git API endpoints."
---

{/* Tag landing page generated by scripts/generate-docs.ts — edit the script, not this file. */}

<Cards>
  <Card href="/docs/0.2.5/extend/develop/api-reference/core/git/git_branches" title="API endpoint" description={"Lists local branches plus the currently checked-out one so the desktop's composer branch selector can offer a switch. Returns `{is_repo:false}` (HTTP 200) for…"} />
  <Card href="/docs/0.2.5/extend/develop/api-reference/core/git/git_checkout" title="{ cwd, branch }" description={"Switches the workspace to an existing local branch via `git switch` (which refuses pathspec/file behavior, so a stray branch name can't restore files). The bra…"} />
  <Card href="/docs/0.2.5/extend/develop/api-reference/core/git/git_commit_push" title="{ cwd, message?, action?, include_unstaged? }" description={"Commits, pushes, or does both. This is Core (it runs what the user asked; the Gateway is not on the raw-git path). Returns `{ success, committed, pushed, commi…"} />
  <Card href="/docs/0.2.5/extend/develop/api-reference/core/git/git_create_branch" title="{ cwd, branch }" description={"Create a new branch off the current HEAD and switch to it (`git switch -c`). The desktop only exposes this when the working tree is clean, but we re-check serv…"} />
  <Card href="/docs/0.2.5/extend/develop/api-reference/core/git/git_file_diff" title="Read the current diff for selected files" />
  <Card href="/docs/0.2.5/extend/develop/api-reference/core/git/git_init" title="{ cwd }" description={"Initializes a folder as a local repository with `main` as its initial branch. It deliberately does not stage or commit files; the normal commit action remains…"} />
  <Card href="/docs/0.2.5/extend/develop/api-reference/core/git/git_pull" title="{ cwd }" description={"Pulls the current branch from its configured upstream with fast-forward-only semantics. It never stages or commits working-tree files."} />
  <Card href="/docs/0.2.5/extend/develop/api-reference/core/git/git_pull_request" title="{ cwd, title?, body?, base?, draft?, include_unstaged? }" description={"Optionally commits local changes, pushes the current branch, and creates a GitHub pull request with the node's authenticated `gh` installation."} />
  <Card href="/docs/0.2.5/extend/develop/api-reference/core/git/git_reverse_edits" title="Reverse exact text edits from one assistant turn" />
  <Card href="/docs/0.2.5/extend/develop/api-reference/core/git/git_status" title="API endpoint" description={"Returns `{is_repo:false}` (HTTP 200) for any non-repo or missing folder so the desktop header can distinguish \"not a repo\" from \"Core unreachable.\" Tracks ahea…"} />
  <Card href="/docs/0.2.5/extend/develop/api-reference/core/git/git_sync" title="{ cwd }" description={"Pulls the current branch fast-forward-only, then pushes it to its configured upstream. It never stages or commits working-tree files."} />
  <Card href="/docs/0.2.5/extend/develop/api-reference/core/git/clone_project_folder" title="Clone a GitHub repository into a project folder" description={"Clone a GitHub repository into `~/Documents/Ryu/<name>` on the node serving this request. SSH URLs run on that node, so its existing SSH config and keys are us…"} />
  <Card href="/docs/0.2.5/extend/develop/api-reference/core/git/list_directory" title="list the sub-directories of a folder ON" description={"Placement rationale: this is Core — it reads *what is* on the node's own filesystem. Read-only: it returns directory names only, never file contents. The home-…"} />
  <Card href="/docs/0.2.5/extend/develop/api-reference/core/git/create_project_folder" title="{ name }" description={"Create a fresh, empty project folder under `~/Documents/Ryu/<name>` and return its absolute path so the desktop's \"Start from scratch\" flow can open it. This i…"} />
</Cards>
