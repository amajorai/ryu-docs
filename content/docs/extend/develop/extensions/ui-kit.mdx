---
title: "Ryu UI Kit"
description: "The source-of-truth model for Ryu's shared app primitives."
tags: ["ui", "design system", "components", "apps", "companions"]
---

Ryu apps use a shared, opinionated UI kit. The app chooses its data and domain workflow; the kit
chooses the visual grammar. Components are source code in the repository, themed by semantic tokens,
and composed into product surfaces instead of hidden behind a remote design service.

Ryu App UI `v1` is the current stable contract version, not a partial-release marker. Every source
Companion app is required to mount through `RyuAppShell`; an app is not considered ported merely
because it imports the stylesheet.

## Source of truth

The package boundaries are deliberate:

- `@ryu/ui` owns controls, fields, dialogs, menus, badges, status, loading, and theme tokens.
- `@ryu/ui/components/connection-status` owns the shared non-blocking connection toast; hosts supply
  the `checking`, `node-unreachable`, `offline`, or `online` phase from their platform signal.
- `@ryu/blocks/companion/app-ui` owns the fixed app roles: shell, toolbar, list, detail, form,
  empty state, and actions.
- `@ryu/app-host` owns the host seam: live theme subscription, root marking, and generic bridges.
- Ryu Desktop owns navigation chrome. Apps contribute `sidebar_sections`; the host represents each
  enabled app with one Apps-shelf tile in both the normal shell and standalone Ryu App windows.
- A satellite owns its domain state and bridge adapter. It does not copy or fork the visual system.

The one packaging exception is a dependency-free static satellite mirrored outside the monorepo:
it may carry a small contract-compatible v1 adapter, but it must use the same semantic tokens and
manifest-owned navigation. It cannot create a second visual language.

The public catalog and examples should be generated from these same package exports. A catalog page
that drifts from the package is not a UI kit; it is a screenshot gallery.

## Toolkit coverage

Ryu ships the UI and host capabilities across focused packages and keeps runtime authority in Core.
The coverage map below records which reusable seams are available today.

| Toolkit area | Ryu seam | Coverage |
| --- | --- | --- |
| UI primitives and app adapters | `@ryu/ui`, `@ryu/blocks`, `RyuAppShell`, semantic theme tokens | Covered |
| Agent UX | `@ryu/blocks` agent elements, composer, tool approval, runs, widgets | Covered |
| Settings | `@ryu/settings` plus host-owned Desktop settings | Covered |
| Command and navigation | `@ryu/command`, `@ryu/hotkeys`, manifest sidebar/contribution seams | Covered |
| Resources and realtime | `@ryu/app-host`, Core Spaces, realtime/resource bridges, app-owned sidecars | Covered, different ownership |
| Sharing, collaboration, history, review | Product surfaces and primitives exist, but are not one reusable kit contract | Partial |
| Observability and diagnostics | Core/Gateway traces, usage, audit, activity, and app surfaces | Covered, more platform-governed |
| Install-on-demand capability kits | Marketplace, manifests, sidecars, and portable packages | Partial |
| Inspect/add/eject with provenance and safe restore | No equivalent unit manifest/ejection workflow yet | Gap |

The important boundary is state ownership. Ryu does not centralize every app's domain tables in a
shared SQL schema. A plugin Action can use grant-gated `storage:kv`; an app with relational data
owns its SQL database in its sidecar and exposes a narrow Action or route through the extension
proxy. That is less magical than a template-local SQL convention, but it keeps one app from
becoming the authority for another app's data.

For a new app, use the existing package seams directly and keep the app's adapters local:

```tsx
import { RyuAppShell } from "@ryu/blocks/companion/app-ui";
import { Button } from "@ryu/ui/components/button";

export function App() {
  return (
    <RyuAppShell>
      <Button>Save</Button>
    </RyuAppShell>
  );
}
```

The next productization step is a manifest-backed Toolkit catalog and safe ejection workflow - not
another component barrel. The UI catalog is maintained from the package exports and source modules;
runtime and app-owned data remain behind their existing authority boundaries.

## Fixed roles

Use the smallest role that expresses the surface:

| Role | Primitive |
| --- | --- |
| Root surface | `RyuAppShell` |
| Title and actions | `RyuAppToolbar` |
| Page grouping | `RyuAppMain`, `RyuAppSection` |
| Collections | `RyuAppList`, `RyuAppListSection`, `RyuAppListItem` |
| Selected content | `RyuAppDetail` |
| Data entry | `RyuAppForm`, `RyuAppField` |
| No data / recovery | `RyuAppEmpty`, `RyuAppActions` |

Domain-specific editors, graphs, canvases, and media surfaces may keep their specialized renderer,
but their chrome and generic controls still use the Ryu kit. This keeps a canvas app recognizable as
Ryu without pretending a graph node is a generic list row. A left navigation rail is not domain art:
move it into the app manifest so Ryu can render it consistently in both hosted and standalone modes.

## App contract

```css
@import "tailwindcss";
@import "@ryu/ui/app-ui.css";
```

```tsx
import { markCompanionAppRoot } from "@ryu/app-host/companion-theme";
import { RyuAppShell } from "@ryu/blocks/companion/app-ui";

markCompanionAppRoot(document.getElementById("ryu-plugin-root"));

export function App() {
  return <RyuAppShell>{/* domain content */}</RyuAppShell>;
}
```

The repository contract check covers every source Companion entrypoint. It verifies the root marker,
the shared stylesheet, the `@ryu/blocks` dependency, and the `RyuAppShell` composition. Primary
navigation is still owned by Desktop: app-specific sections belong in manifest sidebar
contributions, while editor, canvas, and inspector rails remain domain content inside the shell.

The repository checks every first-party Companion package for the v1 root marker and the shared
stylesheet import. Rendered checks still matter: inspect light/dark themes, focus, narrow widths,
empty/error/loading states, and reduced motion.

Continue with [Extensions](/docs/extend/develop/extensions), [Ryu UI](/docs/ui), [UI getting started](/docs/ui/getting-started), [UI components](/docs/ui/components), and [Companion UI](/docs/extend/develop/extensions/companion-ui).
