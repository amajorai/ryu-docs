---
title: "Chat widget templates"
description: "Declare visual chat use cases for every Ryu app, connect them to a Library view or tool, and add richer inline widgets over time."
tags: ["chat", "widgets", "apps", "views", "manifest"]
---

Chat should be able to answer with the app's natural visual surface. A calendar app can show the
next seven days, a social app can show a queue, a quest app can show a board, and a monitoring app
can show recent checks. `contributes.chat_widget_templates[]` is the shared discovery contract for
those prompts.

## Contract

Each template has an id, title, description, examples, and trigger phrases. An available template
must bind to exactly one `tool_id` or `view_id`; a coming-soon template may omit the binding while an
app finishes its renderer:

```json
{
  "contributes": {
    "chat_widget_templates": [
      {
        "id": "next-seven-days",
        "title": "Next seven days",
        "description": "Show upcoming events in a calendar",
        "examples": ["What is happening next week?"],
        "triggers": ["next week", "upcoming"],
        "backing": { "view_id": "calendar" }
      }
    ]
  }
}
```

The template is not an HTML widget and does not grant a tool. The host validates the backing,
filters the contribution to the current surface, and applies the app's normal grants and allowlist.
Use `contributes.widgets[]` plus a render tool when the answer needs a self-contained interactive
HTML card. Use a view-backed template when the answer should open or project the app's shared Library
surface. See [contribution surfaces](/docs/extend/develop/extensions/contribution-surfaces) for the
view and widget contracts.

## Current app coverage

Every current app declares a chat template. The first backed templates are deliberately small and
use the host's existing view projections; the remaining templates are discoverable as coming-soon
entries so the chat composer can advertise the intended use case without claiming that a renderer
already exists.

| App | Example chat intent | Backing today |
| --- | --- | --- |
| Activity | “Show my recent activity” | Coming soon |
| Agent Status | “Which agents are running?” | Coming soon |
| Approvals | “Show approvals waiting for me” | Coming soon |
| Blueprint | “Open the current plan review” | `surface:blueprint` |
| Browser | “Show the pages I have open” | Coming soon |
| Calendar | “What is happening next week?” | Coming soon |
| Canvas | “Show the latest canvas” | Coming soon |
| Clips | “Show my recent clips” | Coming soon |
| Harbor (CRM) | “Show my customer follow-ups” | Coming soon |
| Dashboards | “Open the operations dashboard” | Coming soon |
| Virtual Desktop | “Show my desktop session” | Coming soon |
| Dictation | “Show my latest dictation” | Coming soon |
| Docling | “Summarize the parsed document” | Coming soon |
| Drafts | “Show drafts waiting for review” | Coming soon |
| Fine-tuning | “What is the training run status?” | Coming soon |
| Self-Healing | “Show recent repair attempts” | Coming soon |
| Learning | “What has the system learned?” | Coming soon |
| Mail | “Show unread mail from this week” | Coming soon |
| MarkItDown | “Show the converted document” | Coming soon |
| Meetings | “Show this week's meetings” | `surface:meetings` |
| MinerU | “Show the extracted document structure” | Coming soon |
| Mission Control | “What work is in flight?” | Coming soon |
| Monitors | “Which monitors are failing?” | `surface:monitors` |
| Wire (news) | “Show the latest news queue” | `surface:news` |
| Autocomplete | “Show autocomplete usage” | Coming soon |
| Pull Requests | “Show pull requests needing review” | Coming soon |
| Quests | “Put my tasks on a board” | `quest-board` |
| Automated Reasoning | “Open the reasoning checks” | `surface:reasoning` |
| Recipes | “Show recipes I can replay” | Coming soon |
| Sprout Studio | “Show scheduled social content” | Coming soon |
| Research | “Show the research results” | Coming soon |
| Recursive Language Model | “Show the corpus run trace” | `surface:rlm` |
| Simulators | “Show the latest simulation” | Coming soon |
| Skill Editor | “Show skills I am editing” | Coming soon |
| Outpost (social) | “Show next week's social queue” | `surface:social` |
| Subtitles | “Show the subtitle cues” | `surface:subtitles` |
| Teams | “Show my agent teams” | Coming soon |
| Timeline | “Show the work timeline” | Coming soon |
| Token Table | “Show token usage by run” | Coming soon |
| Tuition | “Show my learning plan” | `surface:tuition` |
| UGC | “Show the latest user content” | `surface:ugc` |
| Unstructured | “Show extracted content” | Coming soon |
| Voice | “Show the latest voice job” | Coming soon |
| Warmup | “What has been warmed up?” | Coming soon |
| Webhooks | “Show webhook deliveries” | Coming soon |
| Whiteboard | “Open the whiteboard” | Coming soon |
| Workflows | “Show workflow runs for this week” | Coming soon |

The catalog currently has 47 templates: 11 with a host-rendered backing and 36 explicitly marked
coming soon. This split is intentional and is checked by the manifest/schema tests. As an app adds a
real widget or view, it changes only the backing and renderer contract; the chat intent, examples,
and trigger vocabulary can remain stable.

## Designing good templates

- Start with a question users naturally ask in chat, not with the app's internal route name.
- Keep examples short enough to become composer suggestions.
- Choose one backing per template. Split calendar, board, and detail intents into separate templates.
- Return a bounded, useful first projection; let the user open the full app for deep editing.
- Make the no-data and unavailable states readable. A missing provider or permission is not an empty
  successful result.
- Add tests for manifest validation, backing availability, and the host's fallback renderer.

See [Library views](/docs/extend/develop/extensions/contribution-surfaces#library-views),
[native bridges](/docs/extend/develop/extensions/native-bridges), and
[built-in tools](/docs/core/built-in-tools) for the adjacent contracts.
