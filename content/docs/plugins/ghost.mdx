---
title: "Ghost"
description: "Let agents see and drive your desktop"
tags: ["Automation"]
---

## What it does

Desktop automation: 29 screen perception and input control tools. Cross-platform (Windows, macOS, Linux).

This page is part of the [Plugins catalog](/docs/plugins), [plugin manifests](/docs/extend/develop/extensions/plugin-json-manifest), [plugin runtime](/docs/extend/develop/extensions/plugin-runtime), [unified tool catalog](/docs/core/unified-tool-catalog), [Marketplace](/docs/extend/develop/extensions/marketplace), [Gateway governance](/docs/gateway/governance) documentation path. In the same category, compare [Bytebot Desktop](/docs/plugins/bytebot), [Effort Escalator](/docs/plugins/effort-escalator), [Subagents](/docs/plugins/pi-subagent), [Usage Pacer](/docs/plugins/usage-pacer).

## Surfaces

| Surface | Support |
|---|---|
| gateway | none |
| core | full |
| desktop | full |
| island | full |
| mobile | full |
| extension | full |
| web | full |
| cli | full |

## What it exposes

### Capabilities

- **Computer** - capability `computer.control`
  - Target: local-machine
  - Verbs:
    - `computer.capture` -> `ghost.ghost_annotate`
    - `computer.click` -> `ghost.ghost_click`
    - `computer.type` -> `ghost.ghost_type`
    - `computer.key` -> `ghost.ghost_hotkey`
    - `computer.scroll` -> `ghost.ghost_scroll`
    - `computer.focus_app` -> `ghost.ghost_focus`

### MCP server

MCP (Model Context Protocol) is a standard way for an AI host to find and call tools. An MCP server provides those tools.

- **`ghost`** - Ghost - desktop automation (29 tools: screen perception + input control). Cross-platform (Windows, macOS, Linux); install the `ghost` sidecar to enable. Unavailable until installed.

## Permissions

**Grants:** `mcp:ghost`

## Engine requirement

Requires Ryu >=0.1.0.
