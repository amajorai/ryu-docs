---
title: "Gateway Firewall"
description: "Turn on the Gateway's prompt-injection and PII screen"
tags: ["Security"]
---

## What it does

An on/off switch over the Gateway's built-in firewall, which screens model traffic for prompt injection and PII. Enabling this plugin forces `firewall.enabled` on at the next Gateway restart; the pattern set and policy mode stay owned by the Gateway config (GET/PUT /v1/config), not by this plugin.

This page is part of the [Plugins catalog](/docs/plugins), [plugin manifests](/docs/extend/develop/extensions/plugin-json-manifest), [plugin runtime](/docs/extend/develop/extensions/plugin-runtime), [unified tool catalog](/docs/core/unified-tool-catalog), [Marketplace](/docs/extend/develop/extensions/marketplace), [Gateway governance](/docs/gateway/governance) documentation path. In the same category, compare [Bitwarden Secrets Manager](/docs/plugins/bitwarden), [Double Check](/docs/plugins/double-check), [Observer Agents](/docs/plugins/observer-agents), [Proof of Work](/docs/plugins/proof).

## Surfaces

| Surface | Support |
|---|---|
| gateway | none |
| core | full |
| desktop | full |
| island | full |
| mobile | full |
| extension | full |
| web | full |
| cli | full |

## What it exposes

### Policies

- **firewall** - On/off switch over the gateway firewall. The pattern set / policy mode stay owned by GET/PUT /v1/config; enabling this plugin forces firewall.enabled on at the next gateway restart.

## UI it contributes

### Policies

- **Gateway Firewall**

## Engine requirement

Requires Ryu >=0.1.0.
