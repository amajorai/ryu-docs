---
title: "Tavily Search"
description: "Agent-tuned web search and extraction"
tags: ["Search"]
---

## What it does

Search-and-extract for agents via the Tavily API (https://tavily.com), exposed as two declarative HTTP tools. Provides the swappable `web.search` and `web.extract` layers, so selecting it in the layer picker re-points the stable `web__search` / `web__extract` tools at Tavily without changing what agents call. Requires a BYOK Tavily API key.

## Surfaces

| Surface | Support |
|---|---|
| gateway | none |
| core | full |
| desktop | full |
| island | limited |
| mobile | limited |
| extension | limited |
| web | full |
| cli | full |

## What it exposes

### Tools

### `tavily__search` - Tavily Search

Web search tuned for agents. Returns ranked results with titles, URLs, and extracted content. Requires RYU_TAVILY_API_KEY.

Backend: HTTP · Method: POST

| Parameter | Type | Required | Description |
|---|---|---|---|
| `query` | string | yes | The search query. |
| `max_results` | integer | no | Number of results to return (default 10). |
| `search_depth` | string (one of: basic, advanced) | no | How much work Tavily does per query. |

### `tavily__extract` - Tavily Extract

Extract the readable content of one or more URLs. Requires RYU_TAVILY_API_KEY.

Backend: HTTP · Method: POST

| Parameter | Type | Required | Description |
|---|---|---|---|
| `urls` | array | yes | URLs to extract content from. |

### Capabilities

- **Search** - capability `web.search`
  - Verbs:
    - `web__search` -> `tavily__search`
- **Extract** - capability `web.extract`
  - Verbs:
    - `web__extract` -> `tavily__extract`

## Permissions

**Grants:** `tool:http-egress:api.tavily.com`

## UI it contributes

### Settings tabs

- **Tavily Search** (fields: Tavily API key (secret))

## Engine requirement

Requires Ryu >=0.1.0.
