---
title: "Proof of Work"
description: "Keep going until a verifier proves it's done"
tags: ["Security"]
---

## What it does

The stricter sibling of `/goal`: an independent verifier agent has to prove with tool-gathered evidence that the goal is actually done, rather than judging the transcript alone. The loop runs until that proof lands; `/proof clear` stops it and the verifier model is configurable.

This page is part of the [Plugins catalog](/docs/plugins), [plugin manifests](/docs/extend/develop/extensions/plugin-json-manifest), [plugin runtime](/docs/extend/develop/extensions/plugin-runtime), [unified tool catalog](/docs/core/unified-tool-catalog), [Marketplace](/docs/extend/develop/extensions/marketplace), [Gateway governance](/docs/gateway/governance) documentation path. In the same category, compare [Gateway Firewall](/docs/plugins/firewall), [Observer Agents](/docs/plugins/observer-agents), [Receipts](/docs/plugins/receipts), [Security Guidance](/docs/plugins/security-guidance).

## Surfaces

| Surface | Support |
|---|---|
| gateway | none |
| core | full |
| desktop | full |
| island | limited |
| mobile | limited |
| extension | limited |
| web | full |
| cli | full |

## Permissions

**Grants:** `hook:run-agent`, `storage:kv`

## UI it contributes

### Slash commands

- **/proof** - Pursue a goal until an INDEPENDENT verifier agent proves - with tool-gathered evidence - that it is actually done. Stronger than /goal, which only judges the transcript. Use '/proof clear' to stop.

### Settings tabs

- **Proof of Work** (fields: Verifier model (model_picker))

### Turn hooks

- **proof.loop** (on: `post_assistant_turn`)

## Engine requirement

Requires Ryu >=0.1.0.

## Activation

Activates on: `*`
