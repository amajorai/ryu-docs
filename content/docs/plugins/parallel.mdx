---
title: "Parallel Search"
description: "Objective-driven web search built for agent loops"
tags: ["Search"]
---

## What it does

Parallel (https://parallel.ai) provides web search and content extraction through three declarative HTTP tools. It supplies the swappable `web.search` and `web.extract` layers, so selecting it routes the stable tools to Parallel without changing what agents call. Search works without a key through Parallel's public Search MCP endpoint. A BYOK Parallel API key unlocks the higher-limit REST API and extraction.

## Surfaces

| Surface | Support |
|---|---|
| gateway | none |
| core | full |
| desktop | full |
| island | limited |
| mobile | limited |
| extension | limited |
| web | full |
| cli | full |

## What it exposes

### Tools

### `parallel.search` - Parallel Search

Web search that takes a natural-language objective plus 2-3 keyword queries and returns ranked URLs with long, LLM-ready markdown excerpts. There is no result-count parameter: size the response with max_chars_total instead. Requires RYU_PARALLEL_API_KEY.

Backend: HTTP · Method: POST

| Parameter | Type | Required | Description |
|---|---|---|---|
| `objective` | string | no | Natural-language description of the question driving the search. Should be self-contained and atomic - one specific piece of information. |
| `search_queries` | array | yes | Concise keyword search queries, 3-6 words each. At least one is required; 2-3 give the best results. |
| `mode` | string (one of: turbo, basic, advanced) | no | Search preset. turbo is the fastest, basic is the low-latency agent-loop default used here, advanced does more retrieval and compression (Parallel's own default when omitted). |
| `max_chars_total` | integer | no | Upper bound on total characters across the excerpts of all results. |

### `parallel.free_search` - Parallel Search (no key)

Web search through Parallel's public Search MCP endpoint, which needs no API key. This rate-limited free tier is the zero-setup path when `RYU_PARALLEL_API_KEY` is unset; add a key to use the higher-limit `parallel.search` tool. The endpoint speaks JSON-RPC: the caller supplies the complete `tools/call` envelope, and the reply contains a JSON string in its result payload, so the capability adapter parses it instead of a response map.

Backend: HTTP · Method: POST

| Parameter | Type | Required | Description |
|---|---|---|---|
| `jsonrpc` | string | yes | JSON-RPC version, always "2.0". |
| `id` | integer | yes | JSON-RPC request id. |
| `method` | string | yes | JSON-RPC method, always "tools/call". |
| `params` | object | yes | `&#123;name, arguments&#125;` naming the Parallel MCP tool to call (web_search or web_fetch) and its arguments. |

### `parallel.extract` - Parallel Extract

Fetch up to 20 URLs and return their readable content as markdown. Parallel returns objective-focused excerpts by default; this tool asks for full page content instead, so a caller that wanted the whole page gets it. Requires RYU_PARALLEL_API_KEY.

Backend: HTTP · Method: POST

| Parameter | Type | Required | Description |
|---|---|---|---|
| `urls` | array | yes | URLs to extract content from. Up to 20 per request. |
| `objective` | string | no | Natural-language description of what to look for in these URLs, used to focus the excerpts. |
| `search_queries` | array | no | Keyword queries (3-6 words each) used with the objective to focus excerpts. Pass the queries from the search that surfaced these URLs. |
| `max_chars_total` | integer | no | Upper bound on total characters across all extracted results. |

### Capabilities

- **Search** - capability `web.search`
  - Verbs:
    - `web.search` -> `parallel.search`
- **Extract** - capability `web.extract`
  - Verbs:
    - `web.extract` -> `parallel.extract`

## Permissions

**Grants:** `tool:http-egress:api.parallel.ai`, `tool:http-egress:search.parallel.ai`, `tool:execute`

## UI it contributes

### Settings tabs

- **Parallel Search** (fields: Parallel API key (secret))

## Engine requirement

Requires Ryu >=0.1.0.
