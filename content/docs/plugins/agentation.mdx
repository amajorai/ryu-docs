---
title: "Agentation"
description: "Visual feedback for coding agents through the Agentation MCP server."
tags: ["Developer Tools", "UI", "annotations", "MCP"]
---

## What it does

[Agentation](https://www.agentation.com/) turns feedback on a running web page
into structured annotations containing the selected element, source location,
component context, styles, and the user's request. An agent can read and act on
that feedback without copy-paste.

## MCP server

Ryu ships **`@ryu/agentation`** as an official Core-tier plugin, but it is opt-in.
After you install and enable it, Core starts the local server with:

```bash
npx -y agentation-mcp server
```

The tools are exposed under the `agentation.` namespace. Node.js is required;
the Agentation toolbar must be connected to the local server (port `4747`) for
sessions and annotations to be available. It is not activated on a fresh install
because enabling it downloads third-party code.

## Permissions

**Grant:** `mcp:agentation`
