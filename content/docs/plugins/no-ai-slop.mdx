---
title: "No AI Slop"
description: "Every answer edited until it stops sounding like AI"
tags: ["Productivity"]
---

## What it does

Bundles the `no-ai-slop` editing skill and runs it on every finished answer: a separate reviewer with a fresh context reads the reply, names the AI-slop patterns it finds, and the agent rewrites the answer to fix them. One pass by default, configurable up to twelve, or set to 0 and trigger it by hand from the composer '+' menu. A clean answer is left alone silently.

This page is part of the [Plugins catalog](/docs/plugins), [plugin manifests](/docs/extend/develop/extensions/plugin-json-manifest), [plugin runtime](/docs/extend/develop/extensions/plugin-runtime), [unified tool catalog](/docs/core/unified-tool-catalog), [Marketplace](/docs/extend/develop/extensions/marketplace), [Gateway governance](/docs/gateway/governance) documentation path. In the same category, compare [Temporary Chats](/docs/plugins/ghost-chats), [Goal](/docs/plugins/goal), [Output Styles](/docs/plugins/output-styles), [Plan Continue](/docs/plugins/plan-continue).

## Surfaces

| Surface | Support |
|---|---|
| gateway | none |
| core | full |
| desktop | full |
| island | limited |
| mobile | limited |
| extension | limited |
| web | full |
| cli | full |

## Permissions

**Grants:** `hook:run-agent`, `preferences:read`

## UI it contributes

### Composer controls

- **No AI slop** - Check this turn's answer for AI-slop patterns, even when automatic passes are set to 0. (type: toggle)

### Settings tabs

- **No AI slop** (fields: Automatic review passes (number), What a review does (select))

### Turn hooks

- **no-ai-slop.review** (on: `post_assistant_turn`)

## Engine requirement

Requires Ryu >=0.1.0.

## Activation

Activates on: `*`
