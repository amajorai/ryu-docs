---
title: "Textarea"
description: "Multi-line text input"
---

Multi-line text input The module is part of the `@ryu/ui` package and is available on every surface that includes the package.

This reference is part of the [Ryu UI overview](/docs/ui), the [component catalog](/docs/ui/components), and the [UI getting started guide](/docs/ui/getting-started). Read the [primitive usage guide](/docs/ui/primitives) for composition and ownership rules. In the same primitives group, compare [Success Check](/docs/ui/components/success-check), [Switch](/docs/ui/components/switch), [Visually Hidden Input](/docs/ui/components/visually-hidden-input).

## Preview

<UiComponentPreview component="components/textarea" exportName="Textarea" />

The preview uses representative props and is safe to interact with. It does not call a provider, write data, or depend on a host application.

## Settings

<UiComponentPreview component="components/textarea" exportName="Textarea" mode="settings" />

Use the controls to compare supported settings without changing application data or calling a provider.

## Import

```tsx
import { Textarea } from "@ryu/ui/components/textarea";
```

The module exports `Textarea`, so you can compose the primitive with its related parts.

## Usage

```tsx
import { Textarea } from "@ryu/ui/components/textarea";

export function Example() {
  return (
    <Textarea placeholder="Write a note" />
  );
}
```

Add the props required by your workflow and compose the named exports as needed. TypeScript provides the complete prop and event contract at the import site.

## Package path

`@ryu/ui/components/textarea`

This page is generated from the package export and source module so the catalog stays aligned with the usable component surface.
