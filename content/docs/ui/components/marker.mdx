---
title: "Marker"
description: "Highlight marker"
---

Highlight marker The module is part of the `@ryu/ui` package and is available on every surface that includes the package.

This reference is part of the [Ryu UI overview](/docs/ui), the [component catalog](/docs/ui/components), and the [UI getting started guide](/docs/ui/getting-started). Read the [primitive usage guide](/docs/ui/primitives) for composition and ownership rules. In the same specialized group, compare [Lanyard](/docs/ui/components/lanyard-lanyard), [Logo](/docs/ui/components/logo), [Plan Badge](/docs/ui/components/plan-badge), [Signature](/docs/ui/components/signature).

## Preview

<UiComponentPreview component="components/marker" exportName="Marker" />

The preview uses representative props and is safe to interact with. It does not call a provider, write data, or depend on a host application.

## Variants

<UiComponentPreview component="components/marker" exportName="Marker" mode="variants" />

This section renders the named variants, sizes, and states declared by the module when they are available.

## Settings

<UiComponentPreview component="components/marker" exportName="Marker" mode="settings" />

Use the controls to compare supported settings without changing application data or calling a provider.

## Import

```tsx
import { Marker, MarkerContent, MarkerIcon } from "@ryu/ui/components/marker";
```

The module exports `Marker, MarkerContent, MarkerIcon`, so you can compose the primitive with its related parts.

## Usage

```tsx
import { Marker } from "@ryu/ui/components/marker";

export function Example() {
  return (
    <Marker />
  );
}
```

Add the props required by your workflow and compose the named exports as needed. TypeScript provides the complete prop and event contract at the import site.

## Package path

`@ryu/ui/components/marker`

This page is generated from the package export and source module so the catalog stays aligned with the usable component surface.
