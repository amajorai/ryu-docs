---
title: "Marker"
description: "Highlight marker"
---

Highlight marker The module is part of the `@ryu/ui` package and is available on every surface that includes the package.

## Import

```tsx
import { Marker, MarkerContent, MarkerIcon } from "@ryu/ui/components/marker";
```

The module exports `Marker, MarkerContent, MarkerIcon`, so you can compose the primitive with its related parts.

## Usage

```tsx
import { Marker } from "@ryu/ui/components/marker";

export function Example() {
  return (
    <Marker />
  );
}
```

Add the props required by your workflow and compose the named exports as needed. TypeScript provides the complete prop and event contract at the import site.

## Package path

`@ryu/ui/components/marker`

This page is generated from the package export and source module so the catalog stays aligned with the usable component surface.
