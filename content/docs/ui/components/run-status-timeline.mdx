---
title: "Run Status Timeline"
description: "Maps scheduled run outcomes onto a compact 24-hour status strip."
---

Maps scheduled run outcomes onto a compact 24-hour status strip. The module is part of the `@ryu/ui` package and is available on every surface that includes the package.

## Preview

<UiComponentPreview component="components/run-status-timeline" exportName="RunStatusTimeline" />

The preview uses representative props and is safe to interact with. It does not call a provider, write data, or depend on a host application.

## Settings

<UiComponentPreview component="components/run-status-timeline" exportName="RunStatusTimeline" mode="settings" />

Use the controls to compare supported settings without changing application data or calling a provider.

## Import

```tsx
import { RunStatusTimeline, RunStatusTimelineLegend } from "@ryu/ui/components/run-status-timeline";
```

The module exports `RunStatusTimeline, RunStatusTimelineLegend`, so you can compose the primitive with its related parts.

## Usage

```tsx
import { RunStatusTimeline } from "@ryu/ui/components/run-status-timeline";

export function Example() {
  return (
    <RunStatusTimeline ariaLabel="Run status" endAt={Date.now()} entries={[]} startAt={Date.now() - 86400000} />
  );
}
```

Add the props required by your workflow and compose the named exports as needed. TypeScript provides the complete prop and event contract at the import site.

## Package path

`@ryu/ui/components/run-status-timeline`

This page is generated from the package export and source module so the catalog stays aligned with the usable component surface.
