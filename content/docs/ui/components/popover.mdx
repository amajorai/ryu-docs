---
title: "Popover"
description: "Popover overlay with an optional shared full-window backdrop"
---

Popover overlay with an optional shared full-window backdrop The module is part of the `@ryu/ui` package and is available on every surface that includes the package.

## Preview

<UiComponentPreview component="components/popover" exportName="Popover" />

The preview uses representative props and is safe to interact with. It does not call a provider, write data, or depend on a host application.

## Variants

<UiComponentPreview component="components/popover" exportName="Popover" mode="variants" />

This section renders the named variants, sizes, and states declared by the module when they are available.

## Settings

<UiComponentPreview component="components/popover" exportName="Popover" mode="settings" />

Use the controls to compare supported settings without changing application data or calling a provider.

## Import

```tsx
import { Popover, PopoverAnchor, PopoverContent, PopoverDescription, PopoverHeader, PopoverTitle, PopoverTrigger } from "@ryu/ui/components/popover";
```

The module exports `Popover, PopoverAnchor, PopoverContent, PopoverDescription, PopoverHeader, PopoverTitle, PopoverTrigger`, so you can compose the primitive with its related parts.

## Usage

```tsx
import { Popover } from "@ryu/ui/components/popover";

export function Example() {
  return (
    <Popover>Additional context</Popover>
  );
}
```

Add the props required by your workflow and compose the named exports as needed. TypeScript provides the complete prop and event contract at the import site.

## Package path

`@ryu/ui/components/popover`

This page is generated from the package export and source module so the catalog stays aligned with the usable component surface.
