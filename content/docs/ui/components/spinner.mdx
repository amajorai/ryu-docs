---
title: "Spinner"
description: "Loading spinner"
---

Loading spinner The module is part of the `@ryu/ui` package and is available on every surface that includes the package.

This reference is part of the [Ryu UI overview](/docs/ui), the [component catalog](/docs/ui/components), and the [UI getting started guide](/docs/ui/getting-started). Read the [primitive usage guide](/docs/ui/primitives) for composition and ownership rules. In the same primitives group, compare [Skeleton](/docs/ui/components/skeleton), [Slider](/docs/ui/components/slider), [Status Badge](/docs/ui/components/status-badge), [Success Check](/docs/ui/components/success-check).

## Preview

<UiComponentPreview component="components/spinner" exportName="Spinner" />

The preview uses representative props and is safe to interact with. It does not call a provider, write data, or depend on a host application.

## Settings

<UiComponentPreview component="components/spinner" exportName="Spinner" mode="settings" />

Use the controls to compare supported settings without changing application data or calling a provider.

## Import

```tsx
import { Spinner } from "@ryu/ui/components/spinner";
```

The module exports `Spinner`, so you can compose the primitive with its related parts.

## Usage

```tsx
import { Spinner } from "@ryu/ui/components/spinner";

export function Example() {
  return (
    <Spinner />
  );
}
```

Add the props required by your workflow and compose the named exports as needed. TypeScript provides the complete prop and event contract at the import site.

## Package path

`@ryu/ui/components/spinner`

This page is generated from the package export and source module so the catalog stays aligned with the usable component surface.
