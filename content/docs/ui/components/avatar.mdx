---
title: "Avatar"
description: "Avatar with badge, group, and count"
---

Avatar with badge, group, and count The module is part of the `@ryu/ui` package and is available on every surface that includes the package.

This reference is part of the [Ryu UI overview](/docs/ui), the [component catalog](/docs/ui/components), and the [UI getting started guide](/docs/ui/getting-started). Read the [primitive usage guide](/docs/ui/primitives) for composition and ownership rules. In the same chat & communication group, compare [Attachment](/docs/ui/components/attachment), [Bubble](/docs/ui/components/bubble), [Emoji Picker](/docs/ui/components/emoji-picker).

## Preview

<UiComponentPreview component="components/avatar" exportName="Avatar" />

The preview uses representative props and is safe to interact with. It does not call a provider, write data, or depend on a host application.

## Variants

<UiComponentPreview component="components/avatar" exportName="Avatar" mode="variants" />

This section renders the named variants, sizes, and states declared by the module when they are available.

## Settings

<UiComponentPreview component="components/avatar" exportName="Avatar" mode="settings" />

Use the controls to compare supported settings without changing application data or calling a provider.

## Import

```tsx
import { Avatar, AvatarBadge, AvatarFallback, AvatarGroup, AvatarGroupCount, AvatarImage } from "@ryu/ui/components/avatar";
```

The module exports `Avatar, AvatarBadge, AvatarFallback, AvatarGroup, AvatarGroupCount, AvatarImage`, so you can compose the primitive with its related parts.

## Usage

```tsx
import { Avatar } from "@ryu/ui/components/avatar";

export function Example() {
  return (
    <Avatar />
  );
}
```

Add the props required by your workflow and compose the named exports as needed. TypeScript provides the complete prop and event contract at the import site.

## Package path

`@ryu/ui/components/avatar`

This page is generated from the package export and source module so the catalog stays aligned with the usable component surface.
