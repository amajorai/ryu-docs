---
title: "Input"
description: "Base text input"
---

Base text input The module is part of the `@ryu/ui` package and is available on every surface that includes the package.

This reference is part of the [Ryu UI overview](/docs/ui), the [component catalog](/docs/ui/components), and the [UI getting started guide](/docs/ui/getting-started). Read the [primitive usage guide](/docs/ui/primitives) for composition and ownership rules. In the same primitives group, compare [Connection Status](/docs/ui/components/connection-status), [Elastic Slider](/docs/ui/components/elastic-slider), [Kbd](/docs/ui/components/kbd), [Label](/docs/ui/components/label).

## Preview

<UiComponentPreview component="components/input" exportName="Input" />

The preview uses representative props and is safe to interact with. It does not call a provider, write data, or depend on a host application.

## Variants

<UiComponentPreview component="components/input" exportName="Input" mode="variants" />

This section renders the named variants, sizes, and states declared by the module when they are available.

## Settings

<UiComponentPreview component="components/input" exportName="Input" mode="settings" />

Use the controls to compare supported settings without changing application data or calling a provider.

## Import

```tsx
import { Input } from "@ryu/ui/components/input";
```

The module exports `Input`, so you can compose the primitive with its related parts.

## Usage

```tsx
import { Input } from "@ryu/ui/components/input";

export function Example() {
  return (
    <Input placeholder="Search" />
  );
}
```

Add the props required by your workflow and compose the named exports as needed. TypeScript provides the complete prop and event contract at the import site.

## Package path

`@ryu/ui/components/input`

This page is generated from the package export and source module so the catalog stays aligned with the usable component surface.
