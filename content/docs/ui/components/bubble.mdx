---
title: "Bubble"
description: "Chat bubble (default/secondary/muted/tinted/outline/ghost/destructive + reactions)"
---

Chat bubble (default/secondary/muted/tinted/outline/ghost/destructive + reactions) The module is part of the `@ryu/ui` package and is available on every surface that includes the package.

This reference is part of the [Ryu UI overview](/docs/ui), the [component catalog](/docs/ui/components), and the [UI getting started guide](/docs/ui/getting-started). Read the [primitive usage guide](/docs/ui/primitives) for composition and ownership rules. In the same chat & communication group, compare [Attachment](/docs/ui/components/attachment), [Avatar](/docs/ui/components/avatar), [Emoji Picker](/docs/ui/components/emoji-picker), [Message](/docs/ui/components/message).

## Preview

<UiComponentPreview component="components/bubble" exportName="Bubble" />

The preview uses representative props and is safe to interact with. It does not call a provider, write data, or depend on a host application.

## Variants

<UiComponentPreview component="components/bubble" exportName="Bubble" mode="variants" />

This section renders the named variants, sizes, and states declared by the module when they are available.

## Settings

<UiComponentPreview component="components/bubble" exportName="Bubble" mode="settings" />

Use the controls to compare supported settings without changing application data or calling a provider.

## Import

```tsx
import { Bubble, BubbleContent, BubbleGroup, BubbleReactions } from "@ryu/ui/components/bubble";
```

The module exports `Bubble, BubbleContent, BubbleGroup, BubbleReactions`, so you can compose the primitive with its related parts.

## Usage

```tsx
import { Bubble } from "@ryu/ui/components/bubble";

export function Example() {
  return (
    <Bubble>Message</Bubble>
  );
}
```

Add the props required by your workflow and compose the named exports as needed. TypeScript provides the complete prop and event contract at the import site.

## Package path

`@ryu/ui/components/bubble`

This page is generated from the package export and source module so the catalog stays aligned with the usable component surface.
