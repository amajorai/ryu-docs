---
title: "Elastic Slider"
description: "Rubber-band slider with hash marks and dodge labels"
---

Rubber-band slider with hash marks and dodge labels The module is part of the `@ryu/ui` package and is available on every surface that includes the package.

This reference is part of the [Ryu UI overview](/docs/ui), the [component catalog](/docs/ui/components), and the [UI getting started guide](/docs/ui/getting-started). Read the [primitive usage guide](/docs/ui/primitives) for composition and ownership rules. In the same primitives group, compare [Checkbox](/docs/ui/components/checkbox), [Connection Status](/docs/ui/components/connection-status), [Input](/docs/ui/components/input), [Kbd](/docs/ui/components/kbd).

## Preview

<UiComponentPreview component="components/elastic-slider" exportName="ElasticSlider" />

The preview uses representative props and is safe to interact with. It does not call a provider, write data, or depend on a host application.

## Settings

<UiComponentPreview component="components/elastic-slider" exportName="ElasticSlider" mode="settings" />

Use the controls to compare supported settings without changing application data or calling a provider.

## Import

```tsx
import { ElasticSlider } from "@ryu/ui/components/elastic-slider";
```

The module exports `ElasticSlider`, so you can compose the primitive with its related parts.

## Usage

```tsx
import { ElasticSlider } from "@ryu/ui/components/elastic-slider";

export function Example() {
  return (
    <ElasticSlider />
  );
}
```

Add the props required by your workflow and compose the named exports as needed. TypeScript provides the complete prop and event contract at the import site.

## Package path

`@ryu/ui/components/elastic-slider`

This page is generated from the package export and source module so the catalog stays aligned with the usable component surface.
