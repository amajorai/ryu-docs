---
title: "Icon Swap"
description: "Cross-fade transition for arbitrary React-node content"
---

Cross-fade transition for arbitrary React-node content The module is part of the `@ryu/ui` package and is available on every surface that includes the package.

## Import

```tsx
import { IconSwap } from "@ryu/ui/components/icon-swap";
```

The module exports `IconSwap`, so you can compose the primitive with its related parts.

## Usage

```tsx
import { IconSwap } from "@ryu/ui/components/icon-swap";

export function Example() {
  return (
    <IconSwap />
  );
}
```

Add the props required by your workflow and compose the named exports as needed. TypeScript provides the complete prop and event contract at the import site.

## Package path

`@ryu/ui/components/icon-swap`

This page is generated from the package export and source module so the catalog stays aligned with the usable component surface.
