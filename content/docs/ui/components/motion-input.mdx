---
title: "Input"
description: "Animated input control for focused and changing states."
---

Animated input control for focused and changing states. The module is part of the `@ryu/ui` package and is available on every surface that includes the package.

This reference is part of the [Ryu UI overview](/docs/ui), the [component catalog](/docs/ui/components), and the [UI getting started guide](/docs/ui/getting-started). Read the [primitive usage guide](/docs/ui/primitives) for composition and ownership rules. In the same animation & effects group, compare [Chromatic Text Reveal](/docs/ui/components/motion-chromatic-text-reveal), [Icon Swap](/docs/ui/components/icon-swap), [Loader](/docs/ui/components/motion-loader), [Morph Icon](/docs/ui/components/morph-icon).

## Preview

<UiComponentPreview component="components/motion/input" exportName="Input" />

The preview uses representative props and is safe to interact with. It does not call a provider, write data, or depend on a host application.

## Settings

<UiComponentPreview component="components/motion/input" exportName="Input" mode="settings" />

Use the controls to compare supported settings without changing application data or calling a provider.

## Import

```tsx
import { Input } from "@ryu/ui/components/motion/input";
```

The module exports `Input`, so you can compose the primitive with its related parts.

## Usage

```tsx
import { Input } from "@ryu/ui/components/motion/input";

export function Example() {
  return (
    <Input />
  );
}
```

Add the props required by your workflow and compose the named exports as needed. TypeScript provides the complete prop and event contract at the import site.

## Package path

`@ryu/ui/components/motion/input`

This page is generated from the package export and source module so the catalog stays aligned with the usable component surface.
