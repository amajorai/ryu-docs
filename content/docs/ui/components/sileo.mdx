---
title: "Sileo"
description: "Reusable interface component."
---

Reusable interface component. The module is part of the `@ryu/ui` package and is available on every surface that includes the package.

## Import

```tsx
import { Toaster } from "@ryu/ui/components/sileo";
```

The module exports `Toaster`, so you can compose the primitive with its related parts.

## Usage

```tsx
import { Toaster } from "@ryu/ui/components/sileo";

export function Example() {
  return (
    <Toaster />
  );
}
```

Add the props required by your workflow and compose the named exports as needed. TypeScript provides the complete prop and event contract at the import site.

## Package path

`@ryu/ui/components/sileo`

This page is generated from the package export and source module so the catalog stays aligned with the usable component surface.
