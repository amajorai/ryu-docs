---
title: "Text Shimmer"
description: "Animated shimmer treatment for text content."
---

Animated shimmer treatment for text content. The module is part of the `@ryu/ui` package and is available on every surface that includes the package.

This reference is part of the [Ryu UI overview](/docs/ui), the [component catalog](/docs/ui/components), and the [UI getting started guide](/docs/ui/getting-started). Read the [primitive usage guide](/docs/ui/primitives) for composition and ownership rules. In the same animation & effects group, compare [Stagger Reveal](/docs/ui/components/stagger-reveal), [Text Scramble](/docs/ui/components/motion-text-scramble), [Text Swap](/docs/ui/components/text-swap), [Voice Activity Beam](/docs/ui/components/voice-activity-beam).

## Preview

<UiComponentPreview component="components/motion/text-shimmer" exportName="TextShimmer" />

The preview uses representative props and is safe to interact with. It does not call a provider, write data, or depend on a host application.

## Settings

<UiComponentPreview component="components/motion/text-shimmer" exportName="TextShimmer" mode="settings" />

Use the controls to compare supported settings without changing application data or calling a provider.

## Import

```tsx
import { TextShimmer } from "@ryu/ui/components/motion/text-shimmer";
```

The module exports `TextShimmer`, so you can compose the primitive with its related parts.

## Usage

```tsx
import { TextShimmer } from "@ryu/ui/components/motion/text-shimmer";

export function Example() {
  return (
    <TextShimmer />
  );
}
```

Add the props required by your workflow and compose the named exports as needed. TypeScript provides the complete prop and event contract at the import site.

## Package path

`@ryu/ui/components/motion/text-shimmer`

This page is generated from the package export and source module so the catalog stays aligned with the usable component surface.
