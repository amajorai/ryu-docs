---
title: "Chart"
description: "Recharts container + tooltip + legend"
---

Recharts container + tooltip + legend The module is part of the `@ryu/ui` package and is available on every surface that includes the package.

This reference is part of the [Ryu UI overview](/docs/ui), the [component catalog](/docs/ui/components), and the [UI getting started guide](/docs/ui/getting-started). Read the [primitive usage guide](/docs/ui/primitives) for composition and ownership rules. In the same visualization group, compare [Contributions Graph](/docs/ui/components/contributions-graph).

## Preview

<UiComponentPreview component="components/chart" exportName="ChartContainer" />

The preview uses representative props and is safe to interact with. It does not call a provider, write data, or depend on a host application.

## Settings

<UiComponentPreview component="components/chart" exportName="ChartContainer" mode="settings" />

Use the controls to compare supported settings without changing application data or calling a provider.

## Import

```tsx
import { ChartContainer, ChartLegend, ChartLegendContent, ChartStyle, ChartTooltip, ChartTooltipContent } from "@ryu/ui/components/chart";
```

The module exports `ChartContainer, ChartLegend, ChartLegendContent, ChartStyle, ChartTooltip, ChartTooltipContent`, so you can compose the primitive with its related parts.

## Usage

```tsx
import { ChartContainer } from "@ryu/ui/components/chart";

export function Example() {
  return (
    <ChartContainer />
  );
}
```

Add the props required by your workflow and compose the named exports as needed. TypeScript provides the complete prop and event contract at the import site.

## Package path

`@ryu/ui/components/chart`

This page is generated from the package export and source module so the catalog stays aligned with the usable component surface.
