---
title: "Apple Hello Effect"
description: "Animated SVG \"hello\" handwriting"
---

Animated SVG "hello" handwriting The module is part of the `@ryu/ui` package and is available on every surface that includes the package.

## Preview

<UiComponentPreview component="components/apple-hello-effect" exportName="AppleHelloEffect" />

The preview uses representative props and is safe to interact with. It does not call a provider, write data, or depend on a host application.

## Settings

<UiComponentPreview component="components/apple-hello-effect" exportName="AppleHelloEffect" mode="settings" />

Use the controls to compare supported settings without changing application data or calling a provider.

## Import

```tsx
import { AppleHelloEffect } from "@ryu/ui/components/apple-hello-effect";
```

The module exports `AppleHelloEffect`, so you can compose the primitive with its related parts.

## Usage

```tsx
import { AppleHelloEffect } from "@ryu/ui/components/apple-hello-effect";

export function Example() {
  return (
    <AppleHelloEffect />
  );
}
```

Add the props required by your workflow and compose the named exports as needed. TypeScript provides the complete prop and event contract at the import site.

## Package path

`@ryu/ui/components/apple-hello-effect`

This page is generated from the package export and source module so the catalog stays aligned with the usable component surface.
