---
title: "Radio Group"
description: "Radio button group"
---

Radio button group The module is part of the `@ryu/ui` package and is available on every surface that includes the package.

## Preview

<UiComponentPreview component="components/radio-group" exportName="RadioGroup" />

The preview uses representative props and is safe to interact with. It does not call a provider, write data, or depend on a host application.

## Settings

<UiComponentPreview component="components/radio-group" exportName="RadioGroup" mode="settings" />

Use the controls to compare supported settings without changing application data or calling a provider.

## Import

```tsx
import { RadioGroup, RadioGroupItem } from "@ryu/ui/components/radio-group";
```

The module exports `RadioGroup, RadioGroupItem`, so you can compose the primitive with its related parts.

## Usage

```tsx
import { RadioGroup } from "@ryu/ui/components/radio-group";

export function Example() {
  return (
    <RadioGroup />
  );
}
```

Add the props required by your workflow and compose the named exports as needed. TypeScript provides the complete prop and event contract at the import site.

## Package path

`@ryu/ui/components/radio-group`

This page is generated from the package export and source module so the catalog stays aligned with the usable component surface.
