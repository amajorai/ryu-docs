---
title: "Collapsible"
description: "Collapsible content"
---

Collapsible content The module is part of the `@ryu/ui` package and is available on every surface that includes the package.

This reference is part of the [Ryu UI overview](/docs/ui), the [component catalog](/docs/ui/components), and the [UI getting started guide](/docs/ui/getting-started). Read the [primitive usage guide](/docs/ui/primitives) for composition and ownership rules. In the same layout & navigation group, compare [Breadcrumb](/docs/ui/components/breadcrumb), [Card](/docs/ui/components/card), [Empty](/docs/ui/components/empty), [Item](/docs/ui/components/item).

## Preview

<UiComponentPreview component="components/collapsible" exportName="Collapsible" />

The preview uses representative props and is safe to interact with. It does not call a provider, write data, or depend on a host application.

## Settings

<UiComponentPreview component="components/collapsible" exportName="Collapsible" mode="settings" />

Use the controls to compare supported settings without changing application data or calling a provider.

## Import

```tsx
import { Collapsible, CollapsibleContent, CollapsibleTrigger } from "@ryu/ui/components/collapsible";
```

The module exports `Collapsible, CollapsibleContent, CollapsibleTrigger`, so you can compose the primitive with its related parts.

## Usage

```tsx
import { Collapsible } from "@ryu/ui/components/collapsible";

export function Example() {
  return (
    <Collapsible>Details</Collapsible>
  );
}
```

Add the props required by your workflow and compose the named exports as needed. TypeScript provides the complete prop and event contract at the import site.

## Package path

`@ryu/ui/components/collapsible`

This page is generated from the package export and source module so the catalog stays aligned with the usable component surface.
