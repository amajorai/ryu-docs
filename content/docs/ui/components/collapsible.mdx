---
title: "Collapsible"
description: "Collapsible content"
---

Collapsible content The module is part of the `@ryu/ui` package and is available on every surface that includes the package.

## Import

```tsx
import { Collapsible, CollapsibleContent, CollapsibleTrigger } from "@ryu/ui/components/collapsible";
```

The module exports `Collapsible, CollapsibleContent, CollapsibleTrigger`, so you can compose the primitive with its related parts.

## Usage

```tsx
import { Collapsible } from "@ryu/ui/components/collapsible";

export function Example() {
  return (
    <Collapsible>Details</Collapsible>
  );
}
```

Add the props required by your workflow and compose the named exports as needed. TypeScript provides the complete prop and event contract at the import site.

## Package path

`@ryu/ui/components/collapsible`

This page is generated from the package export and source module so the catalog stays aligned with the usable component surface.
