---
title: "Table"
description: "HTML table"
---

HTML table The module is part of the `@ryu/ui` package and is available on every surface that includes the package.

This reference is part of the [Ryu UI overview](/docs/ui), the [component catalog](/docs/ui/components), and the [UI getting started guide](/docs/ui/getting-started). Read the [primitive usage guide](/docs/ui/primitives) for composition and ownership rules. In the same forms & data group, compare [Input Group](/docs/ui/components/input-group), [OTP Input](/docs/ui/components/motion-otp-input), [Toggle](/docs/ui/components/toggle), [Toggle Group](/docs/ui/components/toggle-group).

## Preview

<UiComponentPreview component="components/table" exportName="Table" />

The preview uses representative props and is safe to interact with. It does not call a provider, write data, or depend on a host application.

## Settings

<UiComponentPreview component="components/table" exportName="Table" mode="settings" />

Use the controls to compare supported settings without changing application data or calling a provider.

## Import

```tsx
import { Table, TableBody, TableCaption, TableCell, TableFooter, TableHead, TableHeader, TableRow } from "@ryu/ui/components/table";
```

The module exports `Table, TableBody, TableCaption, TableCell, TableFooter, TableHead, TableHeader, TableRow`, so you can compose the primitive with its related parts.

## Usage

```tsx
import { Table } from "@ryu/ui/components/table";

export function Example() {
  return (
    <Table />
  );
}
```

Add the props required by your workflow and compose the named exports as needed. TypeScript provides the complete prop and event contract at the import site.

## Package path

`@ryu/ui/components/table`

This page is generated from the package export and source module so the catalog stays aligned with the usable component surface.
