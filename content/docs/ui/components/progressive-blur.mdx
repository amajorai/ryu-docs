---
title: "Progressive Blur"
description: "Graduated blur effect"
---

Graduated blur effect The module is part of the `@ryu/ui` package and is available on every surface that includes the package.

## Import

```tsx
import { ProgressiveBlur } from "@ryu/ui/components/progressive-blur";
```

The module exports `ProgressiveBlur`, so you can compose the primitive with its related parts.

## Usage

```tsx
import { ProgressiveBlur } from "@ryu/ui/components/progressive-blur";

export function Example() {
  return (
    <ProgressiveBlur />
  );
}
```

Add the props required by your workflow and compose the named exports as needed. TypeScript provides the complete prop and event contract at the import site.

## Package path

`@ryu/ui/components/progressive-blur`

This page is generated from the package export and source module so the catalog stays aligned with the usable component surface.
