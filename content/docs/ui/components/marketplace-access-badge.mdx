---
title: "Marketplace Access Badge"
description: "Reusable interface component."
---

Reusable interface component. The module is part of the `@ryu/ui` package and is available on every surface that includes the package.

This reference is part of the [Ryu UI overview](/docs/ui), the [component catalog](/docs/ui/components), and the [UI getting started guide](/docs/ui/getting-started). Read the [primitive usage guide](/docs/ui/primitives) for composition and ownership rules. In the same primitives group, compare [Kbd](/docs/ui/components/kbd), [Label](/docs/ui/components/label), [Native Select](/docs/ui/components/native-select), [Profile Charts](/docs/ui/components/profile-charts).

## Preview

<UiComponentPreview component="components/marketplace-access-badge" exportName="MarketplaceAccessBadge" />

The preview uses representative props and is safe to interact with. It does not call a provider, write data, or depend on a host application.

## Settings

<UiComponentPreview component="components/marketplace-access-badge" exportName="MarketplaceAccessBadge" mode="settings" />

Use the controls to compare supported settings without changing application data or calling a provider.

## Import

```tsx
import { MarketplaceAccessBadge } from "@ryu/ui/components/marketplace-access-badge";
```

The module exports `MarketplaceAccessBadge`, so you can compose the primitive with its related parts.

## Usage

```tsx
import { MarketplaceAccessBadge } from "@ryu/ui/components/marketplace-access-badge";

export function Example() {
  return (
    <MarketplaceAccessBadge />
  );
}
```

Add the props required by your workflow and compose the named exports as needed. TypeScript provides the complete prop and event contract at the import site.

## Package path

`@ryu/ui/components/marketplace-access-badge`

This page is generated from the package export and source module so the catalog stays aligned with the usable component surface.
