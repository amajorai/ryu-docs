---
title: "Alert Dialog"
description: "Confirmation dialog with media/title/description/action slots"
---

Confirmation dialog with media/title/description/action slots The module is part of the `@ryu/ui` package and is available on every surface that includes the package.

This reference is part of the [Ryu UI overview](/docs/ui), the [component catalog](/docs/ui/components), and the [UI getting started guide](/docs/ui/getting-started). Read the [primitive usage guide](/docs/ui/primitives) for composition and ownership rules. In the same overlay & dialog group, compare [Command](/docs/ui/components/command), [Context Menu](/docs/ui/components/context-menu).

## Preview

<UiComponentPreview component="components/alert-dialog" exportName="AlertDialog" />

The preview uses representative props and is safe to interact with. It does not call a provider, write data, or depend on a host application.

## Variants

<UiComponentPreview component="components/alert-dialog" exportName="AlertDialog" mode="variants" />

This section renders the named variants, sizes, and states declared by the module when they are available.

## Settings

<UiComponentPreview component="components/alert-dialog" exportName="AlertDialog" mode="settings" />

Use the controls to compare supported settings without changing application data or calling a provider.

## Import

```tsx
import { AlertDialog, AlertDialogAction, AlertDialogCancel, AlertDialogContent, AlertDialogDescription, AlertDialogFooter, AlertDialogHeader, AlertDialogMedia, AlertDialogOverlay, AlertDialogPortal, AlertDialogTitle, AlertDialogTrigger } from "@ryu/ui/components/alert-dialog";
```

The module exports `AlertDialog, AlertDialogAction, AlertDialogCancel, AlertDialogContent, AlertDialogDescription, AlertDialogFooter, AlertDialogHeader, AlertDialogMedia, AlertDialogOverlay, AlertDialogPortal, AlertDialogTitle, AlertDialogTrigger`, so you can compose the primitive with its related parts.

## Usage

```tsx
import { AlertDialog } from "@ryu/ui/components/alert-dialog";

export function Example() {
  return (
    <AlertDialog>Confirm this change</AlertDialog>
  );
}
```

Add the props required by your workflow and compose the named exports as needed. TypeScript provides the complete prop and event contract at the import site.

## Package path

`@ryu/ui/components/alert-dialog`

This page is generated from the package export and source module so the catalog stays aligned with the usable component surface.
