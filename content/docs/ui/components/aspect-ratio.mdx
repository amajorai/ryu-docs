---
title: "Aspect Ratio"
description: "CSS ratio wrapper"
---

CSS ratio wrapper The module is part of the `@ryu/ui` package and is available on every surface that includes the package.

## Import

```tsx
import { AspectRatio } from "@ryu/ui/components/aspect-ratio";
```

The module exports `AspectRatio`, so you can compose the primitive with its related parts.

## Usage

```tsx
import { AspectRatio } from "@ryu/ui/components/aspect-ratio";

export function Example() {
  return (
    <AspectRatio />
  );
}
```

Add the props required by your workflow and compose the named exports as needed. TypeScript provides the complete prop and event contract at the import site.

## Package path

`@ryu/ui/components/aspect-ratio`

This page is generated from the package export and source module so the catalog stays aligned with the usable component surface.
