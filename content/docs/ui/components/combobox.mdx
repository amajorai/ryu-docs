---
title: "Combobox"
description: "Autocomplete with chips/multi-select and an optional shared full-window backdrop; `ComboboxValue` uses the shared measured overflow fade, hover/focus auto-scroll, and full-value tooltip for constrained selections."
---

Autocomplete with chips/multi-select and an optional shared full-window backdrop; `ComboboxValue` uses the shared measured overflow fade, hover/focus auto-scroll, and full-value tooltip for constrained selections. The module is part of the `@ryu/ui` package and is available on every surface that includes the package.

## Preview

<UiComponentPreview component="components/combobox" exportName="Combobox" />

The preview uses representative props and is safe to interact with. It does not call a provider, write data, or depend on a host application.

## Variants

<UiComponentPreview component="components/combobox" exportName="Combobox" mode="variants" />

This section renders the named variants, sizes, and states declared by the module when they are available.

## Settings

<UiComponentPreview component="components/combobox" exportName="Combobox" mode="settings" />

Use the controls to compare supported settings without changing application data or calling a provider.

## Import

```tsx
import { Combobox, ComboboxChip, ComboboxChips, ComboboxChipsInput, ComboboxCollection, ComboboxContent, ComboboxEmpty, ComboboxGroup, ComboboxInput, ComboboxItem, ComboboxLabel, ComboboxList, ComboboxSeparator, ComboboxTrigger, ComboboxValue } from "@ryu/ui/components/combobox";
```

The module exports `Combobox, ComboboxChip, ComboboxChips, ComboboxChipsInput, ComboboxCollection, ComboboxContent, ComboboxEmpty, ComboboxGroup, ComboboxInput, ComboboxItem, ComboboxLabel, ComboboxList, ComboboxSeparator, ComboboxTrigger, ComboboxValue`, so you can compose the primitive with its related parts.

## Usage

```tsx
import { Combobox } from "@ryu/ui/components/combobox";

export function Example() {
  return (
    <Combobox />
  );
}
```

Add the props required by your workflow and compose the named exports as needed. TypeScript provides the complete prop and event contract at the import site.

## Package path

`@ryu/ui/components/combobox`

This page is generated from the package export and source module so the catalog stays aligned with the usable component surface.
