---
title: "Navigation Menu"
description: "Navigation menu with an optional shared full-window backdrop"
---

Navigation menu with an optional shared full-window backdrop The module is part of the `@ryu/ui` package and is available on every surface that includes the package.

## Preview

<UiComponentPreview component="components/navigation-menu" exportName="NavigationMenu" />

The preview uses representative props and is safe to interact with. It does not call a provider, write data, or depend on a host application.

## Settings

<UiComponentPreview component="components/navigation-menu" exportName="NavigationMenu" mode="settings" />

Use the controls to compare supported settings without changing application data or calling a provider.

## Import

```tsx
import { NavigationMenu, NavigationMenuContent, NavigationMenuIndicator, NavigationMenuItem, NavigationMenuLink, NavigationMenuList, NavigationMenuPositioner, NavigationMenuTrigger } from "@ryu/ui/components/navigation-menu";
```

The module exports `NavigationMenu, NavigationMenuContent, NavigationMenuIndicator, NavigationMenuItem, NavigationMenuLink, NavigationMenuList, NavigationMenuPositioner, NavigationMenuTrigger`, so you can compose the primitive with its related parts.

## Usage

```tsx
import { NavigationMenu } from "@ryu/ui/components/navigation-menu";

export function Example() {
  return (
    <NavigationMenu />
  );
}
```

Add the props required by your workflow and compose the named exports as needed. TypeScript provides the complete prop and event contract at the import site.

## Package path

`@ryu/ui/components/navigation-menu`

This page is generated from the package export and source module so the catalog stays aligned with the usable component surface.
