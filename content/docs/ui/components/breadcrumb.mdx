---
title: "Breadcrumb"
description: "Navigation breadcrumb with separator/ellipsis"
---

Navigation breadcrumb with separator/ellipsis The module is part of the `@ryu/ui` package and is available on every surface that includes the package.

This reference is part of the [Ryu UI overview](/docs/ui), the [component catalog](/docs/ui/components), and the [UI getting started guide](/docs/ui/getting-started). Read the [primitive usage guide](/docs/ui/primitives) for composition and ownership rules. In the same layout & navigation group, compare [Accordion](/docs/ui/components/accordion), [Card](/docs/ui/components/card), [Collapsible](/docs/ui/components/collapsible).

## Preview

<UiComponentPreview component="components/breadcrumb" exportName="Breadcrumb" />

The preview uses representative props and is safe to interact with. It does not call a provider, write data, or depend on a host application.

## Settings

<UiComponentPreview component="components/breadcrumb" exportName="Breadcrumb" mode="settings" />

Use the controls to compare supported settings without changing application data or calling a provider.

## Import

```tsx
import { Breadcrumb, BreadcrumbEllipsis, BreadcrumbItem, BreadcrumbLink, BreadcrumbList, BreadcrumbPage, BreadcrumbSeparator } from "@ryu/ui/components/breadcrumb";
```

The module exports `Breadcrumb, BreadcrumbEllipsis, BreadcrumbItem, BreadcrumbLink, BreadcrumbList, BreadcrumbPage, BreadcrumbSeparator`, so you can compose the primitive with its related parts.

## Usage

```tsx
import { Breadcrumb } from "@ryu/ui/components/breadcrumb";

export function Example() {
  return (
    <Breadcrumb />
  );
}
```

Add the props required by your workflow and compose the named exports as needed. TypeScript provides the complete prop and event contract at the import site.

## Package path

`@ryu/ui/components/breadcrumb`

This page is generated from the package export and source module so the catalog stays aligned with the usable component surface.
