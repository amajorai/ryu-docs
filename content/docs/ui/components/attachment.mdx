---
title: "Attachment"
description: "File attachment card (media/content/title/description/actions)"
---

File attachment card (media/content/title/description/actions) The module is part of the `@ryu/ui` package and is available on every surface that includes the package.

This reference is part of the [Ryu UI overview](/docs/ui), the [component catalog](/docs/ui/components), and the [UI getting started guide](/docs/ui/getting-started). Read the [primitive usage guide](/docs/ui/primitives) for composition and ownership rules. In the same chat & communication group, compare [Avatar](/docs/ui/components/avatar), [Bubble](/docs/ui/components/bubble).

## Preview

<UiComponentPreview component="components/attachment" exportName="Attachment" />

The preview uses representative props and is safe to interact with. It does not call a provider, write data, or depend on a host application.

## Variants

<UiComponentPreview component="components/attachment" exportName="Attachment" mode="variants" />

This section renders the named variants, sizes, and states declared by the module when they are available.

## Settings

<UiComponentPreview component="components/attachment" exportName="Attachment" mode="settings" />

Use the controls to compare supported settings without changing application data or calling a provider.

## Import

```tsx
import { Attachment, AttachmentAction, AttachmentActions, AttachmentContent, AttachmentDescription, AttachmentGroup, AttachmentMedia, AttachmentTitle, AttachmentTrigger } from "@ryu/ui/components/attachment";
```

The module exports `Attachment, AttachmentAction, AttachmentActions, AttachmentContent, AttachmentDescription, AttachmentGroup, AttachmentMedia, AttachmentTitle, AttachmentTrigger`, so you can compose the primitive with its related parts.

## Usage

```tsx
import { Attachment } from "@ryu/ui/components/attachment";

export function Example() {
  return (
    <Attachment />
  );
}
```

Add the props required by your workflow and compose the named exports as needed. TypeScript provides the complete prop and event contract at the import site.

## Package path

`@ryu/ui/components/attachment`

This page is generated from the package export and source module so the catalog stays aligned with the usable component surface.
