---
title: "Button"
description: "Animated button primitives for state and interaction transitions."
---

Animated button primitives for state and interaction transitions. The module is part of the `@ryu/ui` package and is available on every surface that includes the package.

## Preview

<UiComponentPreview component="components/motion/button/index" exportName="Button" />

The preview uses representative props and is safe to interact with. It does not call a provider, write data, or depend on a host application.

## Settings

<UiComponentPreview component="components/motion/button/index" exportName="Button" mode="settings" />

Use the controls to compare supported settings without changing application data or calling a provider.

## Import

```tsx
import { Button, ButtonLink, MagneticButton, StatefulButton } from "@ryu/ui/components/motion/button/index";
```

The module exports `Button, ButtonLink, MagneticButton, StatefulButton`, so you can compose the primitive with its related parts.

## Usage

```tsx
import { Button } from "@ryu/ui/components/motion/button/index";

export function Example() {
  return (
    <Button />
  );
}
```

Add the props required by your workflow and compose the named exports as needed. TypeScript provides the complete prop and event contract at the import site.

## Package path

`@ryu/ui/components/motion/button/index`

This page is generated from the package export and source module so the catalog stays aligned with the usable component surface.
