---
title: "Visually Hidden Input"
description: "Hidden form input"
---

Hidden form input The module is part of the `@ryu/ui` package and is available on every surface that includes the package.

This reference is part of the [Ryu UI overview](/docs/ui), the [component catalog](/docs/ui/components), and the [UI getting started guide](/docs/ui/getting-started). Read the [primitive usage guide](/docs/ui/primitives) for composition and ownership rules. In the same primitives group, compare [Switch](/docs/ui/components/switch), [Textarea](/docs/ui/components/textarea).

## Preview

<UiComponentPreview component="components/visually-hidden-input" exportName="VisuallyHiddenInput" />

The preview uses representative props and is safe to interact with. It does not call a provider, write data, or depend on a host application.

## Settings

<UiComponentPreview component="components/visually-hidden-input" exportName="VisuallyHiddenInput" mode="settings" />

Use the controls to compare supported settings without changing application data or calling a provider.

## Import

```tsx
import { VisuallyHiddenInput } from "@ryu/ui/components/visually-hidden-input";
```

The module exports `VisuallyHiddenInput`, so you can compose the primitive with its related parts.

## Usage

```tsx
import { VisuallyHiddenInput } from "@ryu/ui/components/visually-hidden-input";

export function Example() {
  return (
    <VisuallyHiddenInput />
  );
}
```

Add the props required by your workflow and compose the named exports as needed. TypeScript provides the complete prop and event contract at the import site.

## Package path

`@ryu/ui/components/visually-hidden-input`

This page is generated from the package export and source module so the catalog stays aligned with the usable component surface.
