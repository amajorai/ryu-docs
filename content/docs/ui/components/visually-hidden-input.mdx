---
title: "Visually Hidden Input"
description: "Hidden form input"
---

Hidden form input The module is part of the `@ryu/ui` package and is available on every surface that includes the package.

## Import

```tsx
import { VisuallyHiddenInput } from "@ryu/ui/components/visually-hidden-input";
```

The module exports `VisuallyHiddenInput`, so you can compose the primitive with its related parts.

## Usage

```tsx
import { VisuallyHiddenInput } from "@ryu/ui/components/visually-hidden-input";

export function Example() {
  return (
    <VisuallyHiddenInput />
  );
}
```

Add the props required by your workflow and compose the named exports as needed. TypeScript provides the complete prop and event contract at the import site.

## Package path

`@ryu/ui/components/visually-hidden-input`

This page is generated from the package export and source module so the catalog stays aligned with the usable component surface.
