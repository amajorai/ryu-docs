---
title: "Connection Status"
description: "A compact, non-blocking status toast for network and node outages."
---

A compact, non-blocking status toast for network and node outages. The module is part of the `@ryu/ui` package and is available on every surface that includes the package.

## Preview

<UiComponentPreview component="components/connection-status" exportName="ConnectionStatusToast" />

The preview uses representative props and is safe to interact with. It does not call a provider, write data, or depend on a host application.

## Variants

<UiComponentPreview component="components/connection-status" exportName="ConnectionStatusToast" mode="variants" />

This section renders the named variants, sizes, and states declared by the module when they are available.

## Settings

<UiComponentPreview component="components/connection-status" exportName="ConnectionStatusToast" mode="settings" />

Use the controls to compare supported settings without changing application data or calling a provider.

## Import

```tsx
import { ConnectionStatusToast, ConnectionStatusToastView } from "@ryu/ui/components/connection-status";
```

The module exports `ConnectionStatusToast, ConnectionStatusToastView`, so you can compose the primitive with its related parts.

## Usage

```tsx
import { ConnectionStatusToast } from "@ryu/ui/components/connection-status";

export function Example() {
  return (
    <ConnectionStatusToast nodeName="Design node" phase="node-unreachable" />
  );
}
```

Add the props required by your workflow and compose the named exports as needed. TypeScript provides the complete prop and event contract at the import site.

## Package path

`@ryu/ui/components/connection-status`

This page is generated from the package export and source module so the catalog stays aligned with the usable component surface.
