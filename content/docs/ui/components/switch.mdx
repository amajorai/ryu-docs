---
title: "Switch"
description: "Toggle switch"
---

Toggle switch The module is part of the `@ryu/ui` package and is available on every surface that includes the package.

This reference is part of the [Ryu UI overview](/docs/ui), the [component catalog](/docs/ui/components), and the [UI getting started guide](/docs/ui/getting-started). Read the [primitive usage guide](/docs/ui/primitives) for composition and ownership rules. In the same primitives group, compare [Status Badge](/docs/ui/components/status-badge), [Success Check](/docs/ui/components/success-check), [Textarea](/docs/ui/components/textarea), [Visually Hidden Input](/docs/ui/components/visually-hidden-input).

## Preview

<UiComponentPreview component="components/switch" exportName="Switch" />

The preview uses representative props and is safe to interact with. It does not call a provider, write data, or depend on a host application.

## Variants

<UiComponentPreview component="components/switch" exportName="Switch" mode="variants" />

This section renders the named variants, sizes, and states declared by the module when they are available.

## Settings

<UiComponentPreview component="components/switch" exportName="Switch" mode="settings" />

Use the controls to compare supported settings without changing application data or calling a provider.

## Import

```tsx
import { Switch } from "@ryu/ui/components/switch";
```

The module exports `Switch`, so you can compose the primitive with its related parts.

## Usage

```tsx
import { Switch } from "@ryu/ui/components/switch";

export function Example() {
  return (
    <Switch aria-label="Enable notifications" />
  );
}
```

Add the props required by your workflow and compose the named exports as needed. TypeScript provides the complete prop and event contract at the import site.

## Package path

`@ryu/ui/components/switch`

This page is generated from the package export and source module so the catalog stays aligned with the usable component surface.
