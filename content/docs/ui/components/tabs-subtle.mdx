---
title: "Tabs Subtle"
description: "Subtle tab variant"
---

Subtle tab variant The module is part of the `@ryu/ui` package and is available on every surface that includes the package.

## Import

```tsx
import { TabsSubtle, TabsSubtleItem, TabsSubtlePanel } from "@ryu/ui/components/tabs-subtle";
```

The module exports `TabsSubtle, TabsSubtleItem, TabsSubtlePanel`, so you can compose the primitive with its related parts.

## Usage

```tsx
import { TabsSubtle } from "@ryu/ui/components/tabs-subtle";

export function Example() {
  return (
    <TabsSubtle />
  );
}
```

Add the props required by your workflow and compose the named exports as needed. TypeScript provides the complete prop and event contract at the import site.

## Package path

`@ryu/ui/components/tabs-subtle`

This page is generated from the package export and source module so the catalog stays aligned with the usable component surface.
