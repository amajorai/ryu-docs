---
title: "Calendar"
description: "react-day-picker calendar with range/single"
---

react-day-picker calendar with range/single The module is part of the `@ryu/ui` package and is available on every surface that includes the package.

This reference is part of the [Ryu UI overview](/docs/ui), the [component catalog](/docs/ui/components), and the [UI getting started guide](/docs/ui/getting-started). Read the [primitive usage guide](/docs/ui/primitives) for composition and ownership rules. In the same forms & data group, compare [Button Group](/docs/ui/components/button-group), [Color Picker](/docs/ui/components/color-picker), [Combobox](/docs/ui/components/combobox).

## Preview

<UiComponentPreview component="components/calendar" exportName="Calendar" />

The preview uses representative props and is safe to interact with. It does not call a provider, write data, or depend on a host application.

## Settings

<UiComponentPreview component="components/calendar" exportName="Calendar" mode="settings" />

Use the controls to compare supported settings without changing application data or calling a provider.

## Import

```tsx
import { Calendar, CalendarDayButton } from "@ryu/ui/components/calendar";
```

The module exports `Calendar, CalendarDayButton`, so you can compose the primitive with its related parts.

## Usage

```tsx
import { Calendar } from "@ryu/ui/components/calendar";

export function Example() {
  return (
    <Calendar />
  );
}
```

Add the props required by your workflow and compose the named exports as needed. TypeScript provides the complete prop and event contract at the import site.

## Package path

`@ryu/ui/components/calendar`

This page is generated from the package export and source module so the catalog stays aligned with the usable component surface.
