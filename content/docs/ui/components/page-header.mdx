---
title: "Page Header"
description: "Page header"
---

Page header The module is part of the `@ryu/ui` package and is available on every surface that includes the package.

This reference is part of the [Ryu UI overview](/docs/ui), the [component catalog](/docs/ui/components), and the [UI getting started guide](/docs/ui/getting-started). Read the [primitive usage guide](/docs/ui/primitives) for composition and ownership rules. In the same layout & navigation group, compare [Motion Navigation Menu](/docs/ui/components/motion-navigation-menu), [Navigation Menu](/docs/ui/components/navigation-menu), [Pagination](/docs/ui/components/pagination), [Resizable](/docs/ui/components/resizable).

## Preview

<UiComponentPreview component="components/page-header" exportName="PageHeader" />

The preview uses representative props and is safe to interact with. It does not call a provider, write data, or depend on a host application.

## Settings

<UiComponentPreview component="components/page-header" exportName="PageHeader" mode="settings" />

Use the controls to compare supported settings without changing application data or calling a provider.

## Import

```tsx
import { PageHeader } from "@ryu/ui/components/page-header";
```

The module exports `PageHeader`, so you can compose the primitive with its related parts.

## Usage

```tsx
import { PageHeader } from "@ryu/ui/components/page-header";

export function Example() {
  return (
    <PageHeader />
  );
}
```

Add the props required by your workflow and compose the named exports as needed. TypeScript provides the complete prop and event contract at the import site.

## Package path

`@ryu/ui/components/page-header`

This page is generated from the package export and source module so the catalog stays aligned with the usable component surface.
