---
title: "Color Picker"
description: "Fluid color picker with saturation, hue, alpha, HEX/RGB/HSL/OKLCH formats, eyedropper, and recent swatches"
---

Fluid color picker with saturation, hue, alpha, HEX/RGB/HSL/OKLCH formats, eyedropper, and recent swatches The module is part of the `@ryu/ui` package and is available on every surface that includes the package.

This reference is part of the [Ryu UI overview](/docs/ui), the [component catalog](/docs/ui/components), and the [UI getting started guide](/docs/ui/getting-started). Read the [primitive usage guide](/docs/ui/primitives) for composition and ownership rules. In the same forms & data group, compare [Button Group](/docs/ui/components/button-group), [Calendar](/docs/ui/components/calendar), [Combobox](/docs/ui/components/combobox), [Field](/docs/ui/components/field).

## Preview

<UiComponentPreview component="components/color-picker" exportName="ColorPicker" />

The preview uses representative props and is safe to interact with. It does not call a provider, write data, or depend on a host application.

## Recent colors

The complete panel follows the Fluid Functionalism interaction model with saturation/brightness, hue, alpha, and format controls for HEX, RGB, HSL, and OKLCH values. The built-in swatch strip shows recent selections above any supplied `swatches` presets.

Recent colors are shared by Ryu picker instances and retained in local browser storage. Set `showRecentColors={false}` when a surface should show only its supplied presets. Use `ColorPickerPanel` to compose the complete panel while preserving a custom trigger, or use `ColorPickerPopover` for the compact swatch trigger and panel together.

## Settings

<UiComponentPreview component="components/color-picker" exportName="ColorPicker" mode="settings" />

Use the controls to compare supported settings without changing application data or calling a provider.

## Import

```tsx
import { ColorPicker, ColorPickerAlphaSlider, ColorPickerArea, ColorPickerContent, ColorPickerEyeDropper, ColorPickerFormatSelect, ColorPickerHueSlider, ColorPickerInput, ColorPickerPanel, ColorPickerPopover, ColorPickerSwatch, ColorPickerSwatches, ColorPickerTrigger } from "@ryu/ui/components/color-picker";
```

The module exports `ColorPicker, ColorPickerAlphaSlider, ColorPickerArea, ColorPickerContent, ColorPickerEyeDropper, ColorPickerFormatSelect, ColorPickerHueSlider, ColorPickerInput, ColorPickerPanel, ColorPickerPopover, ColorPickerSwatch, ColorPickerSwatches, ColorPickerTrigger`, so you can compose the primitive with its related parts.

## Usage

```tsx
import { ColorPickerPopover } from "@ryu/ui/components/color-picker";

export function Example() {
  return (
    <ColorPickerPopover defaultValue="#0099ff" swatches={["#000000", "#ffffff", "#ff3b30"]} />
  );
}
```

Add the props required by your workflow and compose the named exports as needed. TypeScript provides the complete prop and event contract at the import site.

## Package path

`@ryu/ui/components/color-picker`

This page is generated from the package export and source module so the catalog stays aligned with the usable component surface.
