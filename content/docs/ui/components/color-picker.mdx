---
title: "Color Picker"
description: "Full color picker (1764 lines): area, hue/alpha sliders, swatch, eye dropper, hex/rgb/hsl/hsb inputs"
---

Full color picker (1764 lines): area, hue/alpha sliders, swatch, eye dropper, hex/rgb/hsl/hsb inputs The module is part of the `@ryu/ui` package and is available on every surface that includes the package.

## Preview

<UiComponentPreview component="components/color-picker" exportName="ColorPicker" />

The preview uses representative props and is safe to interact with. It does not call a provider, write data, or depend on a host application.

## Variants

<UiComponentPreview component="components/color-picker" exportName="ColorPicker" mode="variants" />

This section renders the named variants, sizes, and states declared by the module when they are available.

## Settings

<UiComponentPreview component="components/color-picker" exportName="ColorPicker" mode="settings" />

Use the controls to compare supported settings without changing application data or calling a provider.

## Import

```tsx
import { ColorPicker, ColorPickerAlphaSlider, ColorPickerArea, ColorPickerContent, ColorPickerEyeDropper, ColorPickerFormatSelect, ColorPickerHueSlider, ColorPickerInput, ColorPickerSwatch, ColorPickerTrigger } from "@ryu/ui/components/color-picker";
```

The module exports `ColorPicker, ColorPickerAlphaSlider, ColorPickerArea, ColorPickerContent, ColorPickerEyeDropper, ColorPickerFormatSelect, ColorPickerHueSlider, ColorPickerInput, ColorPickerSwatch, ColorPickerTrigger`, so you can compose the primitive with its related parts.

## Usage

```tsx
import { ColorPicker } from "@ryu/ui/components/color-picker";

export function Example() {
  return (
    <ColorPicker />
  );
}
```

Add the props required by your workflow and compose the named exports as needed. TypeScript provides the complete prop and event contract at the import site.

## Package path

`@ryu/ui/components/color-picker`

This page is generated from the package export and source module so the catalog stays aligned with the usable component surface.
