---
title: "Chromatic Text Reveal"
description: "Color-sweep reveal on the final word of a heading"
---

Color-sweep reveal on the final word of a heading The module is part of the `@ryu/ui` package and is available on every surface that includes the package.

## Preview

<UiComponentPreview component="components/motion/chromatic-text-reveal" exportName="ChromaticTextReveal" />

The preview uses representative props and is safe to interact with. It does not call a provider, write data, or depend on a host application.

## Variants

<UiComponentPreview component="components/motion/chromatic-text-reveal" exportName="ChromaticTextReveal" mode="variants" />

This section renders the named variants, sizes, and states declared by the module when they are available.

## Settings

<UiComponentPreview component="components/motion/chromatic-text-reveal" exportName="ChromaticTextReveal" mode="settings" />

Use the controls to compare supported settings without changing application data or calling a provider.

## Import

```tsx
import { ChromaticTextReveal } from "@ryu/ui/components/motion/chromatic-text-reveal";
```

The module exports `ChromaticTextReveal`, so you can compose the primitive with its related parts.

## Usage

```tsx
import { ChromaticTextReveal } from "@ryu/ui/components/motion/chromatic-text-reveal";

export function Example() {
  return (
    <ChromaticTextReveal />
  );
}
```

Add the props required by your workflow and compose the named exports as needed. TypeScript provides the complete prop and event contract at the import site.

## Package path

`@ryu/ui/components/motion/chromatic-text-reveal`

This page is generated from the package export and source module so the catalog stays aligned with the usable component surface.
