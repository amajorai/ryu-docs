---
title: "Tabs"
description: "Responsive tab navigation with persisted order/visibility, searchable overflow menu, drag reorder, and context-menu hiding"
---

Responsive tab navigation with persisted order/visibility, searchable overflow menu, drag reorder, and context-menu hiding The module is part of the `@ryu/ui` package and is available on every surface that includes the package.

This reference is part of the [Ryu UI overview](/docs/ui), the [component catalog](/docs/ui/components), and the [UI getting started guide](/docs/ui/getting-started). Read the [primitive usage guide](/docs/ui/primitives) for composition and ownership rules. In the same layout & navigation group, compare [Resizable](/docs/ui/components/resizable), [Sidebar](/docs/ui/components/sidebar), [Tabs Subtle](/docs/ui/components/tabs-subtle).

## Preview

<UiComponentPreview component="components/tabs" exportName="Tabs" />

The preview uses representative props and is safe to interact with. It does not call a provider, write data, or depend on a host application.

## Variants

<UiComponentPreview component="components/tabs" exportName="Tabs" mode="variants" />

This section renders the named variants, sizes, and states declared by the module when they are available.

## Settings

<UiComponentPreview component="components/tabs" exportName="Tabs" mode="settings" />

Use the controls to compare supported settings without changing application data or calling a provider.

## Import

```tsx
import { Tabs, TabsContent, TabsIndicator, TabsList, TabsTrigger } from "@ryu/ui/components/tabs";
```

The module exports `Tabs, TabsContent, TabsIndicator, TabsList, TabsTrigger`, so you can compose the primitive with its related parts.

## Usage

```tsx
import { Tabs } from "@ryu/ui/components/tabs";

export function Example() {
  return (
    <Tabs />
  );
}
```

Add the props required by your workflow and compose the named exports as needed. TypeScript provides the complete prop and event contract at the import site.

## Package path

`@ryu/ui/components/tabs`

This page is generated from the package export and source module so the catalog stays aligned with the usable component surface.
