---
title: "Text Morph"
description: "Character-level text morphing backed by Torph; respects reduced motion"
---

Character-level text morphing backed by Torph; respects reduced motion The module is part of the `@ryu/ui` package and is available on every surface that includes the package.

This reference is part of the [Ryu UI overview](/docs/ui), the [component catalog](/docs/ui/components), and the [UI getting started guide](/docs/ui/getting-started). Read the [primitive usage guide](/docs/ui/primitives) for composition and ownership rules. In the same animation & effects group, compare [Rolling Number](/docs/ui/components/rolling-number), [Stagger Reveal](/docs/ui/components/stagger-reveal), [Text Scramble](/docs/ui/components/motion-text-scramble), [Text Shimmer](/docs/ui/components/motion-text-shimmer).

## Preview

<UiComponentPreview component="components/text-morph" exportName="TextMorph" />

The preview uses representative props and is safe to interact with. It does not call a provider, write data, or depend on a host application.

## Text morphing

This Ryu primitive is backed by [Torph](https://github.com/lochie/torph), a dependency-free text morphing library. It accepts text-only children and keeps the current value available to assistive technology while visual segments move between values.

The shared default is a 240 ms ease-out transition with `respectReducedMotion` enabled. Use `disabled` for a host-controlled static state, `numbers={false}` for ordinary character matching, or `as` to choose the semantic element when a heading or other text element is appropriate.

## Settings

<UiComponentPreview component="components/text-morph" exportName="TextMorph" mode="settings" />

Use the controls to compare supported settings without changing application data or calling a provider.

## Import

```tsx
import { TextMorph } from "@ryu/ui/components/text-morph";
```

The module exports `TextMorph`, so you can compose the primitive with its related parts.

## Usage

```tsx
import { TextMorph } from "@ryu/ui/components/text-morph";

export function Example() {
  return (
    <TextMorph duration={240}>Ryu UI</TextMorph>
  );
}
```

Add the props required by your workflow and compose the named exports as needed. TypeScript provides the complete prop and event contract at the import site.

## Package path

`@ryu/ui/components/text-morph`

This page is generated from the package export and source module so the catalog stays aligned with the usable component surface.
