---
title: "Radio"
description: "Animated radio control with reduced-motion support."
---

Animated radio control with reduced-motion support. The module is part of the `@ryu/ui` package and is available on every surface that includes the package.

## Import

```tsx
import { RadioGroup, RadioGroupItem } from "@ryu/ui/components/motion/radio";
```

The module exports `RadioGroup, RadioGroupItem`, so you can compose the primitive with its related parts.

## Usage

```tsx
import { RadioGroup } from "@ryu/ui/components/motion/radio";

export function Example() {
  return (
    <RadioGroup />
  );
}
```

Add the props required by your workflow and compose the named exports as needed. TypeScript provides the complete prop and event contract at the import site.

## Package path

`@ryu/ui/components/motion/radio`

This page is generated from the package export and source module so the catalog stays aligned with the usable component surface.
