---
title: "Checkbox"
description: "Base UI checkbox with Hugeicons tick"
---

Base UI checkbox with Hugeicons tick The module is part of the `@ryu/ui` package and is available on every surface that includes the package.

This reference is part of the [Ryu UI overview](/docs/ui), the [component catalog](/docs/ui/components), and the [UI getting started guide](/docs/ui/getting-started). Read the [primitive usage guide](/docs/ui/primitives) for composition and ownership rules. In the same primitives group, compare [Bouncy Accordion](/docs/ui/components/bouncy-accordion), [Button](/docs/ui/components/button), [Connection Status](/docs/ui/components/connection-status), [Elastic Slider](/docs/ui/components/elastic-slider).

## Preview

<UiComponentPreview component="components/checkbox" exportName="Checkbox" />

The preview uses representative props and is safe to interact with. It does not call a provider, write data, or depend on a host application.

## Settings

<UiComponentPreview component="components/checkbox" exportName="Checkbox" mode="settings" />

Use the controls to compare supported settings without changing application data or calling a provider.

## Import

```tsx
import { Checkbox } from "@ryu/ui/components/checkbox";
```

The module exports `Checkbox`, so you can compose the primitive with its related parts.

## Usage

```tsx
import { Checkbox } from "@ryu/ui/components/checkbox";

export function Example() {
  return (
    <Checkbox aria-label="Enable notifications" />
  );
}
```

Add the props required by your workflow and compose the named exports as needed. TypeScript provides the complete prop and event contract at the import site.

## Package path

`@ryu/ui/components/checkbox`

This page is generated from the package export and source module so the catalog stays aligned with the usable component surface.
