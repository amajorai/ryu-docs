---
title: "Field"
description: "Form field wrapper"
---

Form field wrapper The module is part of the `@ryu/ui` package and is available on every surface that includes the package.

This reference is part of the [Ryu UI overview](/docs/ui), the [component catalog](/docs/ui/components), and the [UI getting started guide](/docs/ui/getting-started). Read the [primitive usage guide](/docs/ui/primitives) for composition and ownership rules. In the same forms & data group, compare [Color Picker](/docs/ui/components/color-picker), [Combobox](/docs/ui/components/combobox), [Input Group](/docs/ui/components/input-group), [OTP Input](/docs/ui/components/motion-otp-input).

## Preview

<UiComponentPreview component="components/field" exportName="Field" />

The preview uses representative props and is safe to interact with. It does not call a provider, write data, or depend on a host application.

## Variants

<UiComponentPreview component="components/field" exportName="Field" mode="variants" />

This section renders the named variants, sizes, and states declared by the module when they are available.

## Settings

<UiComponentPreview component="components/field" exportName="Field" mode="settings" />

Use the controls to compare supported settings without changing application data or calling a provider.

## Import

```tsx
import { Field, FieldContent, FieldDescription, FieldError, FieldGroup, FieldLabel, FieldLegend, FieldSeparator, FieldSet, FieldTitle } from "@ryu/ui/components/field";
```

The module exports `Field, FieldContent, FieldDescription, FieldError, FieldGroup, FieldLabel, FieldLegend, FieldSeparator, FieldSet, FieldTitle`, so you can compose the primitive with its related parts.

## Usage

```tsx
import { Field } from "@ryu/ui/components/field";

export function Example() {
  return (
    <Field />
  );
}
```

Add the props required by your workflow and compose the named exports as needed. TypeScript provides the complete prop and event contract at the import site.

## Package path

`@ryu/ui/components/field`

This page is generated from the package export and source module so the catalog stays aligned with the usable component surface.
