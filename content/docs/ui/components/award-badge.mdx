---
title: "Award Badge"
description: "Reusable interface component."
---

Reusable interface component. The module is part of the `@ryu/ui` package and is available on every surface that includes the package.

This reference is part of the [Ryu UI overview](/docs/ui), the [component catalog](/docs/ui/components), and the [UI getting started guide](/docs/ui/getting-started). Read the [primitive usage guide](/docs/ui/primitives) for composition and ownership rules. In the same primitives group, compare [Alert](/docs/ui/components/alert), [Aspect Ratio](/docs/ui/components/aspect-ratio), [Badge](/docs/ui/components/badge), [Bouncy Accordion](/docs/ui/components/bouncy-accordion).

## Preview

<UiComponentPreview component="components/award-badge" exportName="AwardBadge" />

The preview uses representative props and is safe to interact with. It does not call a provider, write data, or depend on a host application.

## Settings

<UiComponentPreview component="components/award-badge" exportName="AwardBadge" mode="settings" />

Use the controls to compare supported settings without changing application data or calling a provider.

## Import

```tsx
import { AwardBadge } from "@ryu/ui/components/award-badge";
```

The module exports `AwardBadge`, so you can compose the primitive with its related parts.

## Usage

```tsx
import { AwardBadge } from "@ryu/ui/components/award-badge";

export function Example() {
  return (
    <AwardBadge />
  );
}
```

Add the props required by your workflow and compose the named exports as needed. TypeScript provides the complete prop and event contract at the import site.

## Package path

`@ryu/ui/components/award-badge`

This page is generated from the package export and source module so the catalog stays aligned with the usable component surface.
