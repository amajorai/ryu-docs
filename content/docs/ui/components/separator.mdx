---
title: "Separator"
description: "Horizontal/vertical separator"
---

Horizontal/vertical separator The module is part of the `@ryu/ui` package and is available on every surface that includes the package.

## Import

```tsx
import { Separator } from "@ryu/ui/components/separator";
```

The module exports `Separator`, so you can compose the primitive with its related parts.

## Usage

```tsx
import { Separator } from "@ryu/ui/components/separator";

export function Example() {
  return (
    <Separator />
  );
}
```

Add the props required by your workflow and compose the named exports as needed. TypeScript provides the complete prop and event contract at the import site.

## Package path

`@ryu/ui/components/separator`

This page is generated from the package export and source module so the catalog stays aligned with the usable component surface.
