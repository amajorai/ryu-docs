---
title: "Status Badge"
description: "Reusable interface component."
---

Reusable interface component. The module is part of the `@ryu/ui` package and is available on every surface that includes the package.

This reference is part of the [Ryu UI overview](/docs/ui), the [component catalog](/docs/ui/components), and the [UI getting started guide](/docs/ui/getting-started). Read the [primitive usage guide](/docs/ui/primitives) for composition and ownership rules. In the same primitives group, compare [Slider](/docs/ui/components/slider), [Spinner](/docs/ui/components/spinner), [Success Check](/docs/ui/components/success-check), [Switch](/docs/ui/components/switch).

## Preview

<UiComponentPreview component="components/status-badge" exportName="StatusBadge" />

The preview uses representative props and is safe to interact with. It does not call a provider, write data, or depend on a host application.

## Settings

<UiComponentPreview component="components/status-badge" exportName="StatusBadge" mode="settings" />

Use the controls to compare supported settings without changing application data or calling a provider.

## Import

```tsx
import { StatusBadge } from "@ryu/ui/components/status-badge";
```

The module exports `StatusBadge`, so you can compose the primitive with its related parts.

## Usage

```tsx
import { StatusBadge } from "@ryu/ui/components/status-badge";

export function Example() {
  return (
    <StatusBadge />
  );
}
```

Add the props required by your workflow and compose the named exports as needed. TypeScript provides the complete prop and event contract at the import site.

## Package path

`@ryu/ui/components/status-badge`

This page is generated from the package export and source module so the catalog stays aligned with the usable component surface.
