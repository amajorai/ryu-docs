---
title: "Button Group"
description: "Grouped buttons with separator"
---

Grouped buttons with separator The module is part of the `@ryu/ui` package and is available on every surface that includes the package.

## Preview

<UiComponentPreview component="components/button-group" exportName="ButtonGroup" />

The preview uses representative props and is safe to interact with. It does not call a provider, write data, or depend on a host application.

## Settings

<UiComponentPreview component="components/button-group" exportName="ButtonGroup" mode="settings" />

Use the controls to compare supported settings without changing application data or calling a provider.

## Import

```tsx
import { ButtonGroup, ButtonGroupSeparator, ButtonGroupText } from "@ryu/ui/components/button-group";
```

The module exports `ButtonGroup, ButtonGroupSeparator, ButtonGroupText`, so you can compose the primitive with its related parts.

## Usage

```tsx
import { ButtonGroup } from "@ryu/ui/components/button-group";

export function Example() {
  return (
    <ButtonGroup />
  );
}
```

Add the props required by your workflow and compose the named exports as needed. TypeScript provides the complete prop and event contract at the import site.

## Package path

`@ryu/ui/components/button-group`

This page is generated from the package export and source module so the catalog stays aligned with the usable component surface.
