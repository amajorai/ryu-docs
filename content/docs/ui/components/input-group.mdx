---
title: "Input Group"
description: "Input with addons/buttons"
---

Input with addons/buttons The module is part of the `@ryu/ui` package and is available on every surface that includes the package.

## Import

```tsx
import { InputGroup, InputGroupAddon, InputGroupButton, InputGroupInput, InputGroupText, InputGroupTextarea } from "@ryu/ui/components/input-group";
```

The module exports `InputGroup, InputGroupAddon, InputGroupButton, InputGroupInput, InputGroupText, InputGroupTextarea`, so you can compose the primitive with its related parts.

## Usage

```tsx
import { InputGroup } from "@ryu/ui/components/input-group";

export function Example() {
  return (
    <InputGroup />
  );
}
```

Add the props required by your workflow and compose the named exports as needed. TypeScript provides the complete prop and event contract at the import site.

## Package path

`@ryu/ui/components/input-group`

This page is generated from the package export and source module so the catalog stays aligned with the usable component surface.
