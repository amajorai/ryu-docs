---
title: "Message Scroller"
description: "Auto-scrolling message list"
---

Auto-scrolling message list The module is part of the `@ryu/ui` package and is available on every surface that includes the package.

## Import

```tsx
import { MessageScroller, MessageScrollerButton, MessageScrollerContent, MessageScrollerItem, MessageScrollerProvider, MessageScrollerViewport } from "@ryu/ui/components/message-scroller";
```

The module exports `MessageScroller, MessageScrollerButton, MessageScrollerContent, MessageScrollerItem, MessageScrollerProvider, MessageScrollerViewport`, so you can compose the primitive with its related parts.

## Usage

```tsx
import { MessageScroller } from "@ryu/ui/components/message-scroller";

export function Example() {
  return (
    <MessageScroller />
  );
}
```

Add the props required by your workflow and compose the named exports as needed. TypeScript provides the complete prop and event contract at the import site.

## Package path

`@ryu/ui/components/message-scroller`

This page is generated from the package export and source module so the catalog stays aligned with the usable component surface.
