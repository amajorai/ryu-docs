---
title: "Stagger Reveal"
description: "Staggered children reveal"
---

Staggered children reveal The module is part of the `@ryu/ui` package and is available on every surface that includes the package.

## Import

```tsx
import { StaggerReveal } from "@ryu/ui/components/stagger-reveal";
```

The module exports `StaggerReveal`, so you can compose the primitive with its related parts.

## Usage

```tsx
import { StaggerReveal } from "@ryu/ui/components/stagger-reveal";

export function Example() {
  return (
    <StaggerReveal />
  );
}
```

Add the props required by your workflow and compose the named exports as needed. TypeScript provides the complete prop and event contract at the import site.

## Package path

`@ryu/ui/components/stagger-reveal`

This page is generated from the package export and source module so the catalog stays aligned with the usable component surface.
