---
title: "Lanyard"
description: "3D React Three Fiber lanyard"
---

3D React Three Fiber lanyard The module is part of the `@ryu/ui` package and is available on every surface that includes the package.

## Import

```tsx
import { Lanyard } from "@ryu/ui/components/lanyard/Lanyard";
```

The module exports `Lanyard`, so you can compose the primitive with its related parts.

## Usage

```tsx
import { Lanyard } from "@ryu/ui/components/lanyard/Lanyard";

export function Example() {
  return (
    <Lanyard />
  );
}
```

Add the props required by your workflow and compose the named exports as needed. TypeScript provides the complete prop and event contract at the import site.

## Package path

`@ryu/ui/components/lanyard/Lanyard`

This page is generated from the package export and source module so the catalog stays aligned with the usable component surface.
