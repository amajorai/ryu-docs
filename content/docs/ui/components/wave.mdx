---
title: "Wave"
description: "Wave animation"
---

Wave animation The module is part of the `@ryu/ui` package and is available on every surface that includes the package.

This reference is part of the [Ryu UI overview](/docs/ui), the [component catalog](/docs/ui/components), and the [UI getting started guide](/docs/ui/getting-started). Read the [primitive usage guide](/docs/ui/primitives) for composition and ownership rules. In the same animation & effects group, compare [Text Swap](/docs/ui/components/text-swap), [Voice Activity Beam](/docs/ui/components/voice-activity-beam).

## Preview

<UiComponentPreview component="components/wave" exportName="Wave" />

The preview uses representative props and is safe to interact with. It does not call a provider, write data, or depend on a host application.

## Settings

<UiComponentPreview component="components/wave" exportName="Wave" mode="settings" />

Use the controls to compare supported settings without changing application data or calling a provider.

## Import

```tsx
import { Wave } from "@ryu/ui/components/wave";
```

The module exports `Wave`, so you can compose the primitive with its related parts.

## Usage

```tsx
import { Wave } from "@ryu/ui/components/wave";

export function Example() {
  return (
    <Wave />
  );
}
```

Add the props required by your workflow and compose the named exports as needed. TypeScript provides the complete prop and event contract at the import site.

## Package path

`@ryu/ui/components/wave`

This page is generated from the package export and source module so the catalog stays aligned with the usable component surface.
