---
title: "Wave"
description: "Wave animation"
---

Wave animation The module is part of the `@ryu/ui` package and is available on every surface that includes the package.

## Import

```tsx
import { Wave } from "@ryu/ui/components/wave";
```

The module exports `Wave`, so you can compose the primitive with its related parts.

## Usage

```tsx
import { Wave } from "@ryu/ui/components/wave";

export function Example() {
  return (
    <Wave />
  );
}
```

Add the props required by your workflow and compose the named exports as needed. TypeScript provides the complete prop and event contract at the import site.

## Package path

`@ryu/ui/components/wave`

This page is generated from the package export and source module so the catalog stays aligned with the usable component surface.
