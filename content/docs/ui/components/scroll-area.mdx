---
title: "Scroll Area"
description: "Custom scrollbar"
---

Custom scrollbar The module is part of the `@ryu/ui` package and is available on every surface that includes the package.

## Preview

<UiComponentPreview component="components/scroll-area" exportName="ScrollArea" />

The preview uses representative props and is safe to interact with. It does not call a provider, write data, or depend on a host application.

## Settings

<UiComponentPreview component="components/scroll-area" exportName="ScrollArea" mode="settings" />

Use the controls to compare supported settings without changing application data or calling a provider.

## Import

```tsx
import { ScrollArea, ScrollBar } from "@ryu/ui/components/scroll-area";
```

The module exports `ScrollArea, ScrollBar`, so you can compose the primitive with its related parts.

## Usage

```tsx
import { ScrollArea } from "@ryu/ui/components/scroll-area";

export function Example() {
  return (
    <ScrollArea />
  );
}
```

Add the props required by your workflow and compose the named exports as needed. TypeScript provides the complete prop and event contract at the import site.

## Package path

`@ryu/ui/components/scroll-area`

This page is generated from the package export and source module so the catalog stays aligned with the usable component surface.
