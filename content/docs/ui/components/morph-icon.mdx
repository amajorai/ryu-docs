---
title: "Morph Icon"
description: "Spring-powered morphing for compatible stroke-icon data; `MorphIconSwap` handles two-state transitions and honors reduced motion"
---

Spring-powered morphing for compatible stroke-icon data; `MorphIconSwap` handles two-state transitions and honors reduced motion The module is part of the `@ryu/ui` package and is available on every surface that includes the package.

This reference is part of the [Ryu UI overview](/docs/ui), the [component catalog](/docs/ui/components), and the [UI getting started guide](/docs/ui/getting-started). Read the [primitive usage guide](/docs/ui/primitives) for composition and ownership rules. In the same animation & effects group, compare [Input](/docs/ui/components/motion-input), [Loader](/docs/ui/components/motion-loader), [Motion Highlight](/docs/ui/components/motion-highlight), [Number Pop-in](/docs/ui/components/number-pop-in).

## Preview

<UiComponentPreview component="components/morph-icon" exportName="MorphIcon" />

The preview uses representative props and is safe to interact with. It does not call a provider, write data, or depend on a host application.

## Settings

<UiComponentPreview component="components/morph-icon" exportName="MorphIcon" mode="settings" />

Use the controls to compare supported settings without changing application data or calling a provider.

## Import

```tsx
import { MorphIcon, MorphIconSwap } from "@ryu/ui/components/morph-icon";
```

The module exports `MorphIcon, MorphIconSwap`, so you can compose the primitive with its related parts.

## Usage

```tsx
import { MorphIcon } from "@ryu/ui/components/morph-icon";

export function Example() {
  return (
    <MorphIcon />
  );
}
```

Add the props required by your workflow and compose the named exports as needed. TypeScript provides the complete prop and event contract at the import site.

## Package path

`@ryu/ui/components/morph-icon`

This page is generated from the package export and source module so the catalog stays aligned with the usable component surface.
