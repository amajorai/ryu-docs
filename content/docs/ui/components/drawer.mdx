---
title: "Drawer"
description: "Vaul drawer (bottom/top/left/right)"
---

Vaul drawer (bottom/top/left/right) The module is part of the `@ryu/ui` package and is available on every surface that includes the package.

## Import

```tsx
import { Drawer, DrawerClose, DrawerContent, DrawerDescription, DrawerFooter, DrawerHeader, DrawerOverlay, DrawerPortal, DrawerTitle, DrawerTrigger } from "@ryu/ui/components/drawer";
```

The module exports `Drawer, DrawerClose, DrawerContent, DrawerDescription, DrawerFooter, DrawerHeader, DrawerOverlay, DrawerPortal, DrawerTitle, DrawerTrigger`, so you can compose the primitive with its related parts.

## Usage

```tsx
import { Drawer } from "@ryu/ui/components/drawer";

export function Example() {
  return (
    <Drawer />
  );
}
```

Add the props required by your workflow and compose the named exports as needed. TypeScript provides the complete prop and event contract at the import site.

## Package path

`@ryu/ui/components/drawer`

This page is generated from the package export and source module so the catalog stays aligned with the usable component surface.
