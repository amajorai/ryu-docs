---
title: "Drawer"
description: "Vaul drawer (bottom/top/left/right)"
---

Vaul drawer (bottom/top/left/right) The module is part of the `@ryu/ui` package and is available on every surface that includes the package.

This reference is part of the [Ryu UI overview](/docs/ui), the [component catalog](/docs/ui/components), and the [UI getting started guide](/docs/ui/getting-started). Read the [primitive usage guide](/docs/ui/primitives) for composition and ownership rules. In the same overlay & dialog group, compare [Context Menu](/docs/ui/components/context-menu), [Dialog](/docs/ui/components/dialog), [Dropdown Menu](/docs/ui/components/dropdown-menu), [Hover Card](/docs/ui/components/hover-card).

## Preview

<UiComponentPreview component="components/drawer" exportName="Drawer" />

The preview uses representative props and is safe to interact with. It does not call a provider, write data, or depend on a host application.

## Settings

<UiComponentPreview component="components/drawer" exportName="Drawer" mode="settings" />

Use the controls to compare supported settings without changing application data or calling a provider.

## Import

```tsx
import { Drawer, DrawerClose, DrawerContent, DrawerDescription, DrawerFooter, DrawerHeader, DrawerOverlay, DrawerPortal, DrawerTitle, DrawerTrigger } from "@ryu/ui/components/drawer";
```

The module exports `Drawer, DrawerClose, DrawerContent, DrawerDescription, DrawerFooter, DrawerHeader, DrawerOverlay, DrawerPortal, DrawerTitle, DrawerTrigger`, so you can compose the primitive with its related parts.

## Usage

```tsx
import { Drawer } from "@ryu/ui/components/drawer";

export function Example() {
  return (
    <Drawer />
  );
}
```

Add the props required by your workflow and compose the named exports as needed. TypeScript provides the complete prop and event contract at the import site.

## Package path

`@ryu/ui/components/drawer`

This page is generated from the package export and source module so the catalog stays aligned with the usable component surface.
