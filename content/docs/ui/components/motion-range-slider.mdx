---
title: "Range Slider"
description: "Reusable interface component."
---

Reusable interface component. The module is part of the `@ryu/ui` package and is available on every surface that includes the package.

This reference is part of the [Ryu UI overview](/docs/ui), the [component catalog](/docs/ui/components), and the [UI getting started guide](/docs/ui/getting-started). Read the [primitive usage guide](/docs/ui/primitives) for composition and ownership rules. In the same animation & effects group, compare [Progressive Blur](/docs/ui/components/progressive-blur), [Radio](/docs/ui/components/motion-radio), [Range Slider Fluid](/docs/ui/components/motion-range-slider-fluid), [Range Slider Wave](/docs/ui/components/motion-range-slider-wave).

## Preview

<UiComponentPreview component="components/motion/range-slider" exportName="RangeSlider" />

The preview uses representative props and is safe to interact with. It does not call a provider, write data, or depend on a host application.

## Settings

<UiComponentPreview component="components/motion/range-slider" exportName="RangeSlider" mode="settings" />

Use the controls to compare supported settings without changing application data or calling a provider.

## Import

```tsx
import { RangeSlider } from "@ryu/ui/components/motion/range-slider";
```

The module exports `RangeSlider`, so you can compose the primitive with its related parts.

## Usage

```tsx
import { RangeSlider } from "@ryu/ui/components/motion/range-slider";

export function Example() {
  return (
    <RangeSlider />
  );
}
```

Add the props required by your workflow and compose the named exports as needed. TypeScript provides the complete prop and event contract at the import site.

## Package path

`@ryu/ui/components/motion/range-slider`

This page is generated from the package export and source module so the catalog stays aligned with the usable component surface.
