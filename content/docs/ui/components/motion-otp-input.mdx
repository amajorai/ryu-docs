---
title: "OTP Input"
description: "One-time-code field with per-slot animation, error shake and success check"
---

One-time-code field with per-slot animation, error shake and success check The module is part of the `@ryu/ui` package and is available on every surface that includes the package.

## Import

```tsx
import { OTPInput } from "@ryu/ui/components/motion/otp-input";
```

The module exports `OTPInput`, so you can compose the primitive with its related parts.

## Usage

```tsx
import { OTPInput } from "@ryu/ui/components/motion/otp-input";

export function Example() {
  return (
    <OTPInput />
  );
}
```

Add the props required by your workflow and compose the named exports as needed. TypeScript provides the complete prop and event contract at the import site.

## Package path

`@ryu/ui/components/motion/otp-input`

This page is generated from the package export and source module so the catalog stays aligned with the usable component surface.
