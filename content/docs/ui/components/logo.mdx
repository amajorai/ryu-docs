---
title: "Logo"
description: "Ryu ghost logo with animated, filled, outline, eyes-only, shimmer, and muted 1px no-fill `outline-muted`; `expression` and `animation` support random selection plus 16 expressions and 14 animation states"
---

Ryu ghost logo with animated, filled, outline, eyes-only, shimmer, and muted 1px no-fill `outline-muted`; `expression` and `animation` support random selection plus 16 expressions and 14 animation states The module is part of the `@ryu/ui` package and is available on every surface that includes the package.

This reference is part of the [Ryu UI overview](/docs/ui), the [component catalog](/docs/ui/components), and the [UI getting started guide](/docs/ui/getting-started). Read the [primitive usage guide](/docs/ui/primitives) for composition and ownership rules. In the same specialized group, compare [Gradient](/docs/ui/components/dither-kit-gradient), [Lanyard](/docs/ui/components/lanyard-lanyard), [Marker](/docs/ui/components/marker), [Plan Badge](/docs/ui/components/plan-badge).

## Preview

<UiComponentPreview component="components/logo" exportName="Logo" />

The preview uses representative props and is safe to interact with. It does not call a provider, write data, or depend on a host application.

## Variants

<UiComponentPreview component="components/logo" exportName="Logo" mode="variants" />

This section renders the named variants, sizes, and states declared by the module when they are available.

## Settings

<UiComponentPreview component="components/logo" exportName="Logo" mode="settings" />

Use the controls to compare supported settings without changing application data or calling a provider.

## Import

```tsx
import { Logo } from "@ryu/ui/components/logo";
```

The module exports `Logo`, so you can compose the primitive with its related parts.

## Usage

```tsx
import { Logo } from "@ryu/ui/components/logo";

export function Example() {
  return (
    <Logo animation="random" expression="random" variant="outline-muted" />
  );
}
```

Add the props required by your workflow and compose the named exports as needed. TypeScript provides the complete prop and event contract at the import site.

## Package path

`@ryu/ui/components/logo`

This page is generated from the package export and source module so the catalog stays aligned with the usable component surface.
