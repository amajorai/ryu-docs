---
title: "Employee Badge"
description: "Employee badge"
---

Employee badge The module is part of the `@ryu/ui` package and is available on every surface that includes the package.

## Import

```tsx
import { EmployeeBadge } from "@ryu/ui/components/employee-badge";
```

The module exports `EmployeeBadge`, so you can compose the primitive with its related parts.

## Usage

```tsx
import { EmployeeBadge } from "@ryu/ui/components/employee-badge";

export function Example() {
  return (
    <EmployeeBadge />
  );
}
```

Add the props required by your workflow and compose the named exports as needed. TypeScript provides the complete prop and event contract at the import site.

## Package path

`@ryu/ui/components/employee-badge`

This page is generated from the package export and source module so the catalog stays aligned with the usable component surface.
