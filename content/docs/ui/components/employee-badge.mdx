---
title: "Employee Badge"
description: "Employee badge"
---

Employee badge The module is part of the `@ryu/ui` package and is available on every surface that includes the package.

This reference is part of the [Ryu UI overview](/docs/ui), the [component catalog](/docs/ui/components), and the [UI getting started guide](/docs/ui/getting-started). Read the [primitive usage guide](/docs/ui/primitives) for composition and ownership rules. In the same specialized group, compare [Button Download](/docs/ui/components/button-download), [Carousel](/docs/ui/components/carousel), [Gradient](/docs/ui/components/dither-kit-gradient), [Lanyard](/docs/ui/components/lanyard-lanyard).

## Preview

<UiComponentPreview component="components/employee-badge" exportName="EmployeeBadge" />

The preview uses representative props and is safe to interact with. It does not call a provider, write data, or depend on a host application.

## Settings

<UiComponentPreview component="components/employee-badge" exportName="EmployeeBadge" mode="settings" />

Use the controls to compare supported settings without changing application data or calling a provider.

## Import

```tsx
import { EmployeeBadge } from "@ryu/ui/components/employee-badge";
```

The module exports `EmployeeBadge`, so you can compose the primitive with its related parts.

## Usage

```tsx
import { EmployeeBadge } from "@ryu/ui/components/employee-badge";

export function Example() {
  return (
    <EmployeeBadge />
  );
}
```

Add the props required by your workflow and compose the named exports as needed. TypeScript provides the complete prop and event contract at the import site.

## Package path

`@ryu/ui/components/employee-badge`

This page is generated from the package export and source module so the catalog stays aligned with the usable component surface.
