---
title: "Emoji Picker"
description: "Searchable full-list EmojiMart picker"
---

Searchable full-list EmojiMart picker The module is part of the `@ryu/ui` package and is available on every surface that includes the package.

This reference is part of the [Ryu UI overview](/docs/ui), the [component catalog](/docs/ui/components), and the [UI getting started guide](/docs/ui/getting-started). Read the [primitive usage guide](/docs/ui/primitives) for composition and ownership rules. In the same chat & communication group, compare [Avatar](/docs/ui/components/avatar), [Bubble](/docs/ui/components/bubble), [Message](/docs/ui/components/message), [Message Scroller](/docs/ui/components/message-scroller).

## Preview

<UiComponentPreview component="components/emoji-picker" exportName="EmojiPicker" />

The preview uses representative props and is safe to interact with. It does not call a provider, write data, or depend on a host application.

## Settings

<UiComponentPreview component="components/emoji-picker" exportName="EmojiPicker" mode="settings" />

Use the controls to compare supported settings without changing application data or calling a provider.

## Import

```tsx
import { EmojiPicker } from "@ryu/ui/components/emoji-picker";
```

The module exports `EmojiPicker`, so you can compose the primitive with its related parts.

## Usage

```tsx
import { EmojiPicker } from "@ryu/ui/components/emoji-picker";

export function Example() {
  return (
    <EmojiPicker />
  );
}
```

Add the props required by your workflow and compose the named exports as needed. TypeScript provides the complete prop and event contract at the import site.

## Package path

`@ryu/ui/components/emoji-picker`

This page is generated from the package export and source module so the catalog stays aligned with the usable component surface.
