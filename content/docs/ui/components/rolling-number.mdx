---
title: "Rolling Number"
description: "Rolling digit animation"
---

Rolling digit animation The module is part of the `@ryu/ui` package and is available on every surface that includes the package.

## Import

```tsx
import { RollingNumber } from "@ryu/ui/components/rolling-number";
```

The module exports `RollingNumber`, so you can compose the primitive with its related parts.

## Usage

```tsx
import { RollingNumber } from "@ryu/ui/components/rolling-number";

export function Example() {
  return (
    <RollingNumber />
  );
}
```

Add the props required by your workflow and compose the named exports as needed. TypeScript provides the complete prop and event contract at the import site.

## Package path

`@ryu/ui/components/rolling-number`

This page is generated from the package export and source module so the catalog stays aligned with the usable component surface.
