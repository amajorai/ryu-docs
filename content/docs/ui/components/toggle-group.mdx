---
title: "Toggle Group"
description: "Toggle button group; item text uses the same shared fade, hover/focus auto-scroll, and full-value tooltip."
---

Toggle button group; item text uses the same shared fade, hover/focus auto-scroll, and full-value tooltip. The module is part of the `@ryu/ui` package and is available on every surface that includes the package.

## Import

```tsx
import { ToggleGroup, ToggleGroupItem } from "@ryu/ui/components/toggle-group";
```

The module exports `ToggleGroup, ToggleGroupItem`, so you can compose the primitive with its related parts.

## Usage

```tsx
import { ToggleGroup } from "@ryu/ui/components/toggle-group";

export function Example() {
  return (
    <ToggleGroup />
  );
}
```

Add the props required by your workflow and compose the named exports as needed. TypeScript provides the complete prop and event contract at the import site.

## Package path

`@ryu/ui/components/toggle-group`

This page is generated from the package export and source module so the catalog stays aligned with the usable component surface.
