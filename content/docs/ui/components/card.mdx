---
title: "Card"
description: "Card with header/title/description/action/content/footer"
---

Card with header/title/description/action/content/footer The module is part of the `@ryu/ui` package and is available on every surface that includes the package.

## Import

```tsx
import { Card, CardAction, CardContent, CardDescription, CardFooter, CardHeader, CardTitle } from "@ryu/ui/components/card";
```

The module exports `Card, CardAction, CardContent, CardDescription, CardFooter, CardHeader, CardTitle`, so you can compose the primitive with its related parts.

## Usage

```tsx
import { Card } from "@ryu/ui/components/card";

export function Example() {
  return (
    <Card>Content</Card>
  );
}
```

Add the props required by your workflow and compose the named exports as needed. TypeScript provides the complete prop and event contract at the import site.

## Package path

`@ryu/ui/components/card`

This page is generated from the package export and source module so the catalog stays aligned with the usable component surface.
