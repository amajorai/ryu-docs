---
title: "Motion Highlight"
description: "Motion-powered highlight"
---

Motion-powered highlight The module is part of the `@ryu/ui` package and is available on every surface that includes the package.

## Import

```tsx
import { Highlight, HighlightItem } from "@ryu/ui/components/motion-highlight";
```

The module exports `Highlight, HighlightItem`, so you can compose the primitive with its related parts.

## Usage

```tsx
import { Highlight } from "@ryu/ui/components/motion-highlight";

export function Example() {
  return (
    <Highlight />
  );
}
```

Add the props required by your workflow and compose the named exports as needed. TypeScript provides the complete prop and event contract at the import site.

## Package path

`@ryu/ui/components/motion-highlight`

This page is generated from the package export and source module so the catalog stays aligned with the usable component surface.
