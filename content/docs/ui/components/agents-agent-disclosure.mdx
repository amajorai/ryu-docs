---
title: "Agent Disclosure"
description: "Reusable interface component."
---

Reusable interface component. The module is part of the `@ryu/ui` package and is available on every surface that includes the package.

This reference is part of the [Ryu UI overview](/docs/ui), the [component catalog](/docs/ui/components), and the [UI getting started guide](/docs/ui/getting-started). Read the [primitive usage guide](/docs/ui/primitives) for composition and ownership rules. In the same agent ui group, compare [Agent Activity](/docs/ui/components/agents-agent-activity), [Agent Code](/docs/ui/components/agents-agent-code), [Approval Card](/docs/ui/components/agents-approval-card), [Citations](/docs/ui/components/agents-citations).

## Preview

<UiComponentPreview component="components/agents/agent-disclosure" exportName="AgentDisclosure" />

The preview uses representative props and is safe to interact with. It does not call a provider, write data, or depend on a host application.

## Settings

<UiComponentPreview component="components/agents/agent-disclosure" exportName="AgentDisclosure" mode="settings" />

Use the controls to compare supported settings without changing application data or calling a provider.

## Import

```tsx
import { AgentDisclosure } from "@ryu/ui/components/agents/agent-disclosure";
```

The module exports `AgentDisclosure`, so you can compose the primitive with its related parts.

## Usage

```tsx
import { AgentDisclosure } from "@ryu/ui/components/agents/agent-disclosure";

export function Example() {
  return (
    <AgentDisclosure />
  );
}
```

Add the props required by your workflow and compose the named exports as needed. TypeScript provides the complete prop and event contract at the import site.

## Package path

`@ryu/ui/components/agents/agent-disclosure`

This page is generated from the package export and source module so the catalog stays aligned with the usable component surface.
