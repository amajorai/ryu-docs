---
title: "Preview Rail"
description: "Animated preview rail for browsing adjacent content."
---

Animated preview rail for browsing adjacent content. The module is part of the `@ryu/ui` package and is available on every surface that includes the package.

## Import

```tsx
import { PreviewRail } from "@ryu/ui/components/motion/preview-rail";
```

The module exports `PreviewRail`, so you can compose the primitive with its related parts.

## Usage

```tsx
import { PreviewRail } from "@ryu/ui/components/motion/preview-rail";

export function Example() {
  return (
    <PreviewRail />
  );
}
```

Add the props required by your workflow and compose the named exports as needed. TypeScript provides the complete prop and event contract at the import site.

## Package path

`@ryu/ui/components/motion/preview-rail`

This page is generated from the package export and source module so the catalog stays aligned with the usable component surface.
