---
title: "Loading States"
description: "Loading and progress indicators for agent work."
---

Loading and progress indicators for agent work. The module is part of the `@ryu/ui` package and is available on every surface that includes the package.

## Import

```tsx
import { AgentProgress, ReasoningText, ThinkingShimmer } from "@ryu/ui/components/agents/loading-states";
```

The module exports `AgentProgress, ReasoningText, ThinkingShimmer`, so you can compose the primitive with its related parts.

## Usage

```tsx
import { AgentProgress } from "@ryu/ui/components/agents/loading-states";

export function Example() {
  return (
    <AgentProgress />
  );
}
```

Add the props required by your workflow and compose the named exports as needed. TypeScript provides the complete prop and event contract at the import site.

## Package path

`@ryu/ui/components/agents/loading-states`

This page is generated from the package export and source module so the catalog stays aligned with the usable component surface.
