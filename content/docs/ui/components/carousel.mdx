---
title: "Carousel"
description: "Embla carousel with prev/next"
---

Embla carousel with prev/next The module is part of the `@ryu/ui` package and is available on every surface that includes the package.

This reference is part of the [Ryu UI overview](/docs/ui), the [component catalog](/docs/ui/components), and the [UI getting started guide](/docs/ui/getting-started). Read the [primitive usage guide](/docs/ui/primitives) for composition and ownership rules. In the same specialized group, compare [Avatar](/docs/ui/components/dither-kit-avatar), [Button Download](/docs/ui/components/button-download), [Employee Badge](/docs/ui/components/employee-badge), [Gradient](/docs/ui/components/dither-kit-gradient).

## Preview

<UiComponentPreview component="components/carousel" exportName="Carousel" />

The preview uses representative props and is safe to interact with. It does not call a provider, write data, or depend on a host application.

## Settings

<UiComponentPreview component="components/carousel" exportName="Carousel" mode="settings" />

Use the controls to compare supported settings without changing application data or calling a provider.

## Import

```tsx
import { Carousel, CarouselApi, CarouselContent, CarouselItem, CarouselNext, CarouselPrevious } from "@ryu/ui/components/carousel";
```

The module exports `Carousel, CarouselApi, CarouselContent, CarouselItem, CarouselNext, CarouselPrevious`, so you can compose the primitive with its related parts.

## Usage

```tsx
import { Carousel } from "@ryu/ui/components/carousel";

export function Example() {
  return (
    <Carousel />
  );
}
```

Add the props required by your workflow and compose the named exports as needed. TypeScript provides the complete prop and event contract at the import site.

## Package path

`@ryu/ui/components/carousel`

This page is generated from the package export and source module so the catalog stays aligned with the usable component surface.
