---
title: "Carousel"
description: "Embla carousel with prev/next"
---

Embla carousel with prev/next The module is part of the `@ryu/ui` package and is available on every surface that includes the package.

## Import

```tsx
import { Carousel, CarouselApi, CarouselContent, CarouselItem, CarouselNext, CarouselPrevious } from "@ryu/ui/components/carousel";
```

The module exports `Carousel, CarouselApi, CarouselContent, CarouselItem, CarouselNext, CarouselPrevious`, so you can compose the primitive with its related parts.

## Usage

```tsx
import { Carousel } from "@ryu/ui/components/carousel";

export function Example() {
  return (
    <Carousel />
  );
}
```

Add the props required by your workflow and compose the named exports as needed. TypeScript provides the complete prop and event contract at the import site.

## Package path

`@ryu/ui/components/carousel`

This page is generated from the package export and source module so the catalog stays aligned with the usable component surface.
