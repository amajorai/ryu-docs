---
title: "Thinking Shimmer"
description: "Reusable interface component."
---

Reusable interface component. The module is part of the `@ryu/ui` package and is available on every surface that includes the package.

This reference is part of the [Ryu UI overview](/docs/ui), the [component catalog](/docs/ui/components), and the [UI getting started guide](/docs/ui/getting-started). Read the [primitive usage guide](/docs/ui/primitives) for composition and ownership rules. In the same agent ui group, compare [Loading States](/docs/ui/components/agents-loading-states), [Message Scroller](/docs/ui/components/agents-message-scroller), [Todo List](/docs/ui/components/agents-todo-list), [Tool Approval](/docs/ui/components/agents-tool-approval).

## Preview

<UiComponentPreview component="components/agents/loading-states/thinking-shimmer" exportName="ThinkingShimmer" />

The preview uses representative props and is safe to interact with. It does not call a provider, write data, or depend on a host application.

## Settings

<UiComponentPreview component="components/agents/loading-states/thinking-shimmer" exportName="ThinkingShimmer" mode="settings" />

Use the controls to compare supported settings without changing application data or calling a provider.

## Import

```tsx
import { ThinkingShimmer } from "@ryu/ui/components/agents/loading-states/thinking-shimmer";
```

The module exports `ThinkingShimmer`, so you can compose the primitive with its related parts.

## Usage

```tsx
import { ThinkingShimmer } from "@ryu/ui/components/agents/loading-states/thinking-shimmer";

export function Example() {
  return (
    <ThinkingShimmer />
  );
}
```

Add the props required by your workflow and compose the named exports as needed. TypeScript provides the complete prop and event contract at the import site.

## Package path

`@ryu/ui/components/agents/loading-states/thinking-shimmer`

This page is generated from the package export and source module so the catalog stays aligned with the usable component surface.
