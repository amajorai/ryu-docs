---
title: "Gradient"
description: "Renders a dithered gradient visual from tokenized colors."
---

Renders a dithered gradient visual from tokenized colors. The module is part of the `@ryu/ui` package and is available on every surface that includes the package.

## Preview

<UiComponentPreview component="components/dither-kit/gradient" exportName="DitherGradient" />

The preview uses representative props and is safe to interact with. It does not call a provider, write data, or depend on a host application.

## Settings

<UiComponentPreview component="components/dither-kit/gradient" exportName="DitherGradient" mode="settings" />

Use the controls to compare supported settings without changing application data or calling a provider.

## Import

```tsx
import { DitherGradient } from "@ryu/ui/components/dither-kit/gradient";
```

The module exports `DitherGradient`, so you can compose the primitive with its related parts.

## Usage

```tsx
import { DitherGradient } from "@ryu/ui/components/dither-kit/gradient";

export function Example() {
  return (
    <DitherGradient />
  );
}
```

Add the props required by your workflow and compose the named exports as needed. TypeScript provides the complete prop and event contract at the import site.

## Package path

`@ryu/ui/components/dither-kit/gradient`

This page is generated from the package export and source module so the catalog stays aligned with the usable component surface.
