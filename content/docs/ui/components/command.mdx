---
title: "Command"
description: "cmdk command palette + dialog"
---

cmdk command palette + dialog The module is part of the `@ryu/ui` package and is available on every surface that includes the package.

This reference is part of the [Ryu UI overview](/docs/ui), the [component catalog](/docs/ui/components), and the [UI getting started guide](/docs/ui/getting-started). Read the [primitive usage guide](/docs/ui/primitives) for composition and ownership rules. In the same overlay & dialog group, compare [Alert Dialog](/docs/ui/components/alert-dialog), [Context Menu](/docs/ui/components/context-menu), [Dialog](/docs/ui/components/dialog).

## Preview

<UiComponentPreview component="components/command" exportName="Command" />

The preview uses representative props and is safe to interact with. It does not call a provider, write data, or depend on a host application.

## Settings

<UiComponentPreview component="components/command" exportName="Command" mode="settings" />

Use the controls to compare supported settings without changing application data or calling a provider.

## Import

```tsx
import { Command, CommandDialog, CommandEmpty, CommandGroup, CommandInput, CommandItem, CommandList, CommandSeparator, CommandShortcut } from "@ryu/ui/components/command";
```

The module exports `Command, CommandDialog, CommandEmpty, CommandGroup, CommandInput, CommandItem, CommandList, CommandSeparator, CommandShortcut`, so you can compose the primitive with its related parts.

## Usage

```tsx
import { Command } from "@ryu/ui/components/command";

export function Example() {
  return (
    <Command />
  );
}
```

Add the props required by your workflow and compose the named exports as needed. TypeScript provides the complete prop and event contract at the import site.

## Package path

`@ryu/ui/components/command`

This page is generated from the package export and source module so the catalog stays aligned with the usable component surface.
