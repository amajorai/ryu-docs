---
title: "Native Select"
description: "Styled native `<select>`"
---

Styled native `<select>` The module is part of the `@ryu/ui` package and is available on every surface that includes the package.

This reference is part of the [Ryu UI overview](/docs/ui), the [component catalog](/docs/ui/components), and the [UI getting started guide](/docs/ui/getting-started). Read the [primitive usage guide](/docs/ui/primitives) for composition and ownership rules. In the same primitives group, compare [Label](/docs/ui/components/label), [Marketplace Access Badge](/docs/ui/components/marketplace-access-badge), [Profile Charts](/docs/ui/components/profile-charts), [Progress](/docs/ui/components/progress).

## Preview

<UiComponentPreview component="components/native-select" exportName="NativeSelect" />

The preview uses representative props and is safe to interact with. It does not call a provider, write data, or depend on a host application.

## Variants

<UiComponentPreview component="components/native-select" exportName="NativeSelect" mode="variants" />

This section renders the named variants, sizes, and states declared by the module when they are available.

## Settings

<UiComponentPreview component="components/native-select" exportName="NativeSelect" mode="settings" />

Use the controls to compare supported settings without changing application data or calling a provider.

## Import

```tsx
import { NativeSelect, NativeSelectOptGroup, NativeSelectOption } from "@ryu/ui/components/native-select";
```

The module exports `NativeSelect, NativeSelectOptGroup, NativeSelectOption`, so you can compose the primitive with its related parts.

## Usage

```tsx
import { NativeSelect } from "@ryu/ui/components/native-select";

export function Example() {
  return (
    <NativeSelect />
  );
}
```

Add the props required by your workflow and compose the named exports as needed. TypeScript provides the complete prop and event contract at the import site.

## Package path

`@ryu/ui/components/native-select`

This page is generated from the package export and source module so the catalog stays aligned with the usable component surface.
