---
title: "Profile Charts"
description: "Reusable interface component."
---

Reusable interface component. The module is part of the `@ryu/ui` package and is available on every surface that includes the package.

This reference is part of the [Ryu UI overview](/docs/ui), the [component catalog](/docs/ui/components), and the [UI getting started guide](/docs/ui/getting-started). Read the [primitive usage guide](/docs/ui/primitives) for composition and ownership rules. In the same primitives group, compare [Marketplace Access Badge](/docs/ui/components/marketplace-access-badge), [Native Select](/docs/ui/components/native-select), [Progress](/docs/ui/components/progress), [Radio Group](/docs/ui/components/radio-group).

## Preview

<UiComponentPreview component="components/profile-charts" exportName="ActivityArea" />

The preview uses representative props and is safe to interact with. It does not call a provider, write data, or depend on a host application.

## Settings

<UiComponentPreview component="components/profile-charts" exportName="ActivityArea" mode="settings" />

Use the controls to compare supported settings without changing application data or calling a provider.

## Import

```tsx
import { ActivityArea, FeatureMixBar, RankedList, TransportDonut } from "@ryu/ui/components/profile-charts";
```

The module exports `ActivityArea, FeatureMixBar, RankedList, TransportDonut`, so you can compose the primitive with its related parts.

## Usage

```tsx
import { ActivityArea } from "@ryu/ui/components/profile-charts";

export function Example() {
  return (
    <ActivityArea />
  );
}
```

Add the props required by your workflow and compose the named exports as needed. TypeScript provides the complete prop and event contract at the import site.

## Package path

`@ryu/ui/components/profile-charts`

This page is generated from the package export and source module so the catalog stays aligned with the usable component surface.
