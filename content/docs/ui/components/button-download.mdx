---
title: "Button Download"
description: "Download button with progress states"
---

Download button with progress states The module is part of the `@ryu/ui` package and is available on every surface that includes the package.

## Import

```tsx
import { DownloadButton } from "@ryu/ui/components/button-download";
```

The module exports `DownloadButton`, so you can compose the primitive with its related parts.

## Usage

```tsx
import { DownloadButton } from "@ryu/ui/components/button-download";

export function Example() {
  return (
    <DownloadButton />
  );
}
```

Add the props required by your workflow and compose the named exports as needed. TypeScript provides the complete prop and event contract at the import site.

## Package path

`@ryu/ui/components/button-download`

This page is generated from the package export and source module so the catalog stays aligned with the usable component surface.
