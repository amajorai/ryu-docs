---
title: "Progress"
description: "Progress bar"
---

Progress bar The module is part of the `@ryu/ui` package and is available on every surface that includes the package.

This reference is part of the [Ryu UI overview](/docs/ui), the [component catalog](/docs/ui/components), and the [UI getting started guide](/docs/ui/getting-started). Read the [primitive usage guide](/docs/ui/primitives) for composition and ownership rules. In the same primitives group, compare [Native Select](/docs/ui/components/native-select), [Profile Charts](/docs/ui/components/profile-charts), [Radio Group](/docs/ui/components/radio-group), [Run Status Timeline](/docs/ui/components/run-status-timeline).

## Preview

<UiComponentPreview component="components/progress" exportName="Progress" />

The preview uses representative props and is safe to interact with. It does not call a provider, write data, or depend on a host application.

## Settings

<UiComponentPreview component="components/progress" exportName="Progress" mode="settings" />

Use the controls to compare supported settings without changing application data or calling a provider.

## Import

```tsx
import { Progress, ProgressIndicator, ProgressLabel, ProgressTrack, ProgressValue } from "@ryu/ui/components/progress";
```

The module exports `Progress, ProgressIndicator, ProgressLabel, ProgressTrack, ProgressValue`, so you can compose the primitive with its related parts.

## Usage

```tsx
import { Progress } from "@ryu/ui/components/progress";

export function Example() {
  return (
    <Progress value={64} />
  );
}
```

Add the props required by your workflow and compose the named exports as needed. TypeScript provides the complete prop and event contract at the import site.

## Package path

`@ryu/ui/components/progress`

This page is generated from the package export and source module so the catalog stays aligned with the usable component surface.
