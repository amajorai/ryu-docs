---
title: "Range Slider Wave"
description: "Reusable interface component."
---

Reusable interface component. The module is part of the `@ryu/ui` package and is available on every surface that includes the package.

This reference is part of the [Ryu UI overview](/docs/ui), the [component catalog](/docs/ui/components), and the [UI getting started guide](/docs/ui/getting-started). Read the [primitive usage guide](/docs/ui/primitives) for composition and ownership rules. In the same animation & effects group, compare [Range Slider](/docs/ui/components/motion-range-slider), [Range Slider Fluid](/docs/ui/components/motion-range-slider-fluid), [Rolling Number](/docs/ui/components/rolling-number), [Stagger Reveal](/docs/ui/components/stagger-reveal).

## Preview

<UiComponentPreview component="components/motion/range-slider-wave" exportName="WaveSlider" />

The preview uses representative props and is safe to interact with. It does not call a provider, write data, or depend on a host application.

## Settings

<UiComponentPreview component="components/motion/range-slider-wave" exportName="WaveSlider" mode="settings" />

Use the controls to compare supported settings without changing application data or calling a provider.

## Import

```tsx
import { WaveSlider } from "@ryu/ui/components/motion/range-slider-wave";
```

The module exports `WaveSlider`, so you can compose the primitive with its related parts.

## Usage

```tsx
import { WaveSlider } from "@ryu/ui/components/motion/range-slider-wave";

export function Example() {
  return (
    <WaveSlider />
  );
}
```

Add the props required by your workflow and compose the named exports as needed. TypeScript provides the complete prop and event contract at the import site.

## Package path

`@ryu/ui/components/motion/range-slider-wave`

This page is generated from the package export and source module so the catalog stays aligned with the usable component surface.
