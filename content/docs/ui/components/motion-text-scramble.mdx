---
title: "Text Scramble"
description: "Animated text transition that scrambles characters before settling."
---

Animated text transition that scrambles characters before settling. The module is part of the `@ryu/ui` package and is available on every surface that includes the package.

This reference is part of the [Ryu UI overview](/docs/ui), the [component catalog](/docs/ui/components), and the [UI getting started guide](/docs/ui/getting-started). Read the [primitive usage guide](/docs/ui/primitives) for composition and ownership rules. In the same animation & effects group, compare [Rolling Number](/docs/ui/components/rolling-number), [Stagger Reveal](/docs/ui/components/stagger-reveal), [Text Shimmer](/docs/ui/components/motion-text-shimmer), [Text Swap](/docs/ui/components/text-swap).

## Preview

<UiComponentPreview component="components/motion/text-scramble" exportName="TextScramble" />

The preview uses representative props and is safe to interact with. It does not call a provider, write data, or depend on a host application.

## Settings

<UiComponentPreview component="components/motion/text-scramble" exportName="TextScramble" mode="settings" />

Use the controls to compare supported settings without changing application data or calling a provider.

## Import

```tsx
import { TextScramble } from "@ryu/ui/components/motion/text-scramble";
```

The module exports `TextScramble`, so you can compose the primitive with its related parts.

## Usage

```tsx
import { TextScramble } from "@ryu/ui/components/motion/text-scramble";

export function Example() {
  return (
    <TextScramble />
  );
}
```

Add the props required by your workflow and compose the named exports as needed. TypeScript provides the complete prop and event contract at the import site.

## Package path

`@ryu/ui/components/motion/text-scramble`

This page is generated from the package export and source module so the catalog stays aligned with the usable component surface.
