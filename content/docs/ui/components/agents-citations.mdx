---
title: "Citations"
description: "Reusable interface component."
---

Reusable interface component. The module is part of the `@ryu/ui` package and is available on every surface that includes the package.

This reference is part of the [Ryu UI overview](/docs/ui), the [component catalog](/docs/ui/components), and the [UI getting started guide](/docs/ui/getting-started). Read the [primitive usage guide](/docs/ui/primitives) for composition and ownership rules. In the same agent ui group, compare [Agent Disclosure](/docs/ui/components/agents-agent-disclosure), [Approval Card](/docs/ui/components/agents-approval-card), [Code Block](/docs/ui/components/agents-code-block), [File Diff](/docs/ui/components/agents-file-diff).

## Preview

<UiComponentPreview component="components/agents/citations" exportName="Citations" />

The preview uses representative props and is safe to interact with. It does not call a provider, write data, or depend on a host application.

## Settings

<UiComponentPreview component="components/agents/citations" exportName="Citations" mode="settings" />

Use the controls to compare supported settings without changing application data or calling a provider.

## Import

```tsx
import { Citation, CitationFavicon, CitationList, Citations, CitationStack } from "@ryu/ui/components/agents/citations";
```

The module exports `Citation, CitationFavicon, CitationList, Citations, CitationStack`, so you can compose the primitive with its related parts.

## Usage

```tsx
import { Citations } from "@ryu/ui/components/agents/citations";

export function Example() {
  return (
    <Citations />
  );
}
```

Add the props required by your workflow and compose the named exports as needed. TypeScript provides the complete prop and event contract at the import site.

## Package path

`@ryu/ui/components/agents/citations`

This page is generated from the package export and source module so the catalog stays aligned with the usable component surface.
