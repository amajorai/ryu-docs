---
title: "Agent Activity"
description: "Displays the activity timeline for an agent run."
---

Displays the activity timeline for an agent run. The module is part of the `@ryu/ui` package and is available on every surface that includes the package.

## Preview

<UiComponentPreview component="components/agents/agent-activity" exportName="AgentActivity" />

The preview uses representative props and is safe to interact with. It does not call a provider, write data, or depend on a host application.

## Variants

<UiComponentPreview component="components/agents/agent-activity" exportName="AgentActivity" mode="variants" />

This section renders the named variants, sizes, and states declared by the module when they are available.

## Settings

<UiComponentPreview component="components/agents/agent-activity" exportName="AgentActivity" mode="settings" />

Use the controls to compare supported settings without changing application data or calling a provider.

## Import

```tsx
import { AgentActivity } from "@ryu/ui/components/agents/agent-activity";
```

The module exports `AgentActivity`, so you can compose the primitive with its related parts.

## Usage

```tsx
import { AgentActivity } from "@ryu/ui/components/agents/agent-activity";

export function Example() {
  return (
    <AgentActivity />
  );
}
```

Add the props required by your workflow and compose the named exports as needed. TypeScript provides the complete prop and event contract at the import site.

## Package path

`@ryu/ui/components/agents/agent-activity`

This page is generated from the package export and source module so the catalog stays aligned with the usable component surface.
