---
title: "Voice Activity Beam"
description: "Line beam with a live microphone waveform"
---

Line beam with a live microphone waveform The module is part of the `@ryu/ui` package and is available on every surface that includes the package.

## Preview

<UiComponentPreview component="components/voice-activity-beam" exportName="VoiceActivityBeam" />

The preview uses representative props and is safe to interact with. It does not call a provider, write data, or depend on a host application.

## Settings

<UiComponentPreview component="components/voice-activity-beam" exportName="VoiceActivityBeam" mode="settings" />

Use the controls to compare supported settings without changing application data or calling a provider.

## Import

```tsx
import { VoiceActivityBeam } from "@ryu/ui/components/voice-activity-beam";
```

The module exports `VoiceActivityBeam`, so you can compose the primitive with its related parts.

## Usage

```tsx
import { VoiceActivityBeam } from "@ryu/ui/components/voice-activity-beam";

export function Example() {
  return (
    <VoiceActivityBeam />
  );
}
```

Add the props required by your workflow and compose the named exports as needed. TypeScript provides the complete prop and event contract at the import site.

## Package path

`@ryu/ui/components/voice-activity-beam`

This page is generated from the package export and source module so the catalog stays aligned with the usable component surface.
