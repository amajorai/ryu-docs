---
title: "Overflow Actions"
description: "Overflow action menu"
---

Overflow action menu The module is part of the `@ryu/ui` package and is available on every surface that includes the package.

This reference is part of the [Ryu UI overview](/docs/ui), the [component catalog](/docs/ui/components), and the [UI getting started guide](/docs/ui/getting-started). Read the [primitive usage guide](/docs/ui/primitives) for composition and ownership rules. In the same overlay & dialog group, compare [Hover Card](/docs/ui/components/hover-card), [Menubar](/docs/ui/components/menubar), [Popover](/docs/ui/components/popover), [Sheet](/docs/ui/components/sheet).

## Preview

<UiComponentPreview component="components/overflow-actions" exportName="OverflowActions" />

The preview uses representative props and is safe to interact with. It does not call a provider, write data, or depend on a host application.

## Settings

<UiComponentPreview component="components/overflow-actions" exportName="OverflowActions" mode="settings" />

Use the controls to compare supported settings without changing application data or calling a provider.

## Import

```tsx
import { OverflowActions } from "@ryu/ui/components/overflow-actions";
```

The module exports `OverflowActions`, so you can compose the primitive with its related parts.

## Usage

```tsx
import { OverflowActions } from "@ryu/ui/components/overflow-actions";

export function Example() {
  return (
    <OverflowActions />
  );
}
```

Add the props required by your workflow and compose the named exports as needed. TypeScript provides the complete prop and event contract at the import site.

## Package path

`@ryu/ui/components/overflow-actions`

This page is generated from the package export and source module so the catalog stays aligned with the usable component surface.
