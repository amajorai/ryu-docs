---
title: "Toggle"
description: "Toggle button with measured fading, hover/focus auto-scroll, and a full-value tooltip for constrained direct text children."
---

Toggle button with measured fading, hover/focus auto-scroll, and a full-value tooltip for constrained direct text children. The module is part of the `@ryu/ui` package and is available on every surface that includes the package.

## Import

```tsx
import { Toggle } from "@ryu/ui/components/toggle";
```

The module exports `Toggle`, so you can compose the primitive with its related parts.

## Usage

```tsx
import { Toggle } from "@ryu/ui/components/toggle";

export function Example() {
  return (
    <Toggle />
  );
}
```

Add the props required by your workflow and compose the named exports as needed. TypeScript provides the complete prop and event contract at the import site.

## Package path

`@ryu/ui/components/toggle`

This page is generated from the package export and source module so the catalog stays aligned with the usable component surface.
