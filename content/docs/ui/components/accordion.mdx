---
title: "Accordion"
description: "Collapsible sections (Root/Item/Trigger/Content); direct trigger text uses the shared measured overflow fade, hover/focus auto-scroll, and full-value tooltip."
---

Collapsible sections (Root/Item/Trigger/Content); direct trigger text uses the shared measured overflow fade, hover/focus auto-scroll, and full-value tooltip. The module is part of the `@ryu/ui` package and is available on every surface that includes the package.

## Import

```tsx
import { Accordion, AccordionContent, AccordionItem, AccordionTrigger } from "@ryu/ui/components/accordion";
```

The module exports `Accordion, AccordionContent, AccordionItem, AccordionTrigger`, so you can compose the primitive with its related parts.

## Usage

```tsx
import { Accordion } from "@ryu/ui/components/accordion";

export function Example() {
  return (
    <Accordion>Sections</Accordion>
  );
}
```

Add the props required by your workflow and compose the named exports as needed. TypeScript provides the complete prop and event contract at the import site.

## Package path

`@ryu/ui/components/accordion`

This page is generated from the package export and source module so the catalog stays aligned with the usable component surface.
