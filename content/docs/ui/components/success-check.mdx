---
title: "Success Check"
description: "Reusable interface component."
---

Reusable interface component. The module is part of the `@ryu/ui` package and is available on every surface that includes the package.

This reference is part of the [Ryu UI overview](/docs/ui), the [component catalog](/docs/ui/components), and the [UI getting started guide](/docs/ui/getting-started). Read the [primitive usage guide](/docs/ui/primitives) for composition and ownership rules. In the same primitives group, compare [Spinner](/docs/ui/components/spinner), [Status Badge](/docs/ui/components/status-badge), [Switch](/docs/ui/components/switch), [Textarea](/docs/ui/components/textarea).

## Preview

<UiComponentPreview component="components/success-check" exportName="SuccessCheck" />

The preview uses representative props and is safe to interact with. It does not call a provider, write data, or depend on a host application.

## Settings

<UiComponentPreview component="components/success-check" exportName="SuccessCheck" mode="settings" />

Use the controls to compare supported settings without changing application data or calling a provider.

## Import

```tsx
import { SuccessCheck } from "@ryu/ui/components/success-check";
```

The module exports `SuccessCheck`, so you can compose the primitive with its related parts.

## Usage

```tsx
import { SuccessCheck } from "@ryu/ui/components/success-check";

export function Example() {
  return (
    <SuccessCheck />
  );
}
```

Add the props required by your workflow and compose the named exports as needed. TypeScript provides the complete prop and event contract at the import site.

## Package path

`@ryu/ui/components/success-check`

This page is generated from the package export and source module so the catalog stays aligned with the usable component surface.
