---
title: "Motion Navigation Menu"
description: "Animated navigation menu"
---

Animated navigation menu The module is part of the `@ryu/ui` package and is available on every surface that includes the package.

This reference is part of the [Ryu UI overview](/docs/ui), the [component catalog](/docs/ui/components), and the [UI getting started guide](/docs/ui/getting-started). Read the [primitive usage guide](/docs/ui/primitives) for composition and ownership rules. In the same layout & navigation group, compare [Empty](/docs/ui/components/empty), [Item](/docs/ui/components/item), [Navigation Menu](/docs/ui/components/navigation-menu), [Page Header](/docs/ui/components/page-header).

## Preview

<UiComponentPreview component="components/motion-navigation-menu" exportName="MotionNavigationMenu" />

The preview uses representative props and is safe to interact with. It does not call a provider, write data, or depend on a host application.

## Settings

<UiComponentPreview component="components/motion-navigation-menu" exportName="MotionNavigationMenu" mode="settings" />

Use the controls to compare supported settings without changing application data or calling a provider.

## Import

```tsx
import { MotionNavigationMenu, MotionNavigationMenuContent, MotionNavigationMenuIndicator, MotionNavigationMenuItem, MotionNavigationMenuLink, MotionNavigationMenuList, MotionNavigationMenuTrigger, MotionNavigationMenuViewport } from "@ryu/ui/components/motion-navigation-menu";
```

The module exports `MotionNavigationMenu, MotionNavigationMenuContent, MotionNavigationMenuIndicator, MotionNavigationMenuItem, MotionNavigationMenuLink, MotionNavigationMenuList, MotionNavigationMenuTrigger, MotionNavigationMenuViewport`, so you can compose the primitive with its related parts.

## Usage

```tsx
import { MotionNavigationMenu } from "@ryu/ui/components/motion-navigation-menu";

export function Example() {
  return (
    <MotionNavigationMenu />
  );
}
```

Add the props required by your workflow and compose the named exports as needed. TypeScript provides the complete prop and event contract at the import site.

## Package path

`@ryu/ui/components/motion-navigation-menu`

This page is generated from the package export and source module so the catalog stays aligned with the usable component surface.
