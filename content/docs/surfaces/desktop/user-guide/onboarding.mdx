---
title: "Onboarding"
description: "Set up a Ryu node for personal or team work and finish your desktop preferences"
---

Ryu keeps two onboarding decisions separate so setup follows the thing you are configuring:

- **Node onboarding** applies to the server you are connected to. It records whether that node is
  for **Personal use** or **Team or company use**, and it controls which context Ryu builds next.
- **Desktop onboarding** applies only to this installed desktop. It covers appearance, window
  behavior, privacy choices, and other device-local preferences.

## Review the final getting-started and privacy step

The final Desktop onboarding screen combines the useful things to know before your first task with
the privacy disclosure. It explains that agents can take approved actions, that active or scheduled
work can continue in the background, and where to review connections and approvals. It also shows
the independent choices for product analytics, community stats, crash reports, and diagnostics
export using their normal defaults.

Choose the switches before selecting **Get started**. The choices are saved to the node's canonical
preference store and remain available under **Settings → Privacy**. Completing this screen is the
first-run disclosure, so Ryu does not interrupt the next launch with a separate privacy dialog.

## Choose the node's context

The first node setup step asks how you will use the node.

### Personal use

Personal setup lets you add a short note about yourself. It stays with the personal node and is
not turned into shared company knowledge.

### Team or company use

Team setup lets a node administrator add a short company starting point: the team, products,
customers, policies, or working style that should shape the next step. Ryu can then use approved,
read-only connections and imported work to draft shared company knowledge for the organization.
The draft is reviewable; onboarding never edits an external account or silently changes an agent.

Only a node administrator with the `gateway.configure` permission can save or reset team setup.
Personal-node setup is stricter: only that node's owner can save or reset it. The server enforces
both boundaries, including when a member uses a different desktop.

## Choose optional recommended skills

After the node context is chosen, onboarding shows the optional external skill collections available
for the current Ryu release. Each row names the collection and explains the workflows it adds. The
list starts with every collection selected, and provides **Select all** and **Unselect all** controls.
Only the rows still selected when you continue are installed on the node.

This choice belongs to the node because skills are installed in that node's shared Skills home. The
owner controls a personal node; an administrator with `gateway.configure` controls an organization or
team node. A member cannot change a shared node's selection. Ryu's embedded skills (including the
Ryu and pstack skills) and skills supplied by enabled Ryu plugins remain built in and are not shown
as optional rows.

The selection is also available to authenticated clients through:

```text
GET /api/onboarding/skills
PUT /api/onboarding/skills
```

The server validates pack ids and reconciles only the selected external repositories. An empty
`selectedPackIds` array is valid and means the node keeps only its built-in/plugin skills. A failed
download leaves the selection saved so Core can retry it on a later boot; the onboarding screen keeps
the error visible until the selected set is reconciled.

## What runs next

After the node context and optional skill selection are saved, onboarding continues with the node's agent/model defaults,
connections, optional thread imports, and the appropriate private profile or shared company
knowledge draft. The profile build uses the context selected for this node and keeps memory scopes
separate: personal setup writes user-scoped context, while team setup writes organization-scoped
company knowledge.

Sensitive memory remains a separate, default-off consent choice. See [Spaces & Memory](/docs/surfaces/desktop/user-guide/spaces-memory)
for memory scope, recall, and sensitive-topic controls.

## Reset node onboarding

To run node setup again, open the node selector, choose **Gateway settings**, then open
**This device -> Onboarding** and choose **Reset onboarding**. This resets only the node's onboarding
state; it does not delete chats, Spaces, memories, themes, or other data. The next launch (or a
reload) asks for the node context again.

The node state is available to authenticated clients through:

```text
GET    /api/onboarding/state
PUT    /api/onboarding/state
DELETE /api/onboarding/state
```

Desktop onboarding can be restarted independently from **App settings -> General -> Setup**.
Resetting desktop onboarding does not reset the node's personal/team choice.

## Related

<Cards>
  <DocCard href="/docs/surfaces/desktop/user-guide/spaces-memory" />
  <DocCard href="/docs/gateway/configuration-operations" />
  <DocCard href="/docs/surfaces/desktop/user-guide/data-and-storage" />
</Cards>
