---
title: "Search & Auto-Recall"
description: "Search chats, messages, Spaces, and pages, then let agents auto-recall relevant facts on every turn"
---

When **Remember chats** is enabled, Ryu indexes your chat history so you can find earlier discussions by meaning or exact words. Agents can also pull relevant parts of past chats into context automatically. There are two surfaces:

- **Search** - ask an agent to find earlier messages with the `search_conversations` tool.
- **Auto-recall** - a per-node toggle that injects relevant past messages and memory facts into every turn for you.

Semantic search and auto-recall use the local embedder (the `llamacpp-embed` sidecar on :8081 by default), so your chat content never leaves the node to be searched. Exact phrase search uses a local lexical index and does not require the embedder. The indexes are internal to Core: see [Memory & message index](/docs/core/memory) for how they are stored and kept encrypted.

<Callout type="info">
Chat-message indexing and both search modes are opt-in through **Remember chats** in Settings -> Memory. When it is off, new messages are not indexed and an exact or semantic search does not backfill older messages; conversations continue to be saved normally.
</Callout>

<Callout type="warn">
Both surfaces need a running embedder. Out of the box Core uses a local hashing embedder, but real semantic quality needs the `llamacpp-embed` sidecar on :8081 (downloaded during onboarding). If the embedder is down, search reports the index as unavailable and auto-recall fails open inside a 4s timeout, so a missing embedder never blocks a chat turn. This was verified network-free; live semantic quality across a running Core is not yet exercised in CI.
</Callout>

## Search your past conversations

Searching is done by an agent, through the built-in `search_conversations.search` tool (`apps/core/src/sidecar/mcp/search_conversations.rs`). Just ask in plain language:

```
Search our past chats for what we decided about the database schema
```

The tool runs a semantic search across your stored messages and returns matching snippets, each with the conversation id, the role, a timestamp, and a relevance score. Because it rides the unified tool path, it works on both the ACP and OpenAI-compatible chat planes, appears in `GET /api/tools/search`, and is allowlist-gated plus audited like any other tool.

<Callout type="info">
The tool is allowlist-gated. If an agent reports it cannot search your history, grant it the `search_conversations.search` tool in the agent's tools list. See [Tools & connections](/docs/surfaces/desktop/user-guide/tools-and-connections).
</Callout>

## Search chats, messages, Spaces, and pages

The desktop command palette also searches across the workspace. It searches chat titles, archived
chats, Spaces, page titles and page text, plus individual chat messages. Type at least two characters
to search, then use the **Messages** or **Pages** tabs to narrow the result list.

Message results put literal matches first and can add semantic matches when the local message index
is available, so you can search for an exact phrase or describe the discussion you mean. Page results
use literal matching over the page title and body, extracted page text, and the containing Space's
name and description; they do not require an embedding model. Selecting a message opens its
conversation and scrolls to the matched message. Selecting a page opens that page in its Space.

Search rows show a compact relative age such as **just now**, **5m ago**, or **2d ago** beside the
Space, chat, message, or page context. When a search index is unavailable, the palette keeps its
normal navigation and collection results instead of blocking navigation.

When the Memory app is enabled, its local semantic retrieval remains available to agents through the
`memory.search` capability. The chat-message search bridge is separate from durable facts: it searches
conversation messages and applies the same conversation visibility rules before returning a result.

### What the tool accepts

| Argument | Type | Default | Meaning |
|---|---|---|---|
| `query` | string | required | Natural-language search query |
| `limit` | integer (1-20) | 5 | Maximum number of matching snippets to return |
| `conversation_ids` | string array | all | Restrict the search to specific conversations; omit to search everything |

Snippets are truncated to keep tool output compact, so a long matched message comes back shortened. To read the full message, open the conversation the snippet points to.

<TryInRyu page="chat" />

## Auto-recall

Auto-recall does the searching for you. On every chat turn, Core semantically retrieves relevant past chat messages (excluding the current conversation) plus relevant long-term memory facts, and merges them into the system context before the model runs. It is the same injection seam skills use, so both chat planes inherit it.

Auto-recall is **on by default** and is persisted in Core, not in your client, so it applies to every chat turn the node serves rather than just the device you toggled it on.

Onboarding can also import recent sessions from the agents you selected. Imported conversation ids
are available to the optional paid profile build alongside connected source ids, so the first draft
can start with context instead of an empty memory store. Import is confirmable, and the future-thread
auto-import switch can be turned off in the onboarding step or later settings.

### Turn it on or off

Open **Settings -> Memory** and use the auto-recall switch (`MemoryTab.tsx`). The toggle writes the `auto-recall-enabled` preference (`apps/desktop/src/lib/api/preferences.ts`).

The underlying knobs:

| Knob | Where | Default | Effect |
|---|---|---|---|
| `auto-recall-enabled` | Settings -> Memory, or env `RYU_AUTO_RECALL_ENABLED` | on | Master switch for per-turn recall |
| `auto-recall-top-k` | preference key | implementation default | Caps how many recalled snippets are injected per turn |

The **Remember chats** switch is a separate privacy gate for the chat-message source. When it is
off, Core keeps the encrypted transcript but stops embedding, semantic-searching, and auto-recalling
past chat messages. Turning it back on allows the message index to be rebuilt. Durable memory facts
remain available through their own Memory settings.

<TryInRyu page="settings" />

### How it stays clean

Auto-recall only adds what the existing recency window missed. Memory facts injected by recall are deduplicated by fact id against the facts the recency path already added, so a fact never appears twice. The recency path is unchanged and remains the fail-open fallback, so disabling auto-recall does not lose your long-term memory entirely. The full mechanism lives on one page: [Memory & message index](/docs/core/memory).

<Callout type="info">
Auto-recall is a separate setting from your long-term memory facts. Turning recall off stops the per-turn semantic injection but leaves your stored facts and the recency-based fallback intact.
</Callout>

## Related

<Cards>
  <DocCard href="/docs/core/memory" />
  <DocCard href="/docs/surfaces/desktop/user-guide/spaces-memory" />
  <DocCard href="/docs/surfaces/desktop/user-guide/tools-and-connections" />
  <DocCard href="/docs/surfaces/desktop/user-guide/chat" />
</Cards>
