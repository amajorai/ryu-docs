---
title: "Ryu Bot Desktop"
description: "The account-only, managed Ryu desktop experience for everyday chat."
---

Ryu Bot is the managed desktop experience for people who want to use Ryu without configuring an AI
stack. Sign in with a Ryu account, keep an active subscription, and start a conversation.

## What Bot includes

- A focused chat workspace with saved conversations and the shared Ryu Work language mode.
- A Ryu-managed workspace that connects automatically when it is available for your account.
- Ryu-managed model routing, with no provider key or model picker to configure.
- The same chat runtime used by Ryu Console, including Pi and sandbox actions when the managed
  workspace and the current organization allow them.
- Account access and the minimum managed-workspace status needed to explain connection problems.

Bot keeps the visual language and chat behavior of the full desktop app, but its shell does not
show provider setup, model selection, routing, infrastructure, evaluation, agent editing, or
permission configuration.

## Sign-in and managed access

Bot checks the signed-in account's subscription and managed-workspace entitlement before opening
chat. If the account is not subscribed, the app links to the account's subscription flow. If a
subscription is active while its workspace is still being prepared, Bot waits and lets you refresh
the workspace state.

The managed connection uses the account and organization access already enforced by Ryu's control
plane and Core. Bot does not accept a personal provider key or let the client choose another node.

## Device and sandbox actions

Bot does not weaken device-access or sandbox security. Pi and Core continue to enforce the
existing server-side and organization permissions, and the operating system continues to request
the native permissions that an action needs. An action can still be refused when the account,
organization, managed workspace, or operating system does not allow it.

## Bot and Console

Ryu Bot and [Ryu Console](/docs/surfaces/desktop) use the same desktop app, Core, Gateway, managed
Pi, sandbox, and device-access runtime. The sidebar product lockup shows Bot by default. For a
Ryu-managed node, only an owner or admin of that node's organization can open the Bot/Console menu
and switch to Console. Organization members do not see that control.

On a regular Desktop install, **Settings → General → On startup → Realm on startup** controls which
realm opens next time. **Last used** is the default policy: a new user starts in Bot, and later launches
follow the most recent Bot, Console, or OS choice. An explicit realm can be selected instead. Console
access is still checked when the app starts, so an unavailable Console choice falls back to Bot.

Self-hosted and local nodes do not have Ryu platform organization authority. They open directly in
Console, where the node owner remains responsible for its own administration and security. The
Ryu-managed Bot experience is the platform-managed path, not a self-hosted capability.

Console is the power-user desktop surface for configuring managed or self-hosted nodes, bringing
your own AI, and operating the wider Ryu workspace. Use Console when you need those controls; they
are intentionally unavailable in Bot.

Island is not part of the Bot desktop product.

For privacy and telemetry behavior shared by the desktop runtime, see
[Privacy and Telemetry](/docs/surfaces/desktop/transparency). For the underlying chat behavior, see
[Chat Features](/docs/surfaces/desktop/user-guide/chat).
