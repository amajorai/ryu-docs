---
title: Your first agent
description: Meet the default Ryu agent and understand what an agent is in Ryu.
level: 100
---

Ryu installs exactly one agent by default, named **Ryu**. It is the flagship demo of the whole idea,
so it is worth understanding before you chat with it.

## What is an agent in Ryu?

Think of an agent as a **trading card**. It has independently swappable slots:

- a **chat model** that generates text
- optional **voice** and **image** models
- **tools and MCP servers** it is allowed to call
- **memory** and **Spaces** for what it can recall
- a **persona** and instructions
- a slice of **Gateway policy** that governs its calls

No two cards have to be alike. You can build an agent for coding with one model and toolset, and a
different agent for support with another, and switch between them per chat.

## The default Ryu agent

The default **Ryu** agent is Pi with the Gateway on top. Pi is a capable agent engine. The Gateway
is Ryu's control layer. Putting the Gateway in front of Pi is the headline demo of "the car around
the engine" - the same engine, now governed, routed, and measured.

The Ryu agent is self-sufficient. It manages its own engine binary, so it works without any extra
setup.

## Where to find agents

<TryInRyu page="agents" />

Open the **Agents** page from the sidebar or the command palette. You will see the installed set,
which starts as just the Ryu agent. From here you can later:

- browse the **agents catalog** and add other agents such as Claude Code, Codex, Gemini CLI, or bare
  Pi, if you have their tools installed
- create your own agent on a single page
- edit an agent's slots

You will do all of that in the [Operating Ryu](/docs/academy/operator) course. For now you only need
one agent, and you already have it.

<Callout type="info">
  The Ryu agent is a locked built-in. You can still tune its model and provider through a dedicated
  config panel, which you will see in the next course.
</Callout>

## Knowledge check

First, the reflection prompts. Say the answers out loud or jot them down, in your own words.

- What are three of the swappable slots on an agent card?
- What two things make up the default Ryu agent?
- Where in the app do you browse and add more agents?

Then confirm the details with a quick self-test.

<Quiz
  questions={[
    {
      q: "In Ryu, what is a good mental model for an agent?",
      options: [
        "A single fixed model you cannot change",
        "A trading card with independently swappable slots",
        "A cloud service you rent per request",
      ],
      answer: 1,
      explain:
        "An agent is like a trading card with independently swappable slots for model, tools, memory, persona, and policy. No two cards have to be alike.",
    },
    {
      q: "What two things make up the default Ryu agent?",
      options: [
        "Pi with the Gateway on top",
        "Claude Code with a local model",
        "Gemma with a cloud key",
      ],
      answer: 0,
      explain:
        "The default Ryu agent is Pi with the Gateway on top, the headline demo of the car around the engine.",
    },
    {
      q: "Why does the Ryu agent work without extra setup?",
      options: [
        "It requires you to install Pi separately first",
        "It is self-sufficient and manages its own engine binary",
        "It only runs after you sign in",
      ],
      answer: 1,
      explain:
        "The Ryu agent is self-sufficient. It manages its own engine binary, so it works without any extra setup.",
    },
    {
      q: "Where do you browse and add more agents?",
      options: [
        "The Models page",
        "The Agents page, from the sidebar or command palette",
        "The Gateway settings dialog",
      ],
      answer: 1,
      explain:
        "Open the Agents page from the sidebar or the command palette to see the installed set and browse the agents catalog.",
    },
  ]}
/>

Now put the agent to work in [Your first chat](/docs/academy/essentials/your-first-chat).
