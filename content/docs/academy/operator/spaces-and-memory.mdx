---
title: Spaces and memory
description: Spaces are private vector databases for retrieval, and memory is encrypted long-term recall. Both are per-agent slots.
level: 200
---

Two slots on an agent card give it knowledge beyond the conversation in front of it. Spaces hold documents you can retrieve from, and memory holds things the agent should remember over time. Both are per-agent, so different cards can have different knowledge.

## Spaces

<TryInRyu page="spaces" />

A Space is a vector database for retrieval-augmented generation (RAG). Open the **Spaces** page to create a Space and add documents to it.

What makes Ryu's Spaces private is where the embedding happens. Embeddings are generated by a local embedding model (a nomic GGUF served on your own machine), so the contents of your documents do not leave your device to be indexed.

You can also edit markdown pages directly inside a Space. When you save, the page re-embeds automatically, so retrieval always reflects the latest text.

Under the hood, retrieval works by cosine similarity over the stored embeddings - the agent finds the chunks closest in meaning to the question and pulls them in.

<Callout type="info">
  Because embeddings are local, you can add sensitive documents to a Space without sending them to an external indexing service.
</Callout>

## Memory

Memory is encrypted long-term storage the agent can recall from later. Where a Space is a body of documents you curate, memory is the agent's own running recollection.

It is encrypted, and like Spaces it is a per-agent slot. One agent's memory is not another's.

## Per-agent by design

Keeping both slots per-agent is what lets you build sharply different cards:

- A research agent with a large documents Space and rich memory.
- A throwaway drafting agent with neither.

Fill the slots to match the job, not the other way around.

<Callout type="warn">
  Retrieval quality depends on what is in the Space. If answers seem to miss obvious facts, check that the relevant document was added and has re-embedded after edits.
</Callout>

## Knowledge check

First, the reflection prompts. Answer them in your own words.

- Why does keeping embeddings local make Spaces more private?
- What happens to a Space's markdown page after you edit and save it?
- Why might two agents give different answers from the "same" knowledge?

Then confirm the details with a quick self-test.

<Quiz
  questions={[
    {
      q: "What makes Ryu's Spaces private?",
      options: [
        "Documents are encrypted before upload to an indexing service",
        "Embeddings are generated by a local model, so document contents do not leave your device",
        "Only the agent owner can read a Space",
      ],
      answer: 1,
      explain:
        "Embeddings are generated by a local nomic GGUF on your own machine, so your document contents are not sent out to be indexed.",
    },
    {
      q: "What happens to a Space's markdown page after you edit and save it?",
      options: [
        "It re-embeds automatically, so retrieval reflects the latest text",
        "It is archived and a new page is created",
        "Nothing until you manually rebuild the index",
      ],
      answer: 0,
      explain:
        "When you save an edited page, it re-embeds automatically so retrieval always reflects the latest text.",
    },
    {
      q: "How does retrieval find the right chunks for a question?",
      options: [
        "By exact keyword match",
        "By cosine similarity over the stored embeddings, pulling the chunks closest in meaning",
        "By the order documents were added",
      ],
      answer: 1,
      explain:
        "Retrieval works by cosine similarity over the stored embeddings, finding the chunks closest in meaning to the question.",
    },
    {
      q: "Why can two agents give different answers from the same knowledge?",
      options: [
        "Spaces and memory are per-agent slots, so different cards have different knowledge",
        "Each agent uses a different embedding model",
        "Memory is shared but Spaces are not",
      ],
      answer: 0,
      explain:
        "Both Spaces and memory are per-agent slots, so one agent's knowledge is not another's.",
    },
  ]}
/>

Next: put your agents to work on their own in [Automations](/docs/academy/operator/automations).
