---
title: Deploying Ryu
description: What self-hosting ships, how managed Cloud is provisioned, and where the line between feature and service sits.
level: 500
---

When you deploy Ryu for a team, two open-source Rust services do the work: the Core and the Gateway. The Core orchestrates what runs (agents, sessions, tools, workflows). The Gateway governs every model call (routing, firewall, budgets, audit). The Core is Apache-2.0 and the Gateway is AGPL v3; both are open-source and self-hostable. The desktop, web, and backend stay closed, which is the open core plus managed cloud model in practice.

## What gets deployed

A Ryu deployment is the Core plus the Gateway. A few properties matter for a server:

- Headless-first: the Core runs with no UI, no cloud requirement, and no streaming required.
- Encrypted by default, with no telemetry by default.
- The Gateway sits on the default chat path, so a hosted node is already governed the moment it serves traffic.

You point your clients (desktop, CLI, bots) at the node, and they use it like any other Ryu node.

## Self-hosting is about trust

Self-hosting Ryu is not only about cost or control. The parts that orchestrate open-source engines are themselves open. You can read the Core and the Gateway, audit how model calls are routed and governed, and run them on your own hardware. Nothing about the control layer is a black box.

<Callout type="info">
  The Gateway being on the default path means self-hosting does not trade away governance. The firewall, routing, and audit travel with the deployment.
</Callout>

## Managed Cloud and provisioning

There is one-command cloud provisioning, an hcloud-based provision flow with a backups add-on and deploy docs. That gets a node stood up quickly.

Be honest about the rest. Much of managed Ryu Cloud is a service offering, not a single button: A Major audits your needs, builds the agents, deploys them, and hosts them for you. The provisioning command is real, but "we run it for you" is the part that is people plus process.

<Callout type="warn">
  Do not expect the full managed Cloud experience from a provisioning script alone. The script stands up infrastructure; the audit, build, and ongoing operation are the service layer on top.
</Callout>

## One-click deploy

If you do not want to manage a VPS, a hosted node also deploys to a container
platform in one click. Each button builds the repo's `Dockerfile`, which runs
Core (Core spawns and manages the Gateway on loopback), so the node is governed
the moment it serves traffic.

[![Deploy to Render](https://render.com/images/deploy-to-render-button.svg)](https://render.com/deploy?repo=https://github.com/amajorai/ryu)
&nbsp;
[![Deploy on Railway](https://railway.com/button.svg)](https://railway.com/new)
&nbsp;
[![Deploy to DigitalOcean](https://www.deploytodo.com/do-btn-blue.svg)](https://cloud.digitalocean.com/apps/new?repo=https://github.com/amajorai/ryu/tree/main)

Core downloads a local model stack on first boot, so choose a plan with at least
2 GB of RAM, or set a provider key (such as `OPENAI_API_KEY`) to skip the local
download. The Gateway is AGPL v3, so hosting a modified Gateway carries the AGPL
section 13 obligation to offer your changes to its users. See
[Deploy & self-host](/docs/start-here/getting-started/self-host) for the full
walkthrough (Docker, Fly, and the Vercel-for-docs caveat).

## Knowledge check

First, the reflection prompts. Answer them in your own words.

- Which two open-source Rust services make up a Ryu deployment?
- Why does a hosted node stay governed without extra setup?
- What part of managed Cloud is a service offering rather than a single button?

Then confirm the details with a quick self-test.

<Quiz
  questions={[
    {
      q: "Which two open-source Rust services make up a Ryu deployment?",
      options: [
        "The desktop and the web app",
        "The Core and the Gateway",
        "The Core and the control-plane backend",
      ],
      answer: 1,
      explain:
        "A Ryu deployment is the Core, which orchestrates what runs, plus the Gateway, which governs every model call. The Core is Apache-2.0 and the Gateway is AGPL v3; both are open-source and self-hostable.",
    },
    {
      q: "Why does a hosted node stay governed without extra setup?",
      options: [
        "The Gateway sits on the default chat path, so a node is governed the moment it serves traffic",
        "Every client enables governance manually after connecting",
        "Governance is handled by the closed-source desktop app",
      ],
      answer: 0,
      explain:
        "The Gateway being on the default path means self-hosting does not trade away governance. The firewall, routing, and audit travel with the deployment.",
    },
    {
      q: "What part of managed Ryu Cloud is a service offering rather than a single button?",
      options: [
        "The one-command provisioning script",
        "The audit, build, and ongoing operation done by A Major",
        "The Core and Gateway binaries",
      ],
      answer: 1,
      explain:
        "The provisioning command stands up infrastructure, but the audit, build, and ongoing operation are the service layer of people plus process on top.",
    },
  ]}
/>

Next: put your deployed agents into chat platforms in [channel bots](/docs/academy/cloud/channel-bots).
