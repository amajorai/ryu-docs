---
title: "Metering Rates"
description: "Exactly what each unit of usage costs: models, tool calls, image and video, speech, and sandbox compute."
---

Every credit Ryu Cloud spends is metered against one of the units below. This page is the
reference for what each one costs; [Credits & AI Pools](/docs/billing/credits) covers the wallet
itself: pools, top-ups, spend order and auto-recharge.

<Callout type="info">
These rates apply to **Ryu Cloud** (managed inference). Running your own server with your own
provider keys bills you directly from that provider and Ryu meters nothing.
</Callout>

## Models

Model tokens are metered **at cost**. Ryu applies no per-token markup. You pay what the upstream
provider charges, and the platform's margin is taken once at deposit instead (see
[deposit fees](/docs/billing/credits#deposit-fees)).

The Gateway also supports an explicit provider-level billing policy. A provider configured with
`[credits.provider_billing.<provider>] mode = "pass_through"` is charged from its raw upstream cost;
providers without that entry inherit the Gateway's global `markup_bps` setting. The raw provider
amount remains available for audit and reconciliation, while the charged amount is what budgets,
wallet debits, and usage statements use. This billing setting is separate from
[subscription credential passthrough](/docs/gateway/gateway-for-any-agent#subscription-preserving-passthrough-claude-code-codex).

Model prices vary per model and change when providers change theirs, so they are not reproduced
here. The model picker shows the current rate for each entry, and every charge appears on your
usage statement with the model that produced it.

For models routed through Ryu's managed OpenRouter path, the picker uses OpenRouter's current
transaction price. That price already includes an active provider promotion, so a temporary
discount appears as a lower input or output rate and flows through to the wallet debit, usage
statement, and shared-budget accounting after the provider billing policy is applied. Daily
rollups retain the raw transaction price for audit and reconciliation. The price is refreshed
periodically because OpenRouter promotions can change. OpenRouter BYOK traffic remains your
provider account's spend; it is not converted into Ryu managed credit usage.
If either balance is exhausted, chat identifies whether the stopped turn needs
Ryu organization credits or OpenRouter BYOK credits and keeps that error in the
conversation with a Retry action. See [When credits run out in
chat](/docs/billing/credits#when-credits-run-out-in-chat).

Per-agent budget caps use **USD**, represented on the wire as integer micro-USD (`1_000_000 = $1`),
not tokens. For OpenRouter responses, the inline `usage.cost` is the source used for the immediate
charge. OpenRouter generation metadata can be reconciled later using the stable external-user
identity Ryu sends for the agent. A generation lookup should reconcile history, not charge the same
request a second time.

The WhatsApp channel is a delivery transport, not a model provider. A successful Meta `POST
/messages` response currently returns message identifiers rather than a billable fee, so Ryu does
not infer a WhatsApp/Twilio/Sent charge from that response. A future adapter may supply an explicit
provider cost or configured rate through the Gateway billing seam.

Model inference, media generation, and paid Composio actions are separate budget categories. Each
agent can choose which categories count. Subscription ACP passthroughs and direct-egress agents do
not produce an OpenRouter cost, while self-hosted/local models use configured estimates rather than
pretending Ryu knows the operator's electricity or GPU bill.

## Agent tool calls

| Unit | Rate |
|---|---|
| Tool execution | **$0.30 per 1,000 calls** on the standard Composio rate |

Billed at cost; this is the integration provider's standard rate, passed through.
Managed-app or premium-tool contracts can have a higher provider rate, so the
deployment must override `cost_per_tool_call_micro_usd` to match its invoice. A
"tool call" is one executed action against a connected integration, not a step
of the model's reasoning.

## Image and video generation

Image and video use the provider's reported `usage.cost` when it is available. If the provider does
not report a cost, they are metered on the compute time the generation actually used; when neither is
available, Ryu uses the configured flat fallback estimate.

| Unit | Rate |
|---|---|
| Generation compute | **$0.000975 per second** ($3.51/hour) |

Time spent queued before a machine picks the job up is **not** billed. Only the run itself is.

The cost difference comes from the measured run time. A fast 2-second render and a slow 30-second
render are charged for what they use rather than averaged, so a short clip costs a fraction of a
long one.

<Callout type="info">
If a provider returns no cost or timing for a completed job, Ryu falls back to a flat per-call rate:
$0.00195 for an image, $0.0585 for a video. These are estimates of a typical render and only apply
when there is nothing measured to bill from.
</Callout>

## Speech

Speech has no per-second measurement available from the providers, so it is billed per call.

| Unit | Rate |
|---|---|
| Audio (per synthesis) | **$0.000975** |
| Transcription (per request) | **$0.000975** |

## Sandbox compute

A sandbox is a real machine running your agent's code. It is metered **per second, only while it
is running**. A stopped sandbox costs nothing.

Ryu Box has two settlement paths. A Box backed by Ryu-owned capacity registers with the existing
Gateway sandbox heartbeat and follows the rates below. A Box configured for connected provider
mode uses the provider subscription as its billing authority, so Core does not debit
the same machine time from the Ryu wallet. The passthrough flag is explicit because it changes
who owns the spend decision; it never turns a local development filesystem driver into an
isolation boundary. See [Persistent Boxes](/docs/core/boxes#connected-provider-mode).

Charges are the sum of every resource the sandbox holds, per second:

| Resource | Rate |
|---|---|
| vCPU | **$0.0655** per vCPU-hour |
| Memory | **$0.0211** per GiB-hour |
| Storage (above the free allowance) | **$0.10** per GiB-month |
| Windows surcharge | **+$0.1114** per vCPU-hour |

The first **5 GiB** of sandbox storage is free; only usage above that is billed.

### GPU sandboxes

| GPU | Rate |
|---|---|
| H200 | **$5.90** per GPU-hour |
| H100 | **$5.13** per GPU-hour |
| RTX PRO 6000 | **$3.94** per GPU-hour |
| RTX 5090 | **$1.68** per GPU-hour |
| RTX 4090 | **$1.29** per GPU-hour |

GPU time is charged in addition to the vCPU, memory and storage of the same sandbox.

<Callout type="warn">
A sandbox bills for **wall-clock time while running**, not CPU time. An idle sandbox left open
still costs money. Set a per-run budget cap under **Settings → Billing → Spend controls** so a
looping agent cannot drain the wallet, and stop sandboxes you are not using.
</Callout>

## Managed cloud servers

A managed server is a monthly subscription rather than metered usage, so it does not draw on
credits at all. Teams starts with one included managed server and adds a second at 50 billed seats;
Business includes one performance server. Larger or dedicated instances are priced by the month
from the live catalog when you pick one.

An organization's server capacity is additive: plan-included servers plus each active cloud-instance
subscription. A server transfer carries its own subscription entitlement and takes the lowest free
destination slot. If that slot was consumed before payment completes, Ryu refuses the handover
rather than orphaning either managed server.

The hosted-server picker starts with three workload presets: Starter, Balanced, and Power. Choose a
preset by the headroom you need, or open **Advanced selection** to browse
every available instance with its exact CPU, RAM, SSD, price, and regional availability.

## What is never billed

- **Local models** on your own machine or your own server
- **Queue time** before a generation job starts running
- **Stopped sandboxes** — storage is billed while it exists, compute only while running
- **Failed requests** that returned no result

## Seeing what you spent

Every charge is written to your organization's usage statement with the model or resource that
produced it, what it was for, and when. Open it from **Settings → Billing → Usage** in the desktop
app, or from the Credits page on the web dashboard.
