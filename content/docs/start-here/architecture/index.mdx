---
title: "Architecture"
description: "The system at a glance: clients, Core, Gateway, engines, capability layers, and how one request flows through them."
---

Ryu is **the whole car built around AI agent engines**: the engines already exist (OpenAI, Claude
Code, Gemma, any OpenAI-compatible runtime), and Ryu is the orchestration and control layer around
them. This is the one-page map; every box on the diagram has its own deeper section.

## The full architecture

```mermaid
flowchart TB
  subgraph Surfaces["Client surfaces"]
    direction LR
    D["🖥️ Desktop\nTauri v2"]
    W["🌐 Web\nNext.js"]
    M["📱 Mobile\nExpo/RN"]
    T["💻 TUI\nBun + OpenTUI"]
    E["🧩 Extension\nWXT"]
    I["🏝️ Island\nElectron"]
    B["💬 Bots\nTG · Slack · WA · DC"]
  end

  subgraph Gateway["Ryu Gateway · the moat"]
    direction TB
    GW_R["Router\nsmart · fallback · eval"]
    GW_F["Firewall\nPII/DLP · prompt-injection"]
    GW_B["Budgets\nper-user · per-agent · per-session"]
    GW_E["Evals + Audit\n30 evaluators · per-request trail"]
    GW_C["Cache\nexact · semantic"]
    GW_T["Tools\nsearch · allowlist · exec-budget"]
    GW_P["Passthrough\nClaude Code · Codex proxy"]
  end

  subgraph Core["Ryu Core · orchestration engine"]
    direction TB
    CR_S["Sessions + Chat\nstreaming ACP / OpenAI-compat"]
    CR_R["Runnable Registry\nagent · workflow · tool · skill"]
    CR_M["Memory + RAG\nspaces · retrieval · search"]
    CR_W["Workflows\nDAG engine · durable execution"]
    CR_SC["Sidecar Manager\n~16 engines · health-check"]
    CR_PL["Plugin Lifecycle\ninstall · enable · disable"]
    CR_CB["Capability Broker\nresolve abstract → concrete"]
  end

  subgraph Engines["Any engine (BYO everything)"]
    direction LR
    EN_L["llama.cpp + Gemma 4\n(local default)"]
    EN_O["OpenAI\nGPT-4o · o1 · o3"]
    EN_A["Anthropic\nClaude Opus · Sonnet"]
    EN_G["Google\nGemini 2.5 Pro/Flash"]
    EN_OR["OpenRouter\nmeta-routing"]
    EN_ACP["ACP agents\nClaude Code · Codex · Pi"]
  end

  D & W & M & T & E & I & B -->|"HTTP/SSE :7980"| CR_S
  CR_S -->|"every model call"| GW_R
  GW_R --> GW_F --> GW_B --> GW_E
  GW_C --> GW_R
  GW_T --> CR_R
  GW_P --> EN_ACP
  GW_E --> EN_L & EN_O & EN_A & EN_G & EN_OR
  CR_SC --> EN_L
  CR_CB --> CR_SC
  CR_R --> CR_M & CR_W & CR_PL
```

## The single most important rule

Two Rust services do the work: **Ryu Core** (`apps/core`, default `127.0.0.1:7980`) and **Ryu
Gateway** (`apps/gateway`). They are split by one rule:

- If code decides **what runs** (which agent, session, workflow, or tool) it is **Core**.
- If code decides **what is allowed, shared, measured, or paid for** (security, routing, registry,
  evals, budgets, org policy, audit) it is **Gateway**.

Core never enforces policy inline. It routes every model call through the Gateway. The Gateway is
the shared part a team adopts and an enterprise buys.

## How one request flows

A request from any client travels down through Core (which decides what runs), into the Gateway
(which governs the model call), out to an engine, and back.

```mermaid
sequenceDiagram
  participant Client
  participant Core
  participant Gateway
  participant Engine

  Client->>Core: POST /api/chat/stream
  Note over Core: Pick agent, assemble memory,<br/>inject allowlisted tools
  Core->>Gateway: Forward model call<br/>(via_gateway: true)
  Note over Gateway: Route, firewall, budget,<br/>cache, audit
  Gateway->>Engine: Call upstream provider
  Engine-->>Gateway: Streaming response
  Note over Gateway: Response DLP, audit
  Gateway-->>Core: Governed response
  Core-->>Client: Stream to client
```

## The capability layer model

Ryu decomposes into ~40 self-contained packages in layers (L0-L3 + Gateway). Each layer
depends only on layers below it, forming a DAG with no cycles.

```mermaid
flowchart TB
  K["Kernel\nsessions · registry · sidecars · auth"]
  Lg["Gateway (Lg)\nrouter · firewall · budgets · evals"]
  L3["L3 Apps\nmeetings · monitors · workflows · skills"]
  L2["L2 Features\nmemory · spaces · retrieval · tool-exec"]
  L1["L1 Primitives\nrag · tts · stt · sandbox · mesh"]
  L0["L0 Primitives\nstorage · crypto · engines · downloads"]

  K --> Lg
  K --> L3
  L3 --> L2
  L2 --> L1
  L1 --> L0
  Lg --> L0
```

See [Capability Layers](/docs/start-here/architecture/capability-layers) for the full map.

## The two routing planes

Ryu has two distinct routing mechanisms:

- **Plane A (Model routing)** — Gateway picks *which LLM* one completion uses. Smart routing
  (classifier/embedding/keyword) rewrites `body["model"]`, then `ModelRouter::route` maps
  model to provider.
- **Plane B (Agent routing)** — Core picks *which agent* serves the turn. OpenAI-compat agents
  feed Plane A (governed); ACP agents self-route (only tool egress governed via MCP bridge).

```mermaid
flowchart LR
  subgraph PlaneA["Plane A · Model routing (Gateway)"]
    direction TB
    SR["Smart routing\nclassifier picks model"]
    MR["Model router\nmodel → provider"]
    SR --> MR
  end

  subgraph PlaneB["Plane B · Agent routing (Core)"]
    direction TB
    AR["Agent route\npick agent"]
    OAI["OpenAI-compat\n→ feeds Plane A"]
    ACP["ACP subprocess\nself-routes"]
    AR --> OAI
    AR --> ACP
  end

  PlaneB -->|"OpenAI-compat agents"| PlaneA
  ACP -->|"tool egress only"| Gateway["Gateway\ngovernance layer"]
  MR --> Gateway
```

See [Routing planes](/docs/gateway/routing-planes) for the full walkthrough.

## The Runnable model

Everything Ryu executes is a **Runnable** — one contract with input, run, and output. The
eight kinds are peers, not a hierarchy.

```mermaid
flowchart TB
  R["Runnable\ninput → run → output"]
  AG["agent\nmulti-turn model loop"]
  WF["workflow\nDAG of typed nodes"]
  TL["tool\ncallable function"]
  SK["skill\nreusable capability block"]
  CP["companion\nin-desktop overlay"]
  CH["channel\nmessaging bot adapter"]
  EN["engine\ninference backend"]
  PL["policy\nGateway-enforced rule"]

  R --> AG & WF & TL & SK & CP & CH & EN & PL

  AG -->|"invokes as tool"| TL
  WF -->|"calls as step"| AG
  Plugin["Plugin\nmanifest.json manifest"] -->|"bundles"| R
```

See [The Runnable model](/docs/start-here/architecture/runnable-model) for the full vocabulary.

## Where to go next

<AutoCards url="/docs/start-here/architecture" />

<Callout type="info">
  The boxes above each have their own realm. [Core](/docs/core) and [Gateway](/docs/gateway) hold
  the full subsystem reference; [Engines](/docs/desktop/engines) covers the swappable runtimes
  Core manages as sidecars.
</Callout>
