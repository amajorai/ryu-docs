---
title: The Runnable model
description: Runnable, Session, and App — the vocabulary every part of Ryu shares, with a hierarchy diagram.
tags: ["runnable", "session", "app", "object-model", "architecture", "agent", "workflow", "tool", "skill"]
---

Ryu describes everything it can run with one small vocabulary. Learn these three words and the rest
of the system reads cleanly.

## The object model

```mermaid
flowchart TB
  subgraph Plugin["Plugin (manifest.json)"]
    direction TB
    P_META["Manifest\nid · version · permissions"]
    P_RUN["Runnables\nwhat the plugin contributes"]
    P_COMP["Companion\noptional UI surface"]
    P_META --> P_RUN
    P_META --> P_COMP
  end

  subgraph Runnables["Runnables (8 kinds, peers not hierarchy)"]
    direction LR
    AG["agent\nmulti-turn model loop"]
    WF["workflow\nDAG of typed nodes"]
    TL["tool\ncallable function"]
    SK["skill\nreusable capability block"]
    CP["companion\nin-desktop overlay"]
    CH["channel\nmessaging bot adapter"]
    EN["engine\ninference backend"]
    PL["policy\nGateway-enforced rule"]
  end

  subgraph Session["Session"]
    S_HIST["conversation history"]
    S_STATE["run status\nidle · running · completed · failed"]
    S_RID["runnable_id + kind"]
    S_HIST --> S_STATE
    S_RID --> S_HIST
  end

  subgraph Gateway["Gateway"]
    GW_GOV["governance\nfirewall · budget · audit"]
  end

  Plugin --> Runnables
  AG -->|"invokes as tool"| TL
  WF -->|"calls as step"| AG
  Runnables -->|"runs in"| Session
  Session -->|"model calls"| Gateway
  Gateway --> Engine["Engine\nllama.cpp · OpenAI · etc."]
```

## Runnable

A **Runnable** is the one contract that unifies the things Ryu executes. Input goes in, the Runnable
runs, output comes out. Core defines the contract in `apps/core/src/runnable/mod.rs` with a
`RunnableKind` enum of eight variants.

| Kind | What it is |
|---|---|
| `agent` | A multi-turn model loop with swappable model, tools, memory, and persona slots |
| `workflow` | A DAG of typed nodes executed by Core's workflow engine |
| `tool` | A callable function exposed to agents and workflows (wraps an MCP tool) |
| `skill` | An Agent Skill: a reusable, shareable capability block |
| `companion` | An in-desktop overlay or sidebar panel |
| `channel` | A messaging-platform bot adapter (Telegram, Slack, WhatsApp, Discord) |
| `engine` | A model or inference backend binding wired into the Gateway registry |
| `policy` | A Gateway-enforced rule fragment (firewall, PII/DLP, budget) |

The kinds are **peers, not a hierarchy**. An agent can invoke a workflow by exposing it as a named
tool; a workflow can orchestrate agents by calling them as steps. The `RunnableRegistry` activates
Runnables from plugin manifests, with partial-install semantics so one failing Runnable does not abort
the others.

### How Runnables compose

```mermaid
flowchart LR
  subgraph Agent["Agent"]
    A1["model call"]
    A2["tool call"]
    A3["memory recall"]
  end

  subgraph Workflow["Workflow"]
    W1["prompt node"]
    W2["tool node"]
    W3["condition"]
    W4["agent node"]
  end

  subgraph MCP["MCP Server"]
    M1["tool: crawl"]
    M2["tool: search"]
  end

  A2 -->|"wraps"| MCP
  W2 -->|"invokes"| MCP
  W4 -->|"calls as step"| Agent
  A1 -->|"can invoke as tool"| Workflow
```

<Callout type="warn">
  Core validates and activates all eight kinds through per-kind handlers. The handlers register
  agents, workflows, tools, skills, engines, channels, and companions into their corresponding
  stores or contribution registries. Policy entries are validated and coordinated through the
  policy pass, while the Gateway remains the authority for firewall, routing, budget, and other
  enforcement. Enable reports per-entry failures instead of silently dropping a kind. See
  [Plugin manifests](/docs/extend/develop/extensions/plugin-json-manifest).
</Callout>

<Callout type="info">
  The TypeScript SDK authors only the four most common kinds (`agent`, `workflow`, `tool`, `skill`);
  the other four are declared directly in `manifest.json`.
</Callout>

## Session

A **Session** owns a run's conversation history and status. It lives in Core
(`apps/core/src/server/conversations.rs`) and binds a conversation to a Runnable.

```mermaid
stateDiagram-v2
  [*] --> Idle
  Idle --> Running: start run
  Running --> Completed: run finished
  Running --> Failed: error
  Running --> Idle: user cancels
  Completed --> Idle: new run
  Failed --> Idle: retry
```

A session has a `runnable_id`, a `runnable_kind`, and a `status` that is one of `idle`, `running`,
`completed`, or `failed`. Sessions are how Ryu tracks background runs and shows a conversation's run
list. See [Conversations and sessions](/docs/core/conversations-sessions).

## Plugin

A **Plugin** is a `manifest.json` manifest that bundles one or more Runnables plus the permission grants
they need, packaged so Core can install, enable, disable, and update it as a unit. It is modeled on
the plugin-manifest pattern: one declarative file describing everything the plugin contributes.

An agent inside a plugin is a "Pokemon card" with independently swappable slots:

```mermaid
flowchart TB
  Agent["Agent Card"]
  Chat["Chat Model\n(gpt-4o, claude, local)"]
  VoiceRecognition["Voice Recognition Engine\n(whisper, parakeet)"]
  Audio["Audio Engine\n(OuteTTS, KittenTTS)"]
  IMG["Image Model\n(DALL-E, stable-diffusion)"]
  Tools["Tools / MCP\n(spiders, browser, custom)"]
  Mem["Memory / Spaces\n(RAG, GraphRAG)"]
  Persona["Persona\n(system prompt, style)"]
  Policy["Gateway Policy\n(firewall, budget)"]

  Agent --> Chat & VoiceRecognition & Audio & IMG & Tools & Mem & Persona & Policy
```

No two cards are alike, and you can change any slot without rebuilding the agent.

See [Plugin manifests](/docs/extend/develop/extensions/plugin-json-manifest) for the full schema and
[The Store](/docs/surfaces/desktop/user-guide/store) for installing plugins.

## How they connect

```
Plugin (manifest.json)
  bundles -> Runnables (agent, workflow, tool, skill, ...)
                 runs in -> Session (history + status, in Core)
                                model calls -> Gateway -> engine
```

A Plugin is what you install. Its Runnables are what execute. A Session is the live, stateful run of a
Runnable. And every model call a Runnable makes is routed through the Gateway. This is the same
vocabulary the [SDK](/docs/extend/develop/sdk) uses, so code you author with `defineAgent` and friends maps
one-to-one onto what Core runs.

## Related

<Cards>
  <DocCard href="/docs/start-here/architecture/capability-layers" />
  <DocCard href="/docs/start-here/architecture/core-vs-gateway" />
  <DocCard href="/docs/extend/develop/extensions/plugin-json-manifest" />
  <DocCard href="/docs/core/workflows" />
</Cards>
